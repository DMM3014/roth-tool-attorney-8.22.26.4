# Changelog — Roth IRA Conversion & Retirement Planner

> Split out of PRD.md 2026-08-15. Newest entries at the BOTTOM.

## Implemented (checkpoint log)
- **2026-02-13 (this session) — Phase 49: AI Reframe — Remove AI-assisted evaluations from client-facing outputs.** Per client-privacy directive, the app no longer offers client-facing AI-generated content. AI functionality is preserved as an **advisor-only working document ("Advisor Commentary")** that can be exported as a standalone PDF or RTF for the advisor's internal file — never bundled into the client PDF.
  1. **Defaults flipped OFF** — `defaultBranding.include_ai_review = false` in both Client Report and SS Report helpers. The toggle relabeled from "Include AI Review section at the end" → "Enable Advisor Commentary (optional) — advisor-only working document; not printed into the client PDF."
  2. **AIReviewPage removed** from Client Report and SS Report printed bodies. `basePages = 9` (was `branding.include_ai_review ? 10 : 9`). PresenterMode slide list also trimmed. The two `AIReviewPage.jsx` files under `clientReport/` and `ssReport/` were deleted.
  3. **Chat with Gemini removed** from Client Report and SS Report cards (`cr-chat-gemini`, `ssr-chat-gemini`, `cr-gemini-privacy-warning` gone). Advisors can still use the AI Insights tab's per-view analyze buttons and their own BYOK Gemini key on advisor-facing surfaces — but nothing routes through the client-facing report cards anymore.
  4. **Advisor-only export** — new `exportAdvisorCommentary(format)` on both Client Report and SS Report. Emits a `.rtf` (Blob download w/ minimal RTF wrapper — opens in Word/LibreOffice) OR a printable PDF (opens a fresh popup window pre-styled for Save-as-PDF via the browser print dialog). Buttons `cr-ai-export-pdf` / `cr-ai-export-rtf` and `ssr-ai-export-pdf` / `ssr-ai-export-rtf` appear only after commentary text is generated. Both PDFs footer with "Advisor working document. Not for distribution to the client."
  5. **AI-use disclosure footnote** printed on every cover page — Client Report (`cr-cover-ai-disclosure`), SS Report (`ssr-cover-ai-disclosure`), Presentation (`pres-cover-ai-disclosure`). Amber-tinted block: "This report is generated by a static tax-planning engine — no AI-assisted evaluations were used to produce the numbers or narrative below. If you wish to run an AI-generated second opinion, you may download this PDF and evaluate it using any AI tool of your choice, at your own expense and subject to that AI program's privacy restrictions and data-handling limitations."
  6. **SS Report anonymize full-coverage fix** — iter65 flagged partial mask (Household snapshot cards, Lifetime chart title, Survivor page, Recommendations still showed real names). Root cause: subpages each read `scenario.household.client_name/spouse_name` directly. Fix: memoized `scenarioForReport` clones the scenario with household names overridden when anonymize=ON, then piped as the `scenario` prop to every subpage. Single-point override; no per-subpage prop drilling. Verified by iter66 counting zero occurrences of "John"/"Jane" in the ss-report preview and print roots when anonymize is ON.
  7. **GeminiKeyCard copy updated** — no more references to "Client Report AI Review" / "SS Report AI Review". Copy now mentions AI Insights buttons + Advisor Commentary.
  - **Tests**: Testing-agent iteration_66 — 100% pass on all 10 Phase-49 requirements. Backend suite unchanged (Phase 47/48 tests still all green: 24 tests pass).

- **2026-02-13 (this session) — Phase 48: EP Flowchart Business-Logic Corrections + Anonymize Reports + QCD Surfacing.** User-directed corrections to the Phase-47 EP Flowchart shipment plus two cross-cutting features. 
  1. **Plan 2 semantics rebase** — Plan 2 renamed "Add Client's Taxable Assets to GST Trust" → **"Distribute Client's Taxable Assets and Roth IRAs to GST Trust"** with Y1 funding order changed from Taxable-first to **Roth-first, then Taxable** (both flow into the client GST trust up to the fed exclusion). Backend key changed from `taxable_first` → `roth_and_taxable`. Plan 3 and Plan 4 headline text also revised verbatim per user's exact wording.
  2. **House-to-trust for Plans 2/3/4** — Survivor's Cash + House balance at Y2 now routes into the spouse GST trust (up to remaining exclusion) in every trust-funded plan (previously only Plan 4). Only the Traditional IRA flows outright to children (matching user's "only Trad IRA passes outright" business rule). Verified: on the default scenario with $0 Trad, `outright_gross` = $0 in Plans 2/3/4.
  3. **"Totals to Trusts and Children" relabel** — Every plan's chip AND "Totals & notes" row now labels the winner metric as **"Totals to Trusts and Children"** (previously "Total to children") — matches the user's directive since the number includes both trust corpus AND outright dollars. PlanFlowchart, PlanComparisonTable, ScenarioCompareOverlay, and PlanRecommendationBanner all updated.
  4. **Anonymize reports toggle** — Advisor Branding gains `anonymize_names` field (default OFF) on Client Report (`cr-anonymize-toggle`), SS Report (`ssr-anonymize-toggle`), and Presentation (`pres-anonymize-toggle`). When ON, every printed page renders the client as "Client" and their partner as "Client Partner" per user's naming convention. Explanation card cites privacy for financial-planning + legal engagements and prevention of AI-attribution to specific individuals.
  5. **QCD surfaced** — Tax Detail table gets a new "QCD (from RMD)" row in the Taxable Income Build section (hidden when the scenario has no QCD activity). Cashflow table's `buildGrid` maps `l.category === "charity"` → `"gift"` so the QCD expense line groups under "Gifts / charity" instead of falling to "Other".
  - **Tests**: `tests/test_phase46_ep_flowchart.py` rewritten to lock the Plan-2 rename + house-to-trust routing (10 cases, all pass); `tests/test_phase47_gst_funding_order.py` retained (8 cases); `tests/test_iter63_ep_flowchart_http.py` updated for the new plan key. All 24 tests pass. Testing-agent iteration_65: 100% of 14 requested surfaces verified — flagged only 1 HIGH bug (SS Report anonymize partial-mask, fixed in Phase 49).

- **2026-02-13 (this session) — Phase 47: EP Flowchart Winner Banner + Scenario Compare + Estate tab Taxable-first Funding Order.** Three connected estate-planning features shipped in one pass.
  1. **Estate tab — Layered GST-Exempt taxable-first funding option**. Backend `project_estate()` accepts a new `gst_funding_order` param (`roth_first` default | `taxable_first`); `_route_to_trust()` accepts the order and routes Taxable first when requested (Roth fills the remainder up to the Y1 fed exclusion). Applies ONLY to the Layered-GST strategy — portability / bypass / qtip_bypass unchanged. API surface: `POST /api/estate/analyze` gains a regex-guarded `gst_funding_order` field (invalid → 422). Estate.jsx exposes a Select `data-testid=estate-gst-funding-order` in the Estate configuration card with a plain-language "Which order wins?" explainer; choice persists in `estate_plan_settings_v2` localStorage. Tests: 8 unit + 5 HTTP pytest cases, all pass.
  2. **EP Flowchart — Plan Recommendation Banner + Print button**. New `PlanRecommendationBanner.jsx` sits above the four stacked plans and headlines the winner by MAX `total_to_children` (user-picked metric over economic-net). Baseline is Plan 1 (No Trust) — when Plan 1 wins, banner styles amber with an explanatory "no-trust delivers the most on this scenario" body; otherwise green with a "+$X.XM more to your children than doing nothing" delta line and a plain-language mechanism sentence ("this plan saves $Y in federal estate tax at the second death; puts $Z inside a GST-exempt trust that stays sheltered at every future generation"). New `flow-print-btn` in the tab header triggers `window.print()`. Banner also mirrors into the first plan page of the Client Report EP-Flowchart section.
  3. **EP Flowchart — Scenario Compare overlay + Client Report paired-comparison pages**. New `ScenarioCompareBar.jsx` (dropdown `flow-compare-select`) lists other saved scenarios in the workspace and, when picked, re-runs the flowchart engine against the comparison scenario's Y1 balances using the CURRENT scenario's shared assumptions (growth / cap-gains / heir-income). `ScenarioCompareOverlay.jsx` renders a 5-row delta card ABOVE each plan's flowchart (total_to_children · in_trust_y2 · gst_exempt_y2 · fet · forgone_step_up), color-coded green when the current scenario wins that metric and red when the comparison wins. Client Report gets a new customization dropdown `cr-flow-scenario-compare-select` (gated behind the EP-flowchart pages toggle) and, when set, emits one paired-comparison page per selected plan (`cr-page-flow-scenario-compare-{n}`) with the overlay above the current-scenario flowchart box. Compare state persists in `client_report_flow_scenario_compare_id_v1` localStorage.
  - **Tests**: `tests/test_phase47_gst_funding_order.py` (8 unit cases, 100% pass) + testing-agent-authored `tests/test_phase47_gst_http.py` (5 HTTP cases, 100% pass). Testing-agent iteration_64: 100% backend and 100% of specified frontend flows verified against live HTTPS.


- **2026-02-12 (this session) — Phase 44: Workbook-parity feature quintet**. Five features landed atop the Phase-43 math corrections, closing the remaining behavioural gaps between the web engine and the client's uploaded workbook + adding advisor tooling. Backend adds 10 pytest cases (all pass); frontend adds two new components under `frontend/src/components/estate/` and touches 2 existing files.
  1. **Basis Merge Toggle** (`tax.merge_basis_at_first_death`, default `True`). New field on the `Plan` dataclass in `backend/projection.py` and new `Switch` on the Plan Inputs "Tax Assumptions & Heirs" card (testid `tax-merge-basis-toggle`). When ON, `_step_up_basis` pools all Taxable accounts into ONE blended-basis line at first death — matching the spreadsheet's Legacy-page convention. When OFF the accounts remain separate and the stepped-up lot is spent first (tax-efficient but produces a small positive delta vs the sheet). Verified: merge=False reduces lifetime taxes by ~$310K and raises heirs' after-tax estate by ~$700K on the default scenario.
  2. **Lifetime Giving Program** (§2503(b) annual exclusion + §2503(e) direct medical/tuition). New `giving` block in `defaults.py` and new full-width card on Plan Inputs (testid `giving-editor`, `giving-annual-gift`, `giving-2503e`, `giving-start-year`, `giving-end-year`, `giving-summary-preview`). Engine drains the Taxable brokerage annually and compounds gifted dollars in a separate family "gift pot" at the heir reinvestment rate (falls back to first Taxable account's return). Response body gains `summary.lifetime_gifted`, `summary.gift_pot_at_second_death`, and a `giving` block containing `annual_pot` (year → `{contributed, cumulative_pot}`), `total_gifted`, `ending_pot`. Verified: $140K/yr gifts over 25 yrs = $3.5M gifted → $19.9M pot at 2nd death (compound growth) and ending estate NW drops by the expected amount.
  3. **Death+10 per-heir breakdown + heir-rate sensitivity matrix** (`estate/DeathPlusTenDetail.jsx`, testids `estate-per-heir-detail`, `estate-per-heir-table`, `estate-heir-rate-matrix`, `estate-heir-rate-matrix-table`, `estate-heir-rate-cell-{i}-{j}`). Per-heir table walks the `BeneficiariesEditor` list, applies each heir's individual `share_pct` + `fed_rate` + `state_rate` to the winner strategy's Trust / Roth / Taxable / Traditional slices. Traditional is reverse-engineered to pretax then re-taxed per heir. 5×4 sensitivity matrix varies federal (22/24/32/35/37%) × state (0/5/9.3/13.3%) with max cell green-highlighted and min amber-highlighted (highlighting suppressed when matrix is uniformly flat — e.g. no Traditional flowing to heirs, which is the Roth-conversion end-state).
  4. **Workbook Sync Report** (`estate/WorkbookSyncReport.jsx`, testids `workbook-sync-report`, `workbook-sync-overall-chip`, `workbook-sync-table`, `workbook-sync-row-{0..3}`, `workbook-sync-expand`, `workbook-sync-explanation`). Renders a 4-row side-by-side card comparing web-engine outputs vs the uploaded "Estate Plan 8.12.26.2xlsm" Legacy-page baseline (IRA-first Y2 $47.5M / Y2+10 $67.6M, Taxable-first Y2 $46.1M / Y2+10 $65.6M). Each row shows delta $, delta %, and a pass/fail chip (< 2% ✅, 2–5% ⚠️, > 5% ❌). Overall drift chip in the header. Expandable "Why the numbers may differ" section explains basis-consumption fix, survivor SS step-up, merge-basis toggle, and residual < 2% drift attributable to progressive state brackets vs blended flat rate and IRMAA 2-year lookback.
  5. **Trust Distribution Assumption panel** (testid `estate-trust-distribution-note`). Single green callout on the Estate tab, above the Death+10 detail, making the trust operating model explicit: "distributes 100% of annual DNI to beneficiaries (taxed at their marginal rates, not the trust's compressed 37%) and passes appreciated assets in-kind (heirs realize LTCG on eventual sale at their own rate). Under this operating model the trust corpus compounds at the client's gross taxable rate without income-tax drag. Actual results will differ if the trust accumulates."
  - **Tests**: new `tests/test_phase44_basis_merge_and_giving.py` (7 unit tests) + testing-agent-authored `tests/test_phase44_http_endpoints.py` (3 HTTP tests). Golden baseline (`tests/_golden.json`) refreshed for the merge-basis-default-ON math. Stale hardcode in `test_phase18_inflation_and_attribution.py::test_conversion_math_unchanged_by_attribution` bumped to the new baseline (6,254,621.64 → 6,351,096.50 lifetime taxes; 84,677,907.94 → 84,563,211.53 NW; 162,719,110.53 → 162,500,588.90 after-tax heirs). Testing-agent verified 10/10 backend + 14/14 required frontend testids present.

- **2026-06 (prior session) — Phase 43: Excel-parity engine corrections (user-verified against uploaded "Estate Plan 8.12.26" workbook)**. User reported the web app ranked Fill-24% Taxable-first ($73.8M) above IRA-first ($73.5M) at Death+10 while the workbook's Legacy page ranked IRA-first ($67.6M) above Taxable-first ($65.6M). Year-by-year trace showed both engines match TO THE DOLLAR through 2048 under IRA-first; all divergence begins when taxable withdrawals start. Root causes found & fixed in `backend/projection.py`:
  1. **Cost-basis consumption (the flip's root cause)**: the engine never reduced basis when taxable dollars were sold, so realized-gain fractions decayed and LTCG/NIIT/state taxes were understated in withdrawal-heavy (Taxable-first) paths. Now every taxable sale consumes pro-rata basis exactly like the workbook's "Basis Consumed" rows (`consumed = wd × (1 − gain%)` on BOY values; `basis_EOY = MAX(0, basis_BOY − consumed) + sweep`). Plumbed `_withdraw` → `_solve_year_conversion` → `YearFlows.basis_consumed` → `_apply_year_flows`.
  2. **Survivor Social Security**: widow(er) now keeps the HIGHER of the two benefits (SSA rule / workbook "survivor = higher benefit"); previously the survivor kept only their own benefit unless they had none. Cashflow line items gain a synthetic "Survivor benefit step-up (higher SS benefit)" row so the tab still ties to `gross_ss`.
  3. **Inherited home in the Death+10 heir model** compounds at the heir taxable-reinvestment rate (heirs sell tax-free via §1014 step-up and reinvest) matching the workbook's `(Taxable+Home)×(1+r_heir)^n`; previously grew at the property's own 3.5%.
  4. A "conversion-priority" variant was tried and REVERTED: the workbook's own CashFlow!r17 formula caps conversions at `MAX(0, bal − RMD − discretionary_wd)` — identical to the web engine; the workbook's 2049 conversion tail is an artifact of Excel's damped (0.7/0.3) iteration, not a priority rule.
  - **DB**: all 9 saved scenarios set to `tax.survivor_spending_reduction = 0.0` per user (workbook input is 0%; built-in default remains 0.2 for new scenarios).
  - **Verification**: on workbook-mirror inputs the engine now ranks IRA-first ($69.8M) > Taxable-first ($69.1M) matching the workbook direction; IRA-first gross estate within 1.3% of Excel. Remaining documented (intentional) differences: web keeps taxable accounts separate at first death and spends the stepped-up lot first (Excel merges into one blended-basis account → higher survivor drag ≈ $1–2M, mostly IRA-first); heir cash sleeve grows gross vs Excel's after-tax interest (negligible); workbook damped-iteration conversion tail (~$200K).
  - **Tests**: new `tests/test_phase43_excel_parity.py` (12 cases: basis consumption unit + workbook-ranking lock + survivor-SS + heir home rate). Golden snapshot regenerated. 5 stale numeric baselines updated (default-scenario lifetime taxes 6,205,089.71 → 6,254,621.64; ending NW 84,567,534.54 → 84,677,907.94; +10 legacy 160,692,156.01 → 162,719,110.53; strategy-sweep winner is now "Fill 35% · 2026–2034" single instead of the 37% phased front-load — front-loading is properly penalized once conversion taxes funded from low-basis taxable realize gains). Full suite: 398 passed / 0 failed (auth modules excluded per fragility convention; master lockout cleared via documented `auth_config.master_lockout` delete).

- 2026-06: Full app MVP — Planner, Projection, Analytics, Optimizer, Monte Carlo, Scenarios.
- 2026-07 (prior fork): Strategy Optimizer, SS Optimizer, White Paper v2, "Save as defaults", V17 alignment, DB backup scripts, `Presentation.jsx` PDF tab, 5-force + PV toggle already existed in nascent form.
- **2026-02-19 (this fork, follow-up) — Roth Timeline Ribbon UI (Phase C companion)**: new `frontend/src/components/estate/RothTimelineRibbon.jsx` component slotted into the Estate tab between the Strategy Detail table and the Multi-Generational Compounding table. Renders a single card with a shared time axis (today → Y2+30) and 4 sections (one per strategy, winner highlighted in the same #4A6741 green used elsewhere). Each strategy contains 1-2 tracks per Roth "slug" derived from `outcomes[k].trust_components` + `household_components.roth_after_estate_tax`. Every track is a 6-px-high shaded bar with color-coded segments: **owner-alive** (dark green, tax-free lifetime compounding), **survivor-holds** (medium green, still tax-free), **trust SECURE 10-yr window** (bright green, funded death starts the clock), **trust post-SECURE** (amber — Roth wrapper terminates, retained income taxed at compressed trust brackets), **outright SECURE 10-yr** (light green, heirs' fresh tax-free window), **outright post-SECURE** (grey — heirs' taxable brokerage with dividend + LTCG drag). Vertical markers anchor Y1 (first death), Y1+10 (trust SECURE expiry), Y2 (second death), Y2+10 (outright SECURE expiry). Per-track annotations show the funding year + funded $ (e.g. "Funding 2056: $8,429,394" for a bypass Roth). Advisors can now answer "when does each Roth stop being tax-free under each strategy?" at a glance instead of parsing multi-generational tables. All 4 strategies render on the default plan (Portability shows 1 outright slug, Bypass/QTIP+Bypass show 2 slugs each, Layered GST shows 2 trust slugs with independent SECURE clocks from Y1 and Y2). Fixed a `Math.min(secureEnd, null) === 0` coercion bug that initially collapsed the trust SECURE segment on all trust strategies. Zero backend changes — 100% derived from data already in the `analyzeEstate` response. Test IDs: `estate-roth-timeline`, `estate-roth-timeline-{portability,bypass,qtip_bypass,gst_layered}`.
- **2026-02-19 (this fork, follow-up 3) — Trust growth model revision: distribute-to-beneficiaries assumption**: replaced the compressed-bracket drag on trust NAV in `backend/estate.py` per the user's explicit specification that "all assets in trust are assumed to grow at the client taxable account rate without deduction for income taxes or capital gains." Rationale: well-drafted irrevocable trusts distribute ordinary income to beneficiaries annually (taxed at their rate, not the trust's compressed 37%) and pass appreciated assets in-kind (beneficiaries realize LTCG at their own rate on eventual sale), so from the trust's NAV perspective there is no internal drag.
  - `_grow_roth_in_trust` and `_grow_taxable_in_trust` both simplified to a single gross-rate compounding curve `v × (1 + gross_rate) ^ years`. The `_post_secure_effective_rate` helper is deleted (dead code); constants `TRUST_ORD_TAX_RATE` / `TRUST_LTCG_TAX_RATE` / `TRUST_INCOME_YIELD` / `TRUST_TURNOVER` are retained as documentation. The eventual heir-side 15% LTCG haircut on trust-internal appreciation (`_after_tax_taxable`) is unchanged — it correctly captures the tax beneficiaries owe on eventual in-kind sale.
  - The multi-generational compounding narrative in `Estate.jsx` now mirrors the user's exact wording, including the two-part explanation (distribute-ordinary-income + distribute-appreciated-assets-in-kind) followed by the "grow at client taxable account rate without deduction" assumption.
  - Roth Timeline Ribbon (`RothTimelineRibbon.jsx`) segment labels + narrative caption updated: the amber "post-SECURE" band now reads *"trust distributes income to beneficiaries — NAV compounds at client rate"* rather than *"compressed trust brackets on retained income"* (the visual segment remains — it still marks the moment the Roth wrapper terminates and full-distribution obligation is met).
  - Pairwise Waterfall (`PairwiseWaterfall.jsx`) driver hints for Δ Outright Roth and Δ Outright Taxable rewritten to describe the revised trust regime. Reading-tip caption purged of "compressed-bracket drag."
  - `_run_bypass` and `_run_gst_layered` outcome-notes rewritten to the new "trust NAV compounds at client rate + heirs owe LTCG on in-kind sale" narrative.
  - VBA `EstatePlanFixes.bas` UDFs updated: `SECURE_ROTH_TRUST` and `TAXABLE_IN_TRUST` now compound at gross for all years. New `SECURE_ROTH_TRUST_RETAINED` UDF preserves the old (worst-case, retained-income, compressed-bracket) curve for advisors who want to compare. `PostSecureRate` private helper retained with an updated header comment explaining it is documentation-only.
  - **Tests**: 4 tests in `test_phase41_estate.py` rewritten to match the revised model — trust-Roth and trust-Taxable now compound identically at gross rate; the "drag applies after SECURE" and "drag from year 1" assertions are replaced with equality-to-gross-compounding assertions. New `test_roth_and_taxable_in_trust_compound_identically` locks the invariant in place. `test_roth_first_funding_all_deceased_roth_into_bypass_when_room` updated to expect trust-Taxable at Y2 = gross-compounded value (was: below naive due to drag).
  - **Verified live** via curl @ h+30y: default plan now shows Portability=$179.2M, Bypass=$230.9M (+$51.7M edge), Layered GST=$275.2M (+$96.1M edge) — up from +$19.9M / +$39.8M under the prior drag model. Trust strategies materially more attractive because the compounding curve is no longer clipped after year 10.

- **2026-02-19 (this fork, follow-up 2) — Pairwise Trade-off Waterfall (Phase D)**: new `frontend/src/components/estate/PairwiseWaterfall.jsx` slotted into `Estate.jsx` directly below the Roth Timeline Ribbon. One card, top-right controls (Horizon tabs Y2/+10y/+20y/+30y + Baseline dropdown), 3-column grid (one waterfall per non-baseline strategy). Each waterfall decomposes the total delta between the baseline and the target strategy into four economically-meaningful drivers at the selected horizon, all derived from the Phase C per-vehicle horizon breakdown: **ΔTrust wealth** (trust-held assets that escape 2nd-death + subsequent-generation estate + GST tax), **ΔOutright Roth** (SECURE clock + heir brokerage regime vs. trust compressed brackets), **ΔOutright Taxable** (§1014 step-up capture vs. trust-locked basis + heir brokerage vs. trust turnover LTCG), **ΔOutright Traditional** (SECURE 10-yr drawdown at heir rate — usually ~$0 since no strategy routes IRAs into trust). Sum of drivers = headline delta by construction (verified on the default plan: Bypass = +109.5M −59.6M −30.0M +0 = +$19.9M ✓ ; Layered GST = +219.0M −119.1M −60.1M +0 = +$39.8M ✓). Horizontal bar rows with positional offset (each delta bar's left edge = running total from previous row), green fill for positive contributions, red for negative, neutral baseline + winner-highlighted final bar. Baseline dropdown lets advisors flip to compare against any of the 4 strategies (default: Portability = the "do nothing" comparison the Excel model reviewers cited). Zero backend calls — 100% derived from `post_death_horizons` rows already populated by the Phase C engine work. Test IDs: `estate-pairwise-waterfall`, `estate-waterfall-{bypass,qtip_bypass,gst_layered}`, `waterfall-horizon-tabs`, `waterfall-baseline-select`.


- **2026-02-19 (this fork) — Phase A + Phase C: OBBBA exclusion rebase + Roth-as-first-class-vehicle in the outright bucket** (`backend/estate.py` + `tests/test_phase41_estate.py`). Two independent engine corrections, delivered in one pass:
  - **Phase A (fed exclusion rebase)**: `FED_EXCLUSION_BASE` = **$15,000,000** at 2026 (OBBBA statutory), chained-CPI indexed forward at 2.4%. Pre-2026 planning years fall back to the pre-OBBBA $13.99M-at-2025 base for historical continuity. New constants `PRE_OBBBA_BASE`, `PRE_OBBBA_BASE_YEAR`. `state_exclusion` gains its own `STATE_EXCLUSION_BASE_YEAR = 2025` anchor so the federal rebase doesn't shift the state indexing curves.
  - **Phase C (outright Roth ledger)**: New helpers `_heir_effective_rate`, `_grow_outright_roth`, `_grow_outright_taxable_stepped`, `_grow_outright_traditional`. Every strategy outcome now exposes a `household_components` dict `{roth_after_estate_tax, taxable_after_estate_tax_stepped, traditional_after_estate_tax_gross}` — the estate/state-tax haircut is proportionally apportioned across the three vehicles so each can be compounded with its OWN post-Y2 clock. The `post_death_horizons` loop now uses:
    - **Outright Roth**: full gross rate for years 0..SECURE_YEARS (tax-free window intact), then heir-taxable-brokerage drag (ordinary tax on dividend yield + 15% LTCG on turnover-realized appreciation).
    - **Outright Taxable**: §1014 step-up at Y2 → basis reset → heir-taxable-brokerage drag from year 1.
    - **Outright Traditional**: proper SECURE 10-year linear drawdown — each year 1/(remaining_secure) is distributed, taxed at `heir_rate`, net lands in the heir brokerage which compounds at the effective heir rate; past year 10 only the brokerage side compounds. Previously the entire household bucket was a single blob compounding at the survivor's gross rate forever, which (a) let outright Roth escape the SECURE cutoff, (b) gave inherited Taxable gross-rate growth instead of brokerage drag, (c) collapsed Traditional to a single-year Y2 haircut. Fix closes the gap flagged in `memory/excel_model_review_2026-06.md`.
  - Each horizon row now carries additive `${name}_household_roth`, `${name}_household_taxable`, `${name}_household_traditional` fields so the frontend can render per-vehicle timeline ribbons. Existing `${name}_household`, `${name}_trust`, `${name}_total`, `household_after_tax_at_y2` keys unchanged (h=0 aggregate exactly matches the immediate-liquidation view).
  - **Tests**: `test_phase41_estate.py` — 2 updated (fed exclusion tests to reflect 2026 base) + 10 new: OBBBA base year, pre-OBBBA fallback, indexed CPI, outright Roth within/after SECURE window, outright Taxable heir brokerage drag, outright Trad h=0 matches liquidation, outright Trad SECURE beats immediate-liquidate, heir effective rate bounds, `household_components` populated on all 4 strategies, horizon per-vehicle breakdown sums to aggregate, portability h=20 below legacy naive blob, h=0 aggregate == household_after_tax_at_y2. **39/39 in `test_phase41_estate.py`; 71/71 across estate + state + state_tax phases**.
  - **Excel VBA macro delivered**: `/app/memory/excel_fix_macro.bas` — importable one-shot macro that patches the three Excel bugs surfaced in the review (`'Estate Plan'!C26` row-36 double-count → row 28, `EP Projections'!H40` Plan-1 sign flip on H36, `Legacy!B66` SECURE-clock anchored to trust funding year instead of second death) + adds four cell-usable UDFs (`=SECURE_ROTH_TRUST`, `=TAXABLE_IN_TRUST`, `=OUTRIGHT_ROTH_HEIR`, `=OBBBA_FED_EXCLUSION`) that mirror the corrected engine so the workbook's Roth compounding, trust-bracket drag, and OBBBA exclusion match the web app to the dollar. Idempotent (each fix guarded by a pre-condition check + logged to a "Fix Log" sheet).

- **2026-02-18 (this fork) — Roth inherited-tax language accuracy pass**: revised every client-facing use of "Roth income-tax free forever" / "tax-free forever" (trust context) to correctly reflect the SECURE Act 10-year distribution window and the compressed-trust-bracket handoff. New shared helper `frontend/src/lib/rothTrustCaveat.js` exports `ROTH_TRUST_CAVEAT_SHORT`, `ROTH_TRUST_CAVEAT_LONG`, and `TRUSTEE_DISTRIBUTION_NOTE`. Files touched: `Estate.jsx` (main tab intro + chart caption + horizons caption + new "Trustee planning note — compressed brackets favor distributions" callout card + Traditional-IRA warning caveat), `clientReport/EstatePlanningPage.jsx` (page intro + SVG flowchart labels + horizons Sub caption + Traditional-IRA warning + new trustee planning-note callout), `clientReport/BasisStepUpPage.jsx` (header comment + intro Ps + Roth row cell label + Sub footnote + Roth-opportunity callout + funding-order guidance for Roth), `clientReport/LegacyPage.jsx` (H2 intro + new SECURE trust caveat Sub + Nominal/PV chart legend labels + paired-chart caption), `ClientReport.jsx` (SECURE Gemini-second-opinion narrative), `Presentation.jsx` (page 5b composition narrative + page 5 wealth line-chart narrative + Sub + tax-cost narrative + Recommendations funding-order block + "convert more in down markets" copy + `FundingOrderRationale` comment + `driverPhrase` primary-Roth branch), `AnalyticsCharts.jsx` (CumulativeTaxChart subtitle + PV/Nominal net-to-family chart subtitles + legend labels), `ProjectionCharts.jsx` (inherited-Roth area chart legend), `ProjectionPanels.jsx` (LegacyPanels headline caption + estate KPI card label + ConvertCompareChart caption), `FundingOrderCompare.jsx` (heir tooltip + Roth-to-heirs sub-row labels), `ssReport/CoordinatedClaimingPage.jsx` (metric-definition Sub), `AIInsights.jsx` ("Net to family?" query prompt). Language rule: (a) owner's lifetime Roth compounding stays "tax-free" (accurate); (b) inherited/heir/trust references now say "income-tax free through the SECURE Act 10-year distribution window; retained trust income thereafter taxed at compressed trust brackets (37% federal above ~$16K)"; (c) advisor guidance added: trustees typically distribute ordinary income (DNI carry-out) and appreciated assets in-kind so beneficiaries' lower marginal rates apply rather than the trust's 37% top bracket. Existing 32 pytest cases in `test_phase41_estate.py` + `test_phase42_state_move.py` pass unchanged (pure client-side language revision — zero backend/engine change).
- **2026-07-31 (this fork, Phase 41)**: **Estate Planning Analysis — Layered GST-Exempt Trust with DSUE Portability + State Estate Tax**.
  - **Engine** (`backend/estate.py`): new module implementing federal + 12-state-plus-DC estate/GST tax (CT/DC/HI/IL/MA/MD/ME/MN/NY/OR/RI/VT/WA), federal exclusion chained-CPI indexed forward ($13.99M base 2025, permanent per OBBBA), per-state exclusions with own CPI indexing (MA/OR/IL frozen; CT/ME/RI/DC/WA index at 2.4%). Proper year-by-year DSUE portability (Form 706 election) — DSUE = Y1 exclusion − Y1 gifted, frozen at Y1 value (never re-indexes). NY cliff (>105% of exclusion loses full exclusion) implemented explicitly. HI + MD portability supported. `project_estate()` returns baseline (no trust) vs. layered strategy (GST trust #1 funded at first death up to fed exclusion, surviving spouse as discretionary beneficiary; GST trust #2 funded at second death up to remaining exclusion incl. DSUE) with net-to-heirs deltas at second death and +0/+10/+20/+30 post-death horizons.
  - **API**: `GET /api/estate/state-metadata` (12 states + DC dropdown data with cliff/portability flags) and `POST /api/estate/analyze` (main projection endpoint, rate-limited 60/min).
  - **Estate tab** (`components/Estate.jsx`): auto-derives death years + trust-eligible balances (Roth + Taxable ONLY — never Traditional) from base projection. Verdict header adapts to whether the couple is over or under combined exclusion. Inputs card: state dropdown (13 rows), trust growth rate slider, 3 GST toggles (fund at first death / fund at second death / DSUE portability). Charts: first-death bifurcation KPI grid, dual-line growth-between-deaths, side-by-side stacked bar for distribution at second death, multi-generational projection table.
  - **Client Report Estate page** (`components/clientReport/EstatePlanningPage.jsx`): toggled via new switch in `MonteCarloBehaviorCard` ("Include Estate Planning print page"). Renders verdict KPIs, first-death bifurcation cards, second-death comparison table (strategy vs. baseline with delta row), multi-gen table, TWO STATIC SVG FLOWCHARTS side-by-side ("Without GST trust" showing marital ded → survivor → taxed at Y2; "With layered GST-exempt trust" showing bifurcation → surviving-spouse-as-discretionary → GST #2 at Y2 → dynasty compounding), amber "do not leave Traditional IRA to trust" warning callout with the 37%-at-$16K bracket compression stat, and legal caveat requiring consultation with qualified attorney.
  - Test file `tests/test_phase41_estate.py` — 24 tests covering fed exclusion indexing, all 12 states + DC, NY cliff, WA highest rate, DSUE portability, use_portability=false, state exclusion frozen (MA/IL/OR) vs. indexed (CT), IRS Form 706 DSUE freeze rule, multi-generational compounding, reversed death-year auto-swap. Golden snapshot unchanged (estate engine is purely additive).
- **2026-07-29 (this fork, Phase 39–40)**: **Full State Income Tax Engine (all 50 states + DC)** and **ClientReport/MonteCarlo refactor**.
  - **Phase 39 — State tax engine**: NEW module `backend/state_tax.py` with `STATE_TAX_RULES` dict covering 50 states + DC and `compute_state_tax()` function. Supports (a) 9 no-income states (AK, FL, NV, NH, SD, TN, TX, WA, WY → $0), (b) 14 flat-rate states (CO, IL, IN, KY, MI, MS, NC, PA, UT, LA, ID, GA, OK, VA, AZ), (c) 25 progressive states (CA, NY, NJ, HI, OR, MN, DC, WI, MD, CT, MT, NM, ME, VT, RI, SC, OH, MA, IA, AL, AR, MO, KS, NE, DE, ND, WV). Handles binary retirement-income exclusions (SS-exempt in ~41 states; pension-exempt in IL/MS/PA/AL/HI/IA/MA; IRA-exempt in IL/MS/PA/IA) plus age-gated partial exclusion caps (e.g. NY $20K/person over 59½ MFJ-doubled; NJ $100K over 62; KY $31K; CO $24K/person 55+; DE $12.5K over 60; GA $65K over 65; MD $40K over 65). Model: `state_AGI = federal_taxable + federal_std + senior_bonus − excluded_items; state_taxable = state_AGI − state_std_deduction; tax = brackets_or_flat(state_taxable)`. Wired into `tax_engine.compute_year_tax()` (populates new `state_detail` field with `state_ss_excluded`, `state_pension_excluded`, `state_ira_excluded`, `state_marginal_rate`, `state_effective_rate`). `projection.py` `_income_from_stream()` now returns 4-tuple with a new `pension` component (detected via type/description regex + annuity streams) so it can be excluded state-side. Legacy behavior preserved: empty `state_code` falls through to `state_rate × federal_taxable` to the cent (verified `test_backward_compat_empty_state_code_matches_pre_refactor`). `GET /api/states` endpoint enriched with `tax_meta` (type + supported + exempts_ss/pension/ira + flat_rate + std_ded + retirement cap). `PlanInputs.jsx` state selector now shows richer badges (Progressive brackets / Flat X% / No income tax / Community Property / Exempts SS/Pension/IRA / $Nk retiree exclusion) and disables state_rate manual input with an explanatory message when a state is set. `TaxDetail.jsx` adds a green state-code chip on the State tax row plus 5 sub-rows: Excluded: SS (−$), Excluded: pension (−$), Excluded: IRA distributions (−$), Retirement exclusion cap (−$), State taxable income. Golden snapshot regenerated (only additive diff: `state_detail` sub-key). Concrete verified sample: default retiree household lifetime state tax = $951K (empty/3.99% flat) → $1.98M (CA progressive) → $0 (FL) → $150K (PA, retirement-exempt) → $172K (IL retirement-exempt). New test file `tests/test_phase39_state_tax.py` — 20 unit + integration cases all pass.
  - **Phase 40 — Refactor**: Extracted 3 large inline JSX blocks (~250 lines total) into dedicated components.
    - `ClientReport.jsx`: 921 → 723 lines. New `clientReport/MonteCarloBehaviorCard.jsx` (halt + guardrail + regime toggle) and `clientReport/ReportCustomizationCard.jsx` (custom milestones + state exclusions + PV slider). Moved `suggestMilestoneYear` helper into `clientReport/helpers.jsx` for reuse.
    - `MonteCarlo.jsx`: 715 → 654 lines. New `monteCarlo/BehaviorRuleCards.jsx` with `GuardrailCard`, `HaltCard`, `RebalanceCadenceCard` named exports (all data-testid values preserved: `mc-guardrail-card`, `mc-halt-card`, `mc-rebalance-card`, `mc-guardrail-toggle`, `mc-halt-toggle`, `mc-halt-drop`, `mc-halt-resume`, `mc-rebalance-annual/biennial/never`). Zero behavior change — pure code motion.
    - Verified end-to-end by `testing_agent` iteration_60: 100% backend + frontend pass, no regressions, no console errors, all refactored testids render on their respective tabs, MC simulation still succeeds with 98.6% success rate under halt+guardrail rules.
- **2026-07-26 (this fork, follow-up 6)**: **State-Taxable Chart + Milestone Custom Years + Legacy PV Slider** — three related Client Report customization features.
  - **State-taxable chart**: `IncomeExpensesPage.jsx` now emits a `cr-statetax-stacked-chart` below `cr-milestone-fedtax` mirroring the federal chart's series but zeroing categories flagged by `stateExclusions` (`ss`, `pension`, `rmds`). Milestone table gains a "State taxable income" row with a small "(SS + pension excluded)" tag; row value = federal taxable − sum of excluded buckets. Display-only — the projection engine's state tax still uses `state_rate × federal taxable income`.
  - **Milestone custom years**: `pickMilestoneRows(rows, custom)` accepts up to 3 `{name, year}` entries; merges chronologically between Today (always first) and End of plan (always last); out-of-range years clamp to plan bounds. Toolbar UI: 3 pairs of name/year inputs (`cr-milestone-name-N`, `cr-milestone-year-N`).
  - **Legacy PV rate slider**: `pvSeries(withRoth, noRoth, scenario, overrideRate)` signature added; `LegacyPage` accepts `pvDiscountRateOverride` prop that flows from toolbar `cr-pv-rate-slider` (range 0–10%, 0.25% step) with `cr-pv-rate-display` + `cr-pv-rate-reset`.
  - All three state values persist per-browser via localStorage (`client_report_milestones_v1`, `client_report_pv_rate_v1`, `client_report_state_exclusions_v1`). `scenario.state_exclusions` also seeded in `backend/defaults.py`.
  - Verified via `testing_agent_v3_fork` iteration_56 (retest after fixing 2 blocking bugs from iteration_55: TDZ on `excl` before init + undefined `setScenario`): 100% frontend success. Concrete data: with SS excluded (default) state-taxable = [$423.6k, $533.1k, $490.7k, $619.8k, $0] vs federal [$423.6k, $565.5k, $571.9k, $728.9k, $5.3k]; slider 3%→10% shrinks PV from $14.7M to $1.06M; "Retirement 2029" milestone inserts chronologically between Today and +5.
- **2026-07-26 (this fork, follow-up 5)**: **Net-to-Family Nominal vs PV pair** and **Excel-style Federal Taxable stacked chart on Client Report**.
  - `AnalyticsCharts.jsx` — renamed titles to explicit "Net to Family at Second Death — Nominal (+ N yrs)" and "— Present Value"; `HeirLegacyCompareChart` now accepts a `span` prop.
  - `Analytics.jsx` — relocated `PvNetToFamilyChart` to the bottom next to `HeirLegacyCompareChart`, both span=1 so they render truly side-by-side.
  - `LegacyPage.jsx` (Client Report) — new `cr-legacy-nominal-pv` block right after the verdict box: two side-by-side horizontal stacked bar charts (Nominal + PV) computed via `pvSeries(withRoth, noRoth, scenario)` from `api.js`; shows tax-free Roth vs other after-tax dollars with the same-plan/two-framings narrative. On the default plan: $47.95M nominal → $14.7M PV for With Conversions.
  - `IncomeExpensesPage.jsx` (Client Report) — new `cr-fedtax-stacked-chart` year-by-year stacked bar chart placed ABOVE the existing `cr-milestone-fedtax` table with matching bucket names (Wages, Pension/annuity, Interest, Dividends & LTCG, RMDs (taxable portion), Social Security (taxable), Other ordinary, Roth conversion) so the visual and the milestone table use the same legend labels.
  - Verified via `testing_agent_v3_fork` iteration_54: 100% frontend success rate. Both charts render truly adjacent on Analytics (identical y-position 4489, height 380); Legacy page's paired charts render with correct $-values matching the verdict; stacked chart appears above the milestone table (verified with compareDocumentPosition) with all 8 legend series populated.
- **2026-07-26 (this fork, follow-up 4)**: **Tax Detail: Standard Deduction + Taxable Income rows** and **Client Report: Milestone snapshot tables**.
  - `TaxDetail.jsx` LINE_SPECS gained two rows in the `taxable` section (after MAGI): `Standard deduction` (muted, getter returns the negated value so both the grid and the CSV/XLSX export stay consistent) and `Taxable income` (strong/bold). Data sources: `r.tax_detail.standard_deduction` + `r.taxable_income` (both already exposed by projection).
  - `IncomeExpensesPage.jsx` (Client Report page 5) gained two milestone-snapshot tables below the two existing bar charts. New helpers `pickMilestoneRows(rows)` (returns rows at Today / +5 / +10 / +20 / End of plan), `sumByKind(items, kinds)`, `sumExpenseCat(items, cats)`.
    - **Table A `cr-milestone-cashflow`**: Income (Wages, SS gross, Dividends, Interest, RMDs, Withdrawals) → Total income → Spending (Spending, Housing, Health, Insurance, Gift, Taxes, Other) → Total spending → Net cash flow (parentheses if negative).
    - **Table B `cr-milestone-fedtax`**: Wages, Pension/annuity, Interest, Dividends & LTCG, RMDs (taxable), SS (taxable), Other ordinary, Roth conversion → (Standard deduction) → Taxable income. Sources computed by bucketing `row.line_items.income[]` by `kind`.
  - Verified via `testing_agent_v3_fork` iteration_53: 100% frontend success rate. Math checks confirmed (Taxable income = Ordinary + Preferential exactly; Federal taxable = sum of source rows − standard deduction exactly). Screenshots captured.
- **2026-07-26 (this fork, follow-up 3)**: **Recovery histogram + Guardrail persistence card + Regime Comparison PDF page** — three parity/reporting enhancements:
  1. **Recovery histogram**: `_simulate` now tracks first-resume year per trial (`resume_idx`); `halt_info` gained `resume_year_counts[T]`, `trials_resumed`, `resumed_pct`, `median/p10/p90_resume_year`. The `HaltHistogramCard` overlays an amber "halts lifted" bar series next to the green "halts triggered" bars and adds a 3-stat sub-strip (`mc-halt-resume-stats`) showing "Trials Resumed 751/807 (93.1%), Median Resume Year 2037, P10→P90 Resume 2030→2045" when recovery is on. Non-breaking when recovery is off.
  2. **Guardrail persistence card**: `guardrail_info` gained `trials_with_cuts`, `trials_with_cuts_pct`, `p10/p90/max_cut_years`, `mean_cut_years`. New `GuardrailPersistenceCard` (testid `mc-guardrail-persistence-card`) renders symmetric to the halt card: two stat strips showing success-without/with, lift, trials-with-cuts%, mean/median/P10→P90/max cut years — so advisors see BOTH the benefit and the cost of the guardrail, not just the headline success%.
  3. **Regime Comparison PDF page**: New `RegimeCompareReportPage` (testid `cr-page-regime-compare`) in the Client Report. Added `cr-regime-toggle` on the report toolbar (persisted to localStorage as `client_report_regime_v1`). When on, ClientReport auto-fetches `runRegimeCompare` with `include_no_behavior_pair` set whenever guardrail or halt is active, and inserts the new page at position 9 (pushing Legacy to 10 and AI Review to 11). Print page renders each regime with success%, Δ vs baseline, P10/P50/P90 ending wealth, "Your Baseline" / "Worst Case" / "★ winner" badges, and paired sub-rows with "+N pts from behavior" chips.
  4. **Tests**: 3 new pytest cases (halt_resume_histogram, halt_resume_zero_when_permanent, guardrail_persistence_metrics) — 14/14 pass. Verified via browser automation: halt shows 807/1000 triggered with 751 resumed (93.1%) median resume year 2037; guardrail shows 999/1000 ever-cut trials with median 9 cut years; regime page renders correctly at page 9 with all 6 regimes ranked. Screenshots captured.
- **2026-07-26 (this fork, follow-up 2)**: **Halt recovery + Rebalance cadence + Regime paired-behavior** — three related MC realism upgrades:
  1. **Halt recovery rule**: `ConversionHaltSpec` gains `resume_after_positive_years` (0–20; default 0 = permanent halt). `_simulate` now runs a per-trial state machine: once halted, count consecutive positive-return years; hitting the threshold un-halts, letting the trial re-enter conversions until another qualifying drop hits. First-trigger year still reported. Frontend: new `mc-halt-resume` input inside the halt card; Client Report methodology auto-updates.
  2. **Rebalance cadence**: `RebalanceSpec` (annual / biennial / never) added. `_historical_factors` + `_portfolio_factors` now return per-class factor arrays (n×T×3); new `_apply_rebalance_cadence()` builds a drifted-weights aggregate return path when annual rebalancing is off. Log-mean re-scaling preserves the anchor-to-plan adjustment. Frontend: new `mc-rebalance-card` (segmented Annual / Biennial / Never), reported in the Client Report methodology ("Rebalancing: biennial (weights drift 1 year, snap back)" etc).
  3. **Regime-compare paired behavior**: `RegimeCompareRequest.include_no_behavior_pair` runs each preset twice (with + without guardrail/halt) at the same seed; frontend renders indented paired sub-rows with a "+N pts from behavior" chip. E2E verified: 70s-stagflation regime shows **+6.6 pts** lift from guardrail+halt, historical_avg only **+2 pts**.
  4. 11/11 halt pytest cases pass (5 new). UI verified via screenshot.
- **2026-07-26 (this fork, follow-up)**: **Halt Overlay + Allocation + Client Report toggles** — four related enhancements shipped in one pass:
  1. **Halt histogram**: `montecarlo.py` now emits `trigger_year_counts[T]` alongside the existing halt aggregates. `MonteCarloResults.jsx` gains a new `HaltHistogramCard` (testid `mc-halt-histogram-card`) with per-year bar chart, sand-shaded conversion-window boundaries (dashed reference lines at window start/end), green bars inside the window vs grey outside, peak-year callout, and a 3-stat strip (trials triggered / median trigger year / P10→P90). A compact `mc-halt-info` chip also appears at the top of the results panel showing "Halt ≥N% drop: triggered in X% of trials · median year YYYY". New pytest `test_halt_year_counts_histogram` locks invariants (length matches years, sum == trials_triggered, no triggers before window opens).
  2. **Client Allocation input**: New `allocation-card` on Plan Inputs (household-level stocks/bonds/cash weights, testids `alloc-stocks/bonds/cash/sum`), defaults 60/30/10, persisted on the scenario as `scenario.allocation`. `defaults.py :: DEFAULT_SCENARIO` grew a matching allocation block. New `allocationToAssets(alloc)` helper in `api.js` folds the weights into the `assets` payload (using DEFAULT_ASSETS mean/vol for each class); auto-normalizes and falls back to null → server DEFAULT_ASSETS when the sum is zero. `MonteCarlo.jsx` now seeds its internal `assets` state from `scenario.allocation` on mount so switching allocation on Plan Inputs flows naturally into the MC. `ClientReport.jsx` runMc call passes `allocationToAssets(scenario.allocation)` so the printed report always reflects the household's actual mix.
  3. **Halt in Client Report + Guardrail in Client Report**: New `cr-mc-behavior-card` on the Client Report toolbar with two side-by-side toggles (`cr-halt-toggle` + `cr-halt-drop`; `cr-gr-toggle` + `cr-gr-cut`), both persisted to localStorage (`client_report_mc_halt_v1`, `client_report_mc_halt_drop_v1`, `client_report_mc_gr_v1`, `client_report_mc_gr_cut_v1`). Toggling either re-runs the report's MC and the methodology block auto-updates (halt copy switches from "LOCKED" to "adaptive — halt on ≥N% YoY drop (triggered in X% of trials)"; guardrail line switches from "OFF" to "active").
  4. Verified end-to-end via browser automation: on the default plan, halt at 10% threshold fires in **81.6%** of 1000 trials (median 2034, peak 2027 with 72 trials, P10→P90 window 2028→2045); histogram renders correctly with sand-shaded window bounds; all four new UI cards present. 7/7 halt pytest cases pass.
- **2026-07-26 (this fork)**: Completed the beneficiary transfer — `ClientReport.jsx` no longer imports/gathers any beneficiary or heir-rate data. Removed: heir-fed/heir-state override card (`cr-input-heir-fed`, `cr-input-heir-state`, `cr-reset-heir-rates`), read-only beneficiaries summary (`cr-beneficiaries-summary`), all related state/effects (`heirFedOverride`, `heirStateOverride`, `beneficiaries`, `weightedBene`, `overrideScenario` memo). Client Report now consumes `scenario.legacy.heir_federal_rate + heir_state_rate` directly from Plan Inputs; individual beneficiary editing lives ONLY in `PlanInputs.jsx → Tax Assumptions & Heirs` via `BeneficiariesEditor.jsx`. Verified via browser automation.
- **2026-07-18 (this session)**:
  - **UX polish — cost-basis cell**: `PlanInputs.jsx` — cost-basis input renders as muted "N/A" for account types where basis is irrelevant (Cash, Tax-Deferred, Tax-Free) and stays editable for Taxable / Real Estate. Engine already ignored the field for those types — zero backend change.
  - **UX polish — stop-date defaults**: `PlanInputs.jsx` — empty stop_date fields in Income Streams and Expenses now display the computed **second-death date** (max(dob + life_expectancy) across both spouses) in muted italic. Data stays null (engine already treats null as "run through end_year" which equals second-death year), so no engine change needed.
  - **Strategy Optimizer explainer rewrite**: `StrategyOptimizer.jsx` — replaced the "aggressive strategy wins" collapsible with a cleaner "Why does the optimizer favor early high-tax conversions?" panel structured around 5 explicit structural forces:
    1. Tax-free compounding runway
    2. RMD wall (SS 85% taxability, IRMAA, NIIT cascade)
    3. Widow's tax cliff (MFJ → Single compression)
    4. Heir bracket arbitrage (SECURE 10-year)
    5. IRAs get no §1014 step-up (IRD) — newly added
  - **Strategy Optimizer PV toggle enhancement**: Added "Rank by" label + clearer "PV (today's $)" button labels + hover tooltips so users notice they can re-sort by PV instead of nominal legacy.
  - **Funding-order refinement (NEW FEATURE)**: `strategy_optimizer.py` — after the main sweep, optionally take the top-2 strategies at each material bracket (37/35/32/24/22) and re-run each against all 3 funding orders. Returns comparison structure with winner, improvement $ and %, and any_improvement flag. Added `_refine_funding_orders()` + `refine_funding_orders` param on `strategy_sweep()`. Wired through `StrategySweepRequest`, `runStrategySweep()`, and a new UI toggle + results panel with row highlights + "Largest gain" callout. Compute cost: ≤20 extra projections (~1.2s).
  - **BUG FIX** during the above feature: initial refinement code used wrong config key `cfg["withdrawals"]` (plural) but the engine reads `cfg["withdrawal"]` (singular) at `projection.py:711`, so alternative funding orders were silently ignored — all three orders returned identical legacies. Fixed by 2-line key rename. Added regression pytest `test_funding_orders_produce_distinct_results` to guarantee this never regresses. After fix: default scenario shows +$1.9M–$4.1M improvements (1.3%–2.6%) when switching from IRA-first to taxable-first funding — validating the user's intuition that taxable-first preserves IRA balance for more Roth conversion room and more tax-free compounding.
- **2026-07-23 (this session, fork continuation)**:
  - **Detail / Cashflow + Tax Detail — pivoted vertical → horizontal**: All three
    year-by-year tables now use a spreadsheet-style layout: **line items as rows,
    years as columns**, matching the existing Cashflow grid view. Left column is
    sticky (line-item label), top row is sticky (years). Sections use colored
    accent bars + subtotal rows. Files changed:
      - `frontend/src/components/DetailCashflow.jsx` — full rewrite of Account
        Detail table + Cashflow table. Account Detail pivots to accounts-as-rows
        grouped by tax-type (Cash / Taxable / Traditional / Roth / Real Estate)
        with per-group subtotals and a bold Net Worth row. Heir columns (10-yr
        SECURE horizon) are shaded orange and show em-dash on individual accounts
        (they merge/step-up at second death) while bucket subtotals carry through.
        Cashflow pivots to CF_LINES-as-rows with 4 sections (Income / Expenses /
        Funding drawn / Net reconciliation). Surplus row bolded, colored red for
        shortfalls, green for surplus. Excel/CSV exports pivot to match display.
      - `frontend/src/components/TaxDetail.jsx` — replaced years-as-rows / cols-
        of-tax-items table + per-row expand drawer with a horizontal grid.
        Sections: TAXABLE INCOME BUILD → FEDERAL TAX → STATE & MEDICARE → bold
        Total tax + Medicare → RATES → CALLOUTS row (per-year chip dots with
        hover tooltips). LTCG dominant-band chip (0% / 15% / 20%) renders inline
        on the "Preferential taxable" row. Column headers show year + age(s).
        Excel/CSV exports pivot to match display.
  - **Zero backend changes** — engine `line_items` and `tax_detail` payloads
    already supplied everything needed; this was a pure view-layer rework.
  - **Grid readability — density + currency scale toggles**: Added a shared
    `<GridControls>` toolbar (`frontend/src/components/grid/GridControls.jsx`)
    with two toggle groups:
      - **Density**: `Compact` (default) ↔ `Roomy` (wider padding + wider year
        columns + larger text, ideal for zoomed client screen-shares).
      - **Format**: `$` (full) ↔ `$k` (thousands) ↔ `$M` (millions). Every cell
        keeps a `title=` tooltip carrying the full USD value even at $M scale,
        so precise numbers are one hover away.
    Wired into TaxDetail, DetailCashflow (Account Detail + Cashflow), and the
    Cashflow tab's Wide-Grid view. Shared helpers live in
    `frontend/src/lib/gridFmt.js` (`scaleMoney`, `densityClasses`) so future
    grids can adopt the same toolbar in one import.
  - **Strategy Optimizer — 6-metric rank selector**:
      - Backend `strategy_optimizer.py` now exposes two extra fields on each
        ranked row: `after_tax_estate_at_death` (after-tax value at 2nd death,
  - **BUG FIX — Cashflow tab merged Wages + Pension into one row**: On the
    standalone Cashflow tab, 2027 showed "$38,081" under a row labeled "Wages /
    other ordinary — Client" instead of separate rows for Client Wages
    ($130,373) and Client Pension 1 ($38,081) that sum to the $168,454 the
    Detail / Cashflow tab's "Wages / Pension" aggregate row shows.
      - **Root cause**: `backend/projection.py::_income_line_items` built the
        row label from `s.get("name") or s.get("category")`, but income_streams
        use the field `description` (see `defaults.py` and `PlanInputs.jsx`).
        Result: every unnamed Ordinary stream fell through to a generic
        fallback label `Wages / other ordinary — {owner}`, and multiple streams
        for the same owner collapsed to the same `{kind, source}` tuple.
      - **Compounding factor**: `frontend/src/components/Cashflow.jsx::buildGrid`
        did `row.values[i] = l.amount` (overwrite), so the last stream
        processed silently erased the earlier one — the user saw only one of
        the two streams' values.
      - **Fix**: Backend now reads `description` first
        (`(s.get("description") or s.get("name") or s.get("category") or "")`),
        so each stream renders with its actual name. Frontend `buildGrid`
        changed to accumulate (`row.values[i] = (row.values[i] || 0) + l.amount`)
        as a safety net against any future key collisions.
      - **Verified**: bug_testing_agent 13/13 pytest passing (incl. new
        `test_cashflow_line_item_descriptions_regression.py`), UI shows
        Client Wages 2027 = $130,373 and Client Pension 1 2027 = $38,081,
        sum = $168,454, Detail / Cashflow aggregate unchanged, CSV/Excel
        exports carry individual named streams.

  - **Conversion & Plan Controls — dedicated Roth conversion schedule chart**:
    Added `<ConversionScheduleChart>` (`frontend/src/components/ProjectionCharts.jsx`)
    and slotted it into the "Conversion and Plan Controls" tab right after the
    Tax chart. Renders one prominent green bar per year (Roth conversion amount
    on the left axis) plus a cumulative-converted orange line overlay on the
    right axis. Three quick-stat pills: "Conversion years" count, "Largest year"
    (year + $), and "Total converted". Empty-state message when the plan has
    conversions disabled or zero conversions. Replaces the very thin secondary
    bars inside the Net Worth chart that were easy to miss.

  - **Strategy Optimizer — winner card follows the active rank metric**:
    Winner card used to hard-wire on legacy (`result.best.after_tax_estate`),
    even when the user picked a different sort key. Now:
      - Winner is `sortedResults[0]` (the current #1 under the active metric).
      - Header shows an inline pill with the active metric name — e.g.
        "Best strategy on **Lowest lifetime tax paid**".
      - Primary numeric line renders the metric value + a directional delta
        pill vs the no-conversion baseline ("▲ $2,768,059 less tax" for tax,
        "▲ $X vs. no conversion" for legacy/value metrics).
      - Secondary `<MiniStat>` grid shows every other metric the winner scored
        on so the advisor sees the full trade-off (legacy + PV + at-death +
        value @ 2nd death + ending Roth + lifetime tax + total converted),
        excluding whichever metric is already the primary.
      - "Apply winner" button now applies `sortedResults[0]` (not backend
        default). "Different from best" tooltip content also references the
        metric-aware winner and metric-aware gap.
  - **Presentation — new "Account Composition Over Time" page**:
    Added `<AccountCompositionChart>` to `AnalyticsCharts.jsx` (Panel-wrapped
    stacked area, presentation-friendly). Inserted a new page in Presentation
    (Page 5b) between "Total Household Wealth Over Time" and "The Roth
    Conversion Schedule" — titled "Where the Money Lives — Account Composition
    Over Time" with client-friendly narrative that explicitly calls out the
    terra-cotta Traditional band shrinking into the deep-green Roth band as
    conversions execute.

        pre-heir horizon) and `value_at_death` (gross portfolio at 2nd death,
  - **Tab rename**: "Detail / Cashflow" → **"Accounts & Cashflow"**
    (`frontend/src/components/Planner.jsx`).
  - **Analytics — new stacked-bar chart of account values by year**:
    Added `<AccountValuesStackedBarChart>` to `AnalyticsCharts.jsx` and slotted
    it into the Analytics grid right after `RothConversionsChart` (full-width,
    span=2). One stacked bar per year with segments Cash + Taxable +
    Traditional + Roth using the same colour palette as the composition area
    chart, so the two charts reinforce each other. Bars make each year's
    snapshot unambiguous (no interpolation) — advisors prefer this for
    "how much do I have in the Roth in 2035?" questions during client reviews.

  - **Presentation vertical spacing**: All 6 chart wrappers converted from
    fixed `height` to `minHeight: X + 120` + `marginBottom: 14`, and the Page
    padding bumped from `6/0/12/0` to `6/0/24/0`. Fixes "The engine chose..."
    caption overlapping the Roth Conversion Schedule chart and other Sub
    captions overlapping chart legends.
  - **Persist grid prefs across sessions**: Density + $/$k/$M + Focus persisted
    via `localStorage['roth-planner:gridPrefs:v1']` using a new hook
    `useGridPrefs()` (`frontend/src/lib/useGridPrefs.js`). Shared across Tax
    Detail, Accounts & Cashflow, and standalone Cashflow tabs so advisors
    don't retoggle each login. Safe fallback when localStorage is unavailable
    (SSR / private mode / quota).
  - **Presentation — mirror stacked-bar account-values chart**: Added a new
    page 5c "Account Values by Year — Year-by-Year Snapshots" testid
    `presentation-page-account-values` using `AccountValuesStackedBarChart`.
    Renders after the Composition area chart and before the Roth Conversion
    Schedule page.
  - **Bracket-fill headroom overlay on ConversionScheduleChart**: Backend
    `projection.py::_build_year_row` now emits two new row fields:
    `target_bracket_ceiling` and `conversion_headroom_unused`. Frontend
    `ConversionScheduleChart` stacks a sand-coloured "Unused headroom to
    target bracket" bar on top of each conversion bar (only inside the
    conversion window — headroom outside the window isn't "left on the
    table"). Golden snapshot regenerated to include the new fields.
  - **Focus range on horizontal grids**: New `Focus` dropdown in
    `GridControls` with milestone presets: All years / Pre-SS window / SS →
    RMD / RMD window / After first death. Milestones auto-detected from the
    projection rows via `detectMilestones(rows)`. Filter applied on top of
    the existing "Hide pre-retirement" toggle. Persists to localStorage under
    the same key. Wired into Tax Detail, Accounts & Cashflow, and Cashflow.
  - Verified by testing_agent_v3_fork (iteration_39): 5/5 features PASS,
  - **BUG FIX** — Single-Year Optimizer preferential LTCG omitted special
    dividends. Root cause: initial `recurring_div_ltcg` was set from
    `dividend_yield × taxable-balances` only; any Dividend/LTCG income streams
    (e.g. "Special Dividends & LTCG") were silently excluded, and the value
    never re-synced when the user changed the Tax Year selector. Fix in
    `frontend/src/components/Optimizer.jsx`:
      1. New `streamDividendsForYear(year)` helper mirrors backend
         `projection.py::_stream_amount` semantics — honours `use=false`, reads
         `start_date`/`stop_date` with day-count proration on boundary years,
         supports Monthly/Annual frequency, applies COLA compounding.
      2. Initial state now = `divFromRate + streamDividendsForYear(startYear)`.
      3. `setYear(y)` re-syncs `recurring_div_ltcg` to the new year's stream
         state so year toggles reflect stopped/started streams immediately.
      4. Derivation helper text under the field discloses the stream
         contribution when non-zero.
    Verified by `bug_testing_agent` iteration_42: verdict **fixed** — 300/300
    pytest, zero action items.
  - **AI analysis cards** — new shared `<AIAnalysisCard>` component
    (`frontend/src/components/AIAnalysisCard.jsx`) added to five pages:
    Strategy Optimizer (below the winner card), SS Optimizer (bottom),
    Monte Carlo (below results), Compare (below the delta table), and
    Presentation (screen-preview only — kept out of the printed PDF so client
    decks stay clean). Each card sends a page-specific summary + focus
    context string to the existing `/api/insights` streaming endpoint —
    zero backend changes; leverages the same BYOK Gemini > Claude Fable 5
    fallback chain.
  - **Analytics — Cumulative Lifetime Taxes now includes 10-yr heir horizon**:
    `CumulativeTaxChart` (`AnalyticsCharts.jsx`) extended: X-axis now runs
    from plan start through the second-death year + 10 SECURE years.
    Post-death additions to the running total come from
    `legacy.post_death_rows[*].ira_tax_paid` (heirs' RMD tax on any
    inherited-IRA balance). Roth pays $0 by definition. A dashed
    "2nd death" `ReferenceLine` visually anchors where lifetime ends and
    heir horizon begins. The gap between the two curves typically WIDENS
    across the heir window — the clearest cross-generational value
    demonstration in the app. Subtitle rewritten to explain this.
  - **`<ChartCard>` extracted** — new
    `frontend/src/components/presentation/ChartCard.jsx` replaces all 7
    inline `<div style={{ minHeight, marginTop, marginBottom }}>{chart}</div>`
    wrappers in `Presentation.jsx`. Enforces consistent chart-card spacing
    so future page additions can't drift into the overlap bug that landed on
    advisor clients earlier.
  - **`<GridPrefsProvider>` context** — hoisted density + scale + focus state
    into a Context Provider wrapping the entire Planner (see
    `frontend/src/components/Planner.jsx`). `useGridPrefs()` now consumes
    the context when available so switching between grid tabs (Tax Detail
    ↔ Accounts & Cashflow ↔ Cashflow) reflects prefs LIVE, not just after
    remount. Falls back to independent per-component state when a grid is
    rendered outside a Provider (Storybook / isolated tests). Same
    `roth-planner:gridPrefs:v1` localStorage key.

  - **Presentation — CSV / Excel data-export buttons on every chart**:
    `<ChartCard>` extended with optional `exportData` (flat array of records)
    and `exportFilename` (stem) props. When both are set, the wrapper renders
    small CSV + Excel pills in the top-right of the card that hand the
    underlying data straight into `downloadCSV` / `downloadWorkbook`. Both
    buttons carry `print:hidden` so they stay out of the generated client
    PDF. All 7 charts in the deck wired: Income Sources, Total Wealth,
    Account Composition, Account Values by Year, Roth Conversion Schedule,
    Tax Cost by Category, and Heir Legacy Comparison. Heir Legacy exports as
    a 6-row summary (metric / with_strategy / no_conversions).
  - **Analytics + Presentation — Sources of Income exclude Roth conversions**:
    Roth conversions are internal transfers (Traditional → Roth, same
    household), not household inflows. Removed the `Conversion` bar from
    `IncomeSourcesChart` and dropped `Conversion` from `incomeData` in both
    `Analytics.jsx` and `Presentation.jsx`. The income tax paid on the
    conversion is real and still flows into the black spending line via
    `income_tax`. Chart subtitle rewritten to explain the exclusion.
  - **Presentation — narrative refresh explaining the refinement**:
    - Income & Spending page: new intro paragraph explicitly states
      "Roth conversions do not appear on this chart" and explains why
      (transfers, not new dollars) plus what IS visible (the tax on the
      conversion, embedded in the spending line).
    - Peak-conversion Sub caption now clarifies the peak year is not shown
      as a bar.
    - Account Composition page: intro paragraph now emphasises that "the
      total top of the stacked area stays roughly the same in each year —
      the money isn't lost, just moved."
    - Roth Conversion Schedule page: intro paragraph now leads with
      "these are transfers, not withdrawals."

    299/299 backend tests PASS, 2/2 exercised regressions PASS, zero action
    items.

        pre-tax). Threaded through `_run_and_metrics` and the phased-schedule
        serializer.
      - Frontend replaced the two-button Nominal / PV toggle with a `<select>`
        dropdown offering 6 metrics: 10-yr after-tax legacy · PV · legacy at
        2nd death · portfolio value at 2nd death · highest ending Roth · lowest
        lifetime tax paid. Sort direction is per-option (asc for lifetime tax,
        desc for the others). Table gained two new columns (Legacy @ 2nd death,
        Value @ 2nd death), and the sorted column + its cells are highlighted
        in green so the ranking key is unmistakable.




- **2026-07-19 (this session)**:
  - **Monte Carlo stress test on Strategy Optimizer (NEW FEATURE)**: `backend/strategy_stress.py` + `POST /api/strategy-stress` + `StrategyStressTest.jsx` panel on the Strategy Optimizer tab. Runs the top-2 sweep strategies per bracket (37→22%) + overall winner + no-conversion baseline against ONE shared random-market matrix (paired trials — differences are strategic, never luck). Engines: historical block bootstrap 1928–2024 (default) or lognormal, both flat-anchored to the plan's blended return; inflation held deterministic to isolate return-sequence risk. Reports per strategy: success rate, P10/P50/P90 in two lenses (liquid wealth AND approximate after-tax legacy via linear map legacy(W)=floor+slope·W calibrated to each strategy's deterministic ending mix), worst-5%-early-sequence cohort outcomes, robust (P10-legacy) re-ranking vs deterministic ranking with "robust winner differs" callout, P10/median path chart, per-row Apply buttons.
  - **BUG FIX (latent)**: `_apply_single_bracket` in `strategy_optimizer.py` did not clear stale `roth.year_targets` — if a phased strategy had been applied to the scenario, every single-bracket sweep candidate silently ran the old phased schedule (year_targets overrides target_bracket at projection.py:974). Fixed + regression test.
  - **Robustness page in client PDF Presentation (NEW FEATURE)**: stress-test result lifted from `StrategyStressTest` → `Planner` (state) → `Presentation` (prop, mirrors the existing mcResult pattern). New print page "What If Markets Crash Right After You Convert?" between Legacy and Recommendations: side-by-side cards (deterministic avg-path winner vs P10 robust winner, each with avg legacy, P10 legacy, success rate, worst-5% early-crash outcome), green/amber verdict banner, top-5 + baseline candidate table with avg#1/P10#1 tags, method footnote. Toolbar toggle `include_robustness` (default on) is disabled with a hint until the stress test has been run on the Strategy Optimizer tab; stale-plan warning if the scenario changed since the run. Page also embeds a compact P10 wealth-path chart (`StressP10Chart`) showing det winner / robust winner / dashed baseline at the pessimistic 10th percentile; when the deterministic and robust winners coincide, the side-by-side cards collapse into a single full-width card.
  - **Strategy identifier + tab reorder (NEW UX)**: New shared helper `lib/strategyLabel.js` derives a human-readable label from `scenario.roth` (`No conversion active` / `Fill 22% · 2026–2033` / `Phased: 32% · 2026–2029 → 22% · 2030–2033`) + funding order. New `<StrategyBadge>` renders it as a compact pill at the top of Multi-Year Projection, Detail/Cashflow, Analytics, and Presentation pages, and inside the printed PDF cover header. Same helper reused by `StrategyOptimizer.jsx` (replaces its ~40-line inline duplicate). Tabs reordered to the advisor "Build → Analyze → Present" flow with **Plan Inputs pinned as the first tab**: `Plan Inputs → Scenarios → Single-Year Optimizer → Strategy Optimizer → SS Optimizer → Multi-Year Projection → Detail/Cashflow → Analytics → Monte Carlo → Compare → Presentation → Concepts → White Paper`. Default landing tab is now Plan Inputs (shared read-only view still lands on the Single-Year Optimizer since inputs/scenarios are hidden there).
  - **BUG FIX — Presentation charts (blank on prior fork)**: `Presentation.jsx` was feeding `RothConversionsChart` a data key `roth_conv` (chart reads `conversion`) and `PvNetWorthChart` keys `withRothPv/noRothPv` (chart reads `pvWith/pvNo`) — both charts silently rendered empty axes. Renamed the keys; both now show the correct bars + area/line under Presentation preview and print.
  - **Brand scrub**: Removed all MoneyGuide / MoneyGuide Pro / Boldin references from the codebase (5 files: `strategy_optimizer.py`, `PRD.md`, `Projection.jsx`, `Presentation.jsx`, `StrategyOptimizer.jsx`).
  - **Market Scenario toggle V1 (NEW FEATURE)**: New backend module `market_scenarios.py` ships 5 named regimes — `historical_avg` (default), `70s_stagflation`, `lost_decade`, `high_inflation`, `low_return` — plus a `custom` passthrough. Each preset carries flat `overrides` for `general_inflation`, `cash_return`, `taxable_return`, `ira_return`, `roth_return`. Engine hook `apply_market_scenario()` is called at the top of `run_projection()` — every downstream consumer (sweep, strategy sweep, monte carlo, stress test) picks it up automatically. Inflation family is mirrored into `bracket_indexing` + `irmaa_indexing` so brackets track the assumed CPI. `historical_avg` is a mathematical no-op vs the current defaults (locks in V17 parity). New endpoint `GET /api/market-scenarios` serves the presets library. Frontend: new `<MarketScenarioSelector>` at the top of Plan Inputs; compact `<MarketBadge>` next to `<StrategyBadge>` on Multi-Year Projection / Detail-Cashflow / Analytics / Presentation; amber "Non-baseline" chip lights up whenever a non-default regime is active; printed PDF cover gets a "MARKET ASSUMPTION" pill and the Assumptions page picks up a "Market scenario" row. 13 new pytest cases (`test_phase30_market_scenarios.py`) — snapshot lock that historical_avg reproduces default output to the penny; parametric proof that every non-default regime moves lifetime tax + legacy meaningfully; per-account-type override mapping; input-immutability guarantee.
  - **Editable General Inflation field**: exposed `scenario.projection.general_inflation` as an editable percentage input in Plan Inputs → Taxes card (default 0.03, data-testid `general-inflation`). The onChange cascades the new value into `bracket_indexing` and `irmaa_indexing` in the same update (so tax brackets and IRMAA thresholds always track the assumed CPI — otherwise typing 8% would silently under-index brackets at 3%). Closes the "Custom market scenario is truly user-controlled" gap.
  - **Funding-order as 4th sweep dimension (NEW FEATURE)**: `strategy_sweep()` now accepts `sweep_funding_orders: bool = False`. When ON, the sweep enumerates all 3 funding orders (Taxable-first / IRA-first / Split) as an outer loop on top of start × stop × bracket — so the search space is a strict superset of the 3D sweep. Baselines expand to 3 rows (funding_order affects the no-conversion drawdown too). Every result row carries `funding_order` + `funding_order_short`; labels end with `· Taxable-first / IRA-first / Split`. The pre-existing cheap `refine_funding_orders` post-pass is auto-skipped as redundant. Server-side cap widened to `cells × 3` when the 4D toggle is on so the grid limit still equals the pre-existing MAX_SWEEP_GRID_CELLS worst case. Frontend: new toggle `strategy-sweep-funding` (auto-disables the Refine toggle with an inline hint); Winner card now shows the winning funding order and flags it with an amber "Differs from current plan" badge when it isn't the scenario default; new "Funding" column in the results table; `applyStrategy` now applies the row's `funding_order` alongside the roth block so one click configures the whole plan. 7 new pytest cases (`test_phase31_funding_order_sweep.py`) — row-count parity, order-tag labels, distinct legacies per order (regression lock), refine skipped when 4D on, phased iteration per order, 4D winner ≥ 3D winner. Golden snapshot refreshed (additive change only — zero numeric drift on existing ranked rows).
  - **Presentation PDF — "Why this funding order?" narrative (NEW FEATURE)**: on the Recommendations page, a new dedicated section (rec #3) explains to the client WHY the chosen withdrawal order was selected, with concrete $ deltas. Presentation.jsx fires 3 paired projections (same conversion strategy × 3 funding orders) and detects the DOMINANT driver of the after-tax-legacy swing between winner and loser — larger tax-free Roth vs. larger step-up Taxable vs. smaller SECURE-10 IRA — then writes a 2-3 sentence narrative grounded in that specific mechanic (no boilerplate that contradicts the numbers). Adaptive template: if the client's current order IS the winner, celebrate it; if not, recommend switching with the +$X legacy gain. Followed by a 3-row ranked comparison table (★ + shaded winner row, amber "CURRENT" chip on the client's active order) showing after-tax legacy, lifetime tax, ending Taxable (step-up basis), ending Roth. Footnote calls out the tax tradeoff ("winner accepts $X more lifetime tax in exchange for the larger legacy — pay a little more now, leave a lot more later") or the strict-improvement case when the winner also pays less tax.

- **2026-07-19 (fork session)**:
  - **White Paper rewritten (standalone, no edition references)**: `WhitePaper.jsx` fully rewritten around the user's doctrine: (1) convert while parents' rate < heirs' assumed rate — and NOT automatically if heirs' rate is lower; (2) longevity (unpredictable) is the second-order driver — long LE favors early/larger conversions funded from taxable (Roth compounding outweighs step-up), short LE favors preserving step-up + IRA funding; (3) heir behavior with inherited taxable accounts (never-sell ≈ Roth-like vs. realize-gains → taxable-first funding more desirable); (4) "taxes are certain, assumed returns are speculative" — avoid excessive high-bracket early conversions. All published tables recomputed from the user's SAVED defaults (LE 85/90, second death 2056, 1% dividend yield, heir 32% fed + 4% state = 36% blended, Roth window 2029–2044, taxable-first order). Key figures: no-conversion heir IRA tax $7.98M; B (24% taxable-first) wins base case $47.15M never / $46.80M realized vs A $42.80M/$39.06M. NEW longevity sensitivity table (LE 80/85 vs 85/90 vs 95/100 × both realization bounds, `whitepaper-longevity-table`) + funding-order delta table (`whitepaper-funding-delta-table`): B−C = −$0.07M..+$1.20M never / +$1.94M..+$5.24M realized. MC table updated to 65/25/10 allocation (success 86.4% vs 86.6%, p5 $0 both — ~1 in 7 paths depletes). Live "Run on YOUR plan" buttons kept; main table's +10yr column now uses the default never-realized bound; live MC now passes MC_ASSETS (65/25/10) to match published methodology.
  - **AI Insights default model → Claude Fable 5** (Anthropic Mythos-class, June 2026): `server.py` — new `_fable_stream()` (emergentintegrations LlmChat, model `claude-fable-5`, EMERGENT_LLM_KEY) + `_insights_stream()` dispatcher replacing `_resolve_gemini_key`. Priority: user BYOK Gemini key → Claude Fable 5 on universal key → server DEFAULT_GEMINI_API_KEY → 401. Both `/api/insights` and `/api/insights/chat` use it. Verified live via curl (streams correctly). `AIInsights.jsx` copy notes "powered by Anthropic's Claude Fable 5"; BYOK stays Gemini.
  - **Regime Comparison page in the client PDF Presentation**: `RegimeComparePanel.jsx` gains `onResult` prop → lifted via `MonteCarlo.jsx` (`onRegimeResult`) → `Planner.jsx` state (`regimeResult`, mirrors stressResult pattern) → `Presentation.jsx`. New `RegimeComparePage` print page (testid `presentation-page-regimes`) positioned right after the Robustness page: dynamic client verdict ("The plan works in N of 6 futures. The exceptions: …"), 6-row table with color-coded success bars (green ≥90 / amber 75–90 / orange <75, print-color-adjust exact), ★ winner, YOUR BASELINE + WORST CASE chips, Δ vs baseline, P10/P50/P90 legacy, method footnote. Toolbar toggle `pres-toggle-regimes` (default on, disabled until the comparison has been run on the MC tab, stale-plan warning `pres-regimes-stale`), page-count estimate updated. Verified end-to-end via browser automation (MC run → regime compare → page renders in preview + print DOM).
  - **White Paper annotated with 3 user-supplied sources** (refs 12–14): Kitces 2019 "Capital Gains Bump Zone" (new §2.1 paragraph on ordinary income shoving gains across 0/15/20% thresholds — 27%–~50% effective marginal rates — plus a §6.2 conversion-sizing bullet); Kitces 2016 "Tax-Efficient Spending Strategies From Retirement Portfolios" (§2.3: spend-brokerage + partial-conversion playbook beats spend-IRA-first/brokerage-first/split); Geisler, Harden & Hulse 2021 JFP "A Comparison of the Tax Efficiency of Decumulation Strategies" (§4: client-focused pre-SS income acceleration extends portfolio life up to 11.6% vs Conventional Wisdom/Schwab, tax-torpedo 46.25%; §5.5: "no single best sequence for all clients" supports funding-order-as-preference). Verified zero "edition" references remain; 14 references render.

- **2026-07-19 (fork session, later)**:
  - **Advisor PIN gate (SEC-003)**: 6-digit PIN lock screen (initial PIN 140431 — see memory/test_credentials.md). Backend: `auth_pin.py` (bcrypt hash + random epoch in Mongo `auth_config/advisor_pin`, seeded idempotently from `ADVISOR_PIN_INITIAL`; HS256 JWTs with 30-day TTL embed the epoch — PIN change rotates epoch → all devices signed out instantly). Endpoints: `POST /api/auth/pin/verify` (5/min), `GET /api/auth/pin/status`, `POST /api/auth/pin/change` (returns fresh token so the changing device stays signed in). Enforcement: `require_advisor` (scenario CRUD, share enable/revoke, defaults save/revert, pin change) and `require_advisor_or_share` (all compute/read endpoints — valid `X-Share-Token` header from a `?share=` link unlocks them WITHOUT the PIN, so client share links keep working). Open: `/api/`, share fetch, pin verify/status. Frontend: `LockScreen.jsx` (OTP input, auto-submit, 429 handling), `ChangePasscodeDialog.jsx` (header "Passcode" button), `App.js` gate (share URLs bypass; `advisor-auth-required` event relocks), api.js axios interceptors (Bearer + X-Share-Token + 401 relock) and `authHeaders()` for AIInsights raw fetch. Tests: `tests/test_phase33_pin_auth.py` (7 HTTP tests: 401 wall, wrong/correct PIN, garbage token, share-token scope split, epoch rotation) + conftest now injects a live advisor bearer into legacy HTTP tests (with 401 self-heal re-mint) and dependency-overrides the gate for in-process TestClient tests. Full E2E verified via browser automation incl. share-link bypass.
  - **Latent bug fixed — share_token E11000**: the unique index was `sparse`, which still indexes explicit nulls, so two unshared plans (or two revoked ones — revoke `$set`s null) collided with HTTP 500. Startup now migrates to a **partial unique index** (`share_token: {$type: "string"}`), + regression test in test_phase24_share_links.py.

- **2026-07-19 (fork session, latest)**:
  - **White Paper base-case correction**: `WhitePaper.jsx` §5 base-household block now matches the actual model defaults verbatim: **$9.0M starting assets** ($500K cash / $2.0M taxable @ $800K basis / $5.0M IRA / $1.0M residence), **2026–2056 conversion window**, and explicit **Cash → Taxable → IRA → Roth** spending order noted inline. Previous copy erroneously said $13.0M / $6.0M taxable / 2029–2044 window.
  - **Strategy Optimizer results — 50-row scrollable window**: `StrategyOptimizer.jsx` — "All strategies ranked" table now lives inside a `max-h-[1400px] overflow-auto` container with a **sticky header** (bg-white, subtle 1px shadow, z-10), so the top of the ranking + column headers stay anchored while the full sweep list (100+ rows possible) scrolls internally. `data-testid="strategy-results-scroll"` on the container.
  - **"Different from best" micro-indicator**: `StrategyOptimizer.jsx` — new orange chip in the header next to the "Currently applied" badge (`data-testid="applied-strategy-differs-from-best"`) that appears only when a sweep has been run AND the applied strategy's label OR funding order differs from the sweep's #1 winner. Chip is a clickable button that applies the winner; hover tooltip explains the exact delta (winner label, winning funding order, and after-tax-legacy gap vs no conversion). Auto-hidden when no sweep has run yet, when no conversion is applied, or when the applied plan already matches the winner.
  - **Presentation deck — dynamic Base Household narrative**: `Presentation.jsx` Assumptions page now leads with a `data-testid="presentation-base-household"` narrative block that mirrors the White Paper style but is fully **computed from the live scenario** (`scenario.accounts` sums by tax_type, embedded-gain %, filing status, ages, second-death year, dividend yield, state tax, IRMAA flag, funding order, market regime, heir rates, estate settlement %, SECURE horizon, realization assumption, Roth window). Every subsequent client PDF is guaranteed to match the numbers in the rest of the deck — no more risk of a stale exemplar paragraph.
  - **Default dividend yield 2% → 1%**: `backend/defaults.py`, `backend/projection.py` fallbacks, `PlanInputs.jsx` (`?? 0.02` → `?? 0.01`, "Default 2% (0.02)" → "Default 1% (0.01)"), `Optimizer.jsx`, `Presentation.jsx` Assumptions row (now dynamic: `≈ 6.8% (7% gross − 1% dividend drag)`), `WhitePaper.jsx` ($6M/$60k example → $2M/$20k matching actual defaults), and `StrategyOptimizer.jsx` (rewrote the "1.5–2 pp/year Taxable drag" educational text into a doctrinally-correct "~0.25 pp/year at 1% qualified-dividend yield and 23.45% LTCG, with additional drag from NIIT + IRMAA at high incomes"). Golden snapshot regenerated; 6 hardcoded assertion values in tests_phase12/17/18/19 updated to the new 1% defaults ($6.205M lifetime tax / $84.57M ending NW / $160.69M heir legacy).
  - **Two-row tab layout**: `Planner.jsx` — TabsList now uses `flex flex-wrap h-auto p-1 gap-1` with an invisible `basis-full` line-break element between "SS Optimizer" and "Multi-Year Projection", producing a clean Row 1 (Build: Plan Inputs / Scenarios / Single-Year / Strategy / SS + Admin when master) and Row 2 (Analyze & Present: Projection / Cashflow / Analytics / Monte Carlo / Compare / Presentation / Concepts / White Paper). Zero horizontal scroll at ≥ 1024px.
  - **Two-tier licensing (Phase 34 — NEW FEATURE)**: replaces the single advisor PIN gate with a **master + licensees** model. Master PIN is sourced from `backend/.env` → `MASTER_ADMIN_PIN` (falls back to legacy `ADVISOR_PIN_INITIAL`), bcrypt-hashed in `auth_config/master_pin` on first boot and re-hashed on any env change (auto-rotates the epoch, signing out all master devices). `POST /api/auth/pin/change` is now **410 Gone** — env-only rotation prevents a compromised session from locking out the owner.
    Licensees are provisioned by the master via a new **in-app Admin panel** (master-only "Admin" tab, `AdminPanel.jsx` with issue/rotate/renew/revoke workflows and a one-time PIN reveal dialog with copy-to-clipboard) or via `POST /api/admin/licenses`. Each licensee has `{email, pin_hash, epoch, expires_at, revoked_at, created_at, last_login_at, last_login_ip}` in the new `licenses` collection (unique on email + license_id). Login endpoint `POST /api/auth/license/verify` (rate-limited 5/min) returns a JWT `{role:"licensee", sub:license_id, epoch, exp}` whose TTL is `min(30 days, license.expires_at)`. Revoke/rotate/renew all bump the licensee epoch, invalidating every device instantly.
    Auth deps: `require_advisor` now accepts master OR licensee tokens; `require_master` gates admin endpoints. Legacy `advisor_pin` tokens are still accepted via a backwards-compat branch in `decode_auth_token()` so already-signed-in devices survive the upgrade. Frontend: new `LicenseLogin.jsx` dual-mode (Licensee = email+PIN; Master = PIN only) replaces `LockScreen.jsx`; App.js gate routes accordingly; api.js stores `role` + `email` in localStorage for the session badge; Planner.jsx hides the Admin tab from licensees. Tests: `test_phase34_licenses.py` (8 tests covering master status, deprecated change endpoint, licensee full lifecycle, unknown-email 401, unauth admin 401, invalid email format, expired license blocked).

## Recently shipped

- **2026-08-06 — EP Flowchart tab + Client Report flowchart pages (Phase 46)**:
  - Web replica of the workbook's "EP Projection" flowchart (user's Flowchart.pdf), repopulating from the loaded scenario.
  - **Backend**: `/app/backend/ep_flowchart.py` — `build_ep_flowchart()` traces four estate-funding plans from first death → survivor → children: Plan 1 No Trust Planning (baseline, per user's reorder), Plan 2 Taxable-First GST Trust, Plan 3 Roth-Only GST Trust (2nd step-up on Taxable), Plan 4 GST Trust at Second Death. Trusts fund from Roth+Taxable only; Y2 trust cap = fed excl Y2 + DSUE but GST-exempt portion capped at survivor's own exclusion (GST not portable). Endpoint `POST /api/estate/ep-flowchart` (planning.py, 60/min, advisor/share gated). Engine validated against the PDF: excl 2056 $36,408,937 / 2062 $43,474,175, DSUE $20,489,359, forgone step-up $189,380 — 9/9 tests in `tests/test_phase46_ep_flowchart.py`.
  - **Frontend**: new top-level "EP Flowchart" tab (`tab-ep-flowchart`, Workflow icon) — `EpFlowchart.jsx` slices projection balances at first-death year (50/50 spousal split, cash+house own bucket), 4 plans stacked vertically as print-safe flow diagrams (`flowchart/PlanFlowchart.jsx`: purple client / blue survivor / green trust / amber funding boxes, SVG arrows, metric chips), adjustable growth/cap-gains/heir-income rates (localStorage `ep_flowchart_settings_v1`), plan-comparison matrix (`flowchart/PlanComparisonTable.jsx`, direction-aware starring), GST-non-portability methodology note.
  - **Client Report**: advisor picks which plans print — `cr-flow-card` in ReportCustomizationCard (master `cr-flow-toggle`, per-plan `cr-flow-plan-toggle-1..4`, `cr-flow-compare`); one report page per selected plan (`clientReport/EpFlowchartPage.jsx`, `cr-page-flow-plan-N`) + optional comparison page (`cr-page-flow-compare`, needs ≥2 plans); page numbering extends dynamically (footer M grew exactly +5 with all on). localStorage keys `client_report_flow_{on,plans,compare}_v1`.
  - Iteration 63: backend 6/6 + all frontend flows PASS, Estate tab regression clean.

- **2026-08-04 — Advisor talking points popover + two major refactors (Phase 38)**:
  - **Advisor talking points popover** — every chip in Strategy Optimizer's 'Best funding order per bracket' strip now has a shadcn `HoverCard` popover (120ms open, 80ms close) that generates 2-3 sentences of advisor talking points from the row's own metrics: headline ('At X% target, {order} wins'), concrete $ delta vs. the second-best funding order at the same bracket, and an italic mechanic explainer keyed off the winning order (Taxable-first / IRA-first / Cash-first / Split IRA & Taxable). Zero AI cost — pure client-side templating.
  - **Refactor 1 — StrategyOptimizer.jsx** (773 → 593 lines): extracted `BestFundingChipStrip.jsx` (chip strip + popover) and `AggressiveStrategyExplainer.jsx` (5-force + sequence-of-returns collapsible) into `components/strategy/`.
  - **Refactor 2 — server.py** (1643 → 163 lines, ~10× reduction): app-bootstrap only. Route logic split into `routes/{auth,scenarios,workspaces,planning,insights}.py` via APIRouter. Shared foundation lives in `deps.py` (db, limiter, security constants, session/advisor/master deps, LLM stream helpers). All Pydantic request/response shapes moved to `models.py`.
  - **Critical fix during refactor** — `from __future__ import annotations` (PEP 563) breaks FastAPI's body-model detection when combined with slowapi's `@limiter.limit()` wrapper — annotations become strings and FastAPI mistakenly treats Pydantic body models as query params. Fix: stripped `from __future__ import annotations` from every module that defines rate-limited endpoints; documented the pitfall inline. `RequestValidationError` handler restored to sanitize NaN/Inf out of error responses (SEC-003).
  - **Zero API contract change** — 298/298 backend tests pass through the refactor.

- **2026-08-04 — Cashflow Statement tab (Phase 37)**:
  - New second-row tab (`data-testid=tab-cashflow-detail`, Wallet icon) after Tax Detail. Full year-by-year income + expense statement for the household.
  - **Wide-grid view (default)** — sticky first column of line-item names, sticky top row of years, three collapsible sections (Income / Expenses / Funding drawn) with per-section subtotals in bold + a middle "Net cashflow (income − expenses)" reconciliation row (green for surplus, red for deficit). Roth conversions render in a separate italic "Non-cash tax event" strip so income_subtotal is never overstated by the conversion.

- **2026-02 — 3 UI/UX refinements (Iteration 43)**:
  - **ExportChip on every Analytics chart** — extracted reusable `<ExportChip>`
    (`/app/frontend/src/components/presentation/ExportChip.jsx`) from ChartCard's
    inline CSV/Excel button pair. `AnalyticsCharts.jsx` Panel now accepts
    `exportData` + `exportFilename` props and renders `<ExportChip>` beside every
    chart title. `Analytics.jsx` wires shaped row data + a filename for all 13
    charts (bracket fill, surplus, tax composition, RMD/balance, IRMAA, rate
    trend, cumulative tax, PV net worth, Roth conversions, PV net-to-family,
    heir legacy compare, account composition, account values). Testid pattern:
    `${panelTestid}-csv` and `${panelTestid}-xlsx`.
  - **"Roth Conversion (non-cash transfer)" label + tooltip** — `DetailCashflow.jsx`
    CF_LINES 'conversion' entry gains a `tooltip` field; CfLineRow renders the
    label + a small lucide `<Info>` icon inside a span with `title=line.tooltip`.
    `Cashflow.jsx` mirrors the change in both the wide-grid non-cash strip and
    the per-year card view. Native browser tooltip explains the conversion is a
    Traditional → Roth transfer that never touches real cash but does drive the
    year's income tax bill.
  - **Presentation cashflow summary chip** — new block under the Sources of
    Income chart on Page 4 of the Presentation: "Total dollars converted: $X ·
    Total tax paid on conversions: $Y". `Y` is computed inside `PresentationReport`
    as `sum(rows[i].roth_conversion × rows[i].marginal_rate)` — the intuitive
    advisor "tax paid on those conversions" quantity, always non-negative, robust
    when noRoth hasn't loaded yet.
  - Testing agent verified 100% for the 3 refinements + regression coverage;
    299 backend pytest cases still pass unchanged (no backend edits).
  - **Per-year cards view** — one card per year with mini income statement (grouped income lines + subtotal, grouped expense lines + subtotal, net cashflow figure, shortfall funding block for negative-cashflow years, non-cash conversion mini-row).
  - **"Hide pre-retirement" toggle** — narrows the year set to years with RMDs or gross SS > 0 (same rule as Tax Detail).
  - **Excel + CSV export** — full wide-grid dataset (line-item × year).
  - **Backend emit** — `projection.py` gains `_income_line_items`, `_expense_line_items`, and `_assemble_line_items`. Every yearly row now carries `line_items` with `income`, `expenses`, `funding`, `subtotals`, and `non_cash_events`. Sums reconcile to existing aggregates to the cent (invariants locked by test_phase37_cashflow.py). Categorization is source-name based: `wages / pension / annuity / ss / rmd / interest / dividends / other` for income; `spending / housing / health / insurance / gift / taxes / other` for expenses.
  - **Tests** — `test_phase37_cashflow.py` (12 cases: schema, subtotal reconciliation, conversion double-counting guard, SS per-line proportional taxability, dividend line preferential-only, RMD line appears when expected, helper unit tests, survivor-reduction propagation).
  - Testing agent verified 100% frontend + 100% backend with zero action items.

- **2026-08-04 — Tax Detail tab (Phase 36)**:
  - New second-row tab (`data-testid=tab-tax`) between Detail/Cashflow and Analytics. Shows a stacked-bar timeline of federal ordinary + federal preferential + NIIT + state + IRMAA/Medicare taxes each year with a taxable-income overlay line, a sticky year-by-year table (13 columns: age, ord. taxable, pref. taxable with dominant-band chip, all 5 tax buckets, total tax, marginal %, effective %, callouts), a per-row expand drawer with 3-column diagnostics (Ordinary income build / Preferential build with LTCG band split / IRMAA + SS diagnostics with next-tier threshold), and an educational rail on IRMAA / bump-zone / SS Torpedo.
  - **Callouts (`lib/taxCallouts.js`)** — pure diff-vs-prev-year detectors for LTCG band bumps, IRMAA tier changes, and SS taxable-% steps. Chips have hover tooltips + aria-labels.
  - **Backend exposure of intermediates** — `tax_engine.ltcg_band_split(ord, pref, mfj, idx)` slices preferential dollars across 0/15/20% bands (locked to `federal_ltcg_tax` to the cent). `projection.py` `_build_year_row` emits a `tax_detail` sub-object per year: `preferential_taxable`, `total_preferential`, `taxable_ss`, `provisional_income`, `standard_deduction`, `senior_bonus`, `ss_inclusion_pct` (nullable), `ltcg_band_split`, `marginal_ordinary_rate`, `effective_rate`. Purely additive — golden snapshot refreshed with only the new key.
  - **Tests** — `test_phase36_tax_detail.py` (11 cases: band split correctness, indexed ceilings, MFJ vs Single, 15→20 cliff, projection schema, `ss_inclusion_pct` null when no SS, band-split-sums-to-pref invariant, `15%*in_15 + 20%*in_20 == federal_ltcg_tax` invariant).
  - Excel + CSV export of the full tax detail dataset (23 columns per year).

- **2026-08-04 — Client Workspaces + Capital-Gains Bump-Zone Alert (Phase 35)**:
  - **Client Workspaces (P1)** — named folder per client household that groups saved scenarios. New `workspaces` collection (`{id, name, notes, owner_token, created_at, updated_at}`) with session-scoped ownership. New CRUD endpoints: `POST/GET/PATCH/DELETE /api/workspaces`, plus `PATCH /api/scenarios/{sid}/workspace` to move a plan and workspace_id filter on `GET /api/scenarios` (including an `unfiled` sentinel). Deleting a workspace unfiles its plans (`workspace_id → null`) — no scenario is ever lost. `Scenarios.jsx` gains a new "Client Workspaces" card with pill-chip tabs (with per-folder scenario counts), a create/rename/delete workflow, a "Save into…" folder dropdown next to the name input, a workspace badge on every scenario row, and a per-row "Move…" dropdown. New pytest module `test_phase35_workspaces.py` (13 tests: CRUD, ownership isolation, filter, move, delete-cascade, error surface). Mongo indexes added: `owner_token+workspace_id` on scenarios, `owner_token+created_at` on workspaces.
  - **Bump-Zone Alert (P2)** — `Optimizer.jsx` computes preferential-income band overlap from the existing `/api/tax/optimize` before/after breakdown (no backend calls added). When a recommended conversion pushes qualified dividends or LTCG across the 0→15% or 15→20% preferential cliff (indexed by `bracket_index`), an amber banner (`data-testid=bump-zone-alert`) renders below the 3-metric row in the "Fill-the-Bracket Roth Conversion" card, showing (a) the concrete `$` amount pushed at each cliff, (b) the extra LTCG/QDIV tax the conversion cost, and (c) the ordinary-taxable ceiling that triggered the cliff. Silent when no pref income exists or no cliff is crossed. Verified against tax_engine math with two golden cases (MFJ 15→20 @ 35% target = $45.9K bumped, $2.3K extra pref tax; MFJ 0→15 @ 24% target = $10K bumped, $1.5K extra pref tax).

## 2026-07-25 — Client Retirement Status / Action Report tab (this session)
Added a new top-level "Client Report" tab in Planner (Row 2, next to Presentation). This is a long-form, print/PDF-optimized narrative report for the client, mirroring the Boldin retirement-readiness report format.

**File**: `frontend/src/components/ClientReport.jsx` (single file, ~1,435 lines — flagged for future split into sub-page components).

**Pages** (each is its own print page with pagebreak-before / page-break-after):
1. **Cover / Profile** — client names, strategy pill, market pill, presentation date, cover intro, profile KPIs, advisor block.
2. **Assumptions** — full scenario detail (returns, inflation, funding order, filing status, IRMAA, heir tax rates, SECURE post-death horizon, heir realization mode).
3. **Overview** — Monte Carlo success gauge, net worth donut at 2nd death, avg income vs expenses callouts, projected household wealth line chart (with strategy vs no-conversions baseline).
4. **Savings** — total-converted / starting-vs-ending-NW KPI strip + stacked balances area chart (Cash / Taxable / Traditional / Roth).
5. **Income & Expenses** — stacked income sources bar chart + stacked expense categories bar chart.
6. **Cash Flow** — year-by-year table (Income / Withdrawals / Expenses / Surplus). Auto-samples every-other-year when horizon > 24 years to fit on one page.
7. **Taxes** — lifetime-tax with/without KPIs + tax composition stacked bar chart + total ordinary vs preferential income totals.
8. **Monte Carlo** — big success percentage + percentile fan chart (p10 / p50 / p90) with p10/p50/p90 ending-wealth KPIs.
9. **Roth Conversion & Legacy (centerpiece)** — side-by-side table of "No conversions / RMD-only baseline" vs "Convert at parents' rates" showing parents' lifetime tax, heirs' SECURE-10 IRA tax, total family tax, Traditional balance at 2nd death, and after-tax wealth to heirs. Verdict banner ("Converting delivers $X more" or the inverse). Effective-tax-rate-per-dollar table showing conversion rate vs inherited-IRA rate vs rate arbitrage. Heirs' forced-distribution tax bar chart (A vs B). Footer note explaining why "Wait-and-see / RMD-only" collapses into the No-conversions baseline.
10. **AI Review** — client-facing narrative + advisor talking points, generated by `/api/insights` (Claude Fable 5 → Gemini fallback). Editable textarea; edits persist to `localStorage.client_report_ai_text_v1`. Toggleable via `cr-toggle-ai` (default on).

**Toolbar controls** (screen-only):
- Full branding form (client / spouse name overrides, date, advisor, firm, contact, cover subtitle, cover intro, footer confidentiality) — persists to `localStorage.client_report_branding_v1`.
- **Heir tax rate overrides** — `heir_federal_rate` + `heir_state_rate` inputs override the scenario's saved rates for the report's SECURE analysis; triggers re-projection.
- **AI Review card** — Generate / Regenerate / Clear buttons + editable textarea. Streams from `/api/insights` with the standard `_focus` prompt that instructs the model to output two sections: `### For the Client` (3-4 paragraphs) and `### Advisor Talking Points` (3-4 bullets).
- **Generate PDF** — uses the same `downloadElementAsPdf` pipeline as Presentation. Filename: `<clientname>-retirement-report.pdf`.

**How the "3 strategy" model works**: Per user direction, "Wait-and-see / RMD-only" is treated as logically identical to "No conversions" in this model (RMDs are distributions, not conversions, so they don't move dollars Traditional → Roth). Displayed as a single merged strategy row with an explanatory footnote. The centerpiece analysis is a clean 2-strategy A-vs-B comparison.

**Testing** — `testing_agent` iteration 44: 11/11 review criteria at 100%. Zero console errors during the full flow. Legacy verdict correctly flips as the heir federal rate is adjusted (0.32 → 0.37 changed the verdict from "$5.15M more @ 36%" to "$6.71M more @ 41%"). All persistence, toggles, and re-projection triggers verified.

## 2026-07-25 (later) — Split refactor + new Convert vs Skip tab (this session)
**Two shipped changes:**

**(A) ClientReport.jsx split into subcomponents** — Extracted the 1,435-line monolith into `/app/frontend/src/components/clientReport/`: `helpers.jsx` (99 lines — shared H1/H2/H3/P/Sub/Page/Kpi primitives, branding keys, defaultBranding, loadBranding/saveBranding), plus one file per report page: `CoverPage.jsx` (90), `AssumptionsPage.jsx` (55), `OverviewPage.jsx` (115), `SavingsPage.jsx` (56), `IncomeExpensesPage.jsx` (76), `CashFlowPage.jsx` (62), `TaxesPage.jsx` (44), `MonteCarloPage.jsx` (73), `LegacyPage.jsx` (168), `AIReviewPage.jsx` (35). Main `ClientReport.jsx` is now a 533-line thin orchestrator handling data fetching (2 projections + Monte Carlo + AI streaming), the toolbar UI, and page composition. Zero behavior change — regression tested by `testing_agent` iteration 45: all 15 client-report testids still render at count=2 (live preview + hidden print root).

**(B) New "Convert vs Skip" tab** — `/app/frontend/src/components/ConvertCompare.jsx` (~400 lines). New top-level Planner tab (data-testid=`tab-convert-compare`) placed immediately after "Client Report". Runs the active scenario twice (Roth conversions ON vs OFF) and displays both outcomes side-by-side on ONE screen:
- **Header + strategy/market badges** — reuses `StrategyBadge` + `MarketBadge`
- **Verdict banner** (`convert-compare-verdict`) — green "Converting delivers $X more to heirs" or amber "Skipping conversions is better here", with explanation copy referencing the modeled heir marginal rate + total family tax delta
- **4-column KPI compare grid** — Ending Net Worth, Lifetime Tax (parents), After-Tax Legacy, Total Family Tax. Each tile shows Convert value + Don't-Convert value with the winner highlighted, plus a signed Δ line
- **Wealth-over-time chart** — one chart, two curves (solid green "Convert" + dashed amber "Don't Convert")
- **Two side-by-side account-balance stacks** — Convert (green title) vs Don't Convert (amber title), each showing stacked Cash/Taxable/Traditional/Roth over time
- **Annual income tax bar chart** — paired Convert/Don't-Convert bars per year, showing the classic front-loaded conversion tax spike + late-year RMD tax spike under Don't-Convert
- **Monte Carlo compare panel** — on-demand button that runs Promise.all([runMonteCarlo(scenario), runMonteCarlo(noRothClone)]) at 400 historical-bootstrap trials each; renders two success percentages + fan charts side-by-side. Called out that this is a heavy operation (~15 sec).

Distinct from the existing "Compare" tab (`Compare.jsx`) which lets advisors pick 2-3 saved scenarios and compare their metrics; that feature is preserved unchanged. `Scale` icon added to the Planner's lucide-react import for the new tab.

Testing: `testing_agent` iteration 45 — 11/11 review criteria pass at 100%, zero console errors. Default scenario shows "Converting delivers $5,150,155 more to heirs · 36% heir rate · $9,464,858 less total family tax", with 91% Convert MC success vs 94% Don't-Convert MC success (the higher no-convert success comes from not paying the conversion tax up front — but after SECURE-10 heir tax, Convert still wins by $5.15M).

## 2026-07-25 (session 3) — Return-values bug fix + tax-chart cleanup + Beneficiaries + Gemini second-opinion CTA

**Two bug fixes + two features.**

**Bug 1 fix — Assumptions page returns**: The rows Cash Return / Taxable Return / IRA Return / Roth Return were reading from `scenario.projection.*_return` which does not exist on the scenario schema — return rates live per-account on `scenario.accounts[*].return`. Fixed `AssumptionsPage.jsx` to compute a beginning-balance-weighted blend by tax_type (Cash / Taxable / Tax-Deferred / Tax-Free). Labels updated to "(blended)" and the footer sub-note now explains where the blend comes from. Default scenario now shows Cash 3.0%, Taxable 7.0%, Traditional IRA 7.0%, Roth 7.0%.

**Bug 2 fix — Tax Composition chart overlap**: The `TaxCompositionChart` (from `AnalyticsCharts.jsx`) is wrapped in a `Panel` component with its own title + subtitle + fixed 280px ResponsiveContainer. Using it inside a 175px container in `TaxesPage.jsx` caused it to overflow — the "Total ordinary income" KPI subtitle ("Wages, pensions, IRA/RMD, taxable SS") stacked BEHIND the chart bars, producing the "Wages text hidden behind the chart" symptom. Fix: inlined a simple `<BarChart>` at 200px in `TaxesPage.jsx` with the same 5-color legend (Federal Ordinary / LTCG / Dividends / NIIT 3.8% / State / Medicare + IRMAA) — no Panel wrapper, KPIs now sit cleanly below the chart. Verified DOM: chart bottom=518px, next KPI top=534px, zero overlap.

**Feature — Per-beneficiary heir bracket modeling**: New "Beneficiaries" table on the Client Report toolbar (`cr-beneficiaries-card`) — columns Name / Share % / Federal rate / State rate. Add / Remove buttons per row. `beneficiaryWeightedRate` in `helpers.jsx` computes a share-weighted average federal + state rate, which supersedes the single-rate `heir_federal_rate` + `heir_state_rate` override whenever any beneficiary has a non-zero share. Beneficiaries persist to `localStorage.client_report_beneficiaries_v1`. UI shows the weighted-average rate + share-sum tally (green when 100%, amber otherwise). Effective heir rate label above shows "(overridden by beneficiaries table below)" when the beneficiary weighting is active. Example: Daughter 60% @ 0.37/0.06 + Son 40% @ 0.22/0.04 → weighted "31.0% federal + 5.2% state = 36.2% combined". This makes the "convert at parents' rates" argument quantitatively airtight rather than assumed.

**Feature — Chat with Gemini second-opinion CTA**: New button (`cr-chat-gemini`) on the Client Report AI Review card labeled "Copy summary & open Gemini". Builds a plan summary containing household names, headline numbers (lifetime taxes / after-tax legacy / heir rate / MC success / total converted), and the SECURE-Act context paragraph; copies it to the clipboard via `navigator.clipboard.writeText`; opens `https://gemini.google.com/app` in a new tab; shows a toast telling the user to paste. Under the button, a prominent privacy warning banner (`cr-gemini-privacy-warning`) explains that free consumer AI services may retain data, advises not to paste account numbers/SSNs, and directs the user to download the PDF for sharing with a paid AI or their own advisor. The same second-opinion CTA + privacy note also appears on the printed AI Review page (`cr-page-ai-review`) so the client can act on it from the PDF at home.

**Testing**: `testing_agent` iteration 46 — **14/14 review criteria pass at 100%, no bugs found**. Popup verified opening `https://gemini.google.com/app`; clipboard verified containing 1,082-char plan summary. All 15 pre-existing client-report testids still render at count=2 (live preview + hidden print root). Backend regression pytest: 25/25 pass (golden snapshot + tax_detail + cashflow suites).

## 2026-07-25 (session 4) — Five features: Heir Rate on Convert Compare + Advisor Logo + Widow Tax Trap + IRMAA Cliff-Aware Optimizer + Regime-Switching Inflation

Massive iteration — 5 features shipped in one session, all tested at 100%.

**Feature 1: Heir Rate on Convert Compare.** Mirrored the Client Report heir-rate/beneficiary infrastructure onto ConvertCompare. New card `cc-heir-rate-card` at the top of the Convert vs Skip tab exposes federal + state marginal-rate overrides, a Reset button, and an "Effective" KPI tile. Reads shared beneficiaries from `localStorage.client_report_beneficiaries_v1` (set on Client Report) and uses their weighted-average as the fallback when no single-input override is set. Changes fire a re-projection instantly and the verdict banner updates within 1-2 seconds.

**Feature 2: Advisor Logo Upload.** New `/app/frontend/src/lib/advisorLogo.js` provides `AdvisorLogoUploader` + `LogoHeader` + `LogoWatermark` + `useAdvisorLogo` hook with `CustomEvent`-based cross-consumer sync (fixes stale-state bug where two hook instances didn't share writes). Base64 stored in `localStorage.advisor_logo_v1` up to 500 KB, PNG/JPEG/SVG/WebP accepted, validated on upload with toast on rejection. Appears (a) as `LogoHeader` at the top of the Client Report cover + Presentation cover, and (b) as small `LogoWatermark` in the Client Report page footer + top-right corner of each Presentation page (except cover). Uploader ships to BOTH branding cards; the same logo is used everywhere — one upload, everywhere.

**Feature 3: Widow Tax Trap.** New `/app/frontend/src/components/WidowTaxTrap.jsx` file. `computeWidowStats(rows)` extracts widow years from the projection's `filing_status` field (engine already flips MFJ→Single at first death), computes actual Fed-ordinary Single-bracket tax vs hypothetical MFJ tax on the same taxable ordinary income using 2024 IRS statutory brackets, and reports the compression cost. Two exports: `WidowTaxTrapWeb` (Tailwind-styled for Convert vs Skip) + `WidowTaxTrapPrint` (inline styles for Client Report Legacy page PDF). Both include a 2024 bracket-cap comparison table showing ~50% compression for the 12/22/24/32% bands and ~17% for 35%. Improved labels post-review to make Actual vs Hypothetical apples-to-apples (federal-ordinary layer only) with an engine-total-tax reference footer.

**Feature 4: IRMAA Cliff-Aware Optimizer.** Backend: `tax_engine.optimize_conversion()` now accepts `irmaa_aware=True` + `irmaa_cliff_buffer` (default $3000); when enabled, clips the recommended conversion so `magi_after = base_magi + conversion` stays at least `buffer` dollars below the next IRMAA tier. Returns `avoided_irmaa_cliff = {threshold, buffer, unconstrained_conversion, avoided_conversion_amount}` for UI display. Six new pytest cases covering naive-crosses-tier1, aware-clips-below-tier1-minus-buffer, matches-naive-when-no-cliff, no-effect-when-include-irmaa-off, buffer-scaling, and Single-filer tier. Frontend: new IRMAA-aware toggle + buffer input (0–$20K) card on the Single-Year Optimizer tab; when clipping was applied, a "Cliff Avoided" badge shows the trim + threshold + buffer. Default scenario: toggle ON trims recommendation from $82,750 to $34,000 (avoiding $410K IRMAA tier by $3K).

**Feature 5: Regime-Switching Stochastic Inflation for Monte Carlo.** Backend: new `_regime_inflation_draws()` in `montecarlo.py` — 3-state (Low/Normal/High) Markov chain with configurable persistence `p_stay` (default 0.85, symmetric off-diagonals) and per-regime lognormal (mean, vol) draws. Draws feed into the standard `_inflation_factors` machinery. Response includes an `inflation.regime_switching` block summarizing time-in-regime + parameters. Four new pytest cases covering regime info emission, off-behavior parity, sticky-chain time-in-regime, and realized-inflation reasonableness. Frontend: new `mc-regime-card` on the Monte Carlo tab (visible only when Stochastic Inflation is ON and engine=lognormal). Contains a toggle, a 3-row editable table (Low 2%/0.8%, Normal 3.5%/1.4%, High 6%/2.5%), and a Persistence slider (50–99%). Both the standalone MC run and the compare-with-non-Convert MC run use the new payload.

**Testing.** `testing_agent` iteration 47 — **14/14 review criteria pass at 100%; 309/309 backend pytest pass (10 new + 299 existing including rebaselined golden snapshot).** Zero bugs found, zero console errors. Five minor review comments were surfaced (toast queue cosmetic, localStorage quota handling, IRMAA badge visual polish, regime input clamping, Widow Tax Trap label ambiguity); the label ambiguity was fixed in this same session (compression tiles now surface the federal-ordinary layer explicitly with an engine-total reference note).

## 2026-07-25 (session 5) — Dedicated Social Security Report tab (+ AI Insights URL fix)

Built a dedicated, print-optimized, 10-page **Social Security Report** as a new top-level Planner tab (`tab-ss-report`, placed on Row 2 immediately after `tab-client-report`). Long-form, client-facing narrative SS analysis following the exact same architectural pattern as `ClientReport.jsx`.

**Architecture.** Main orchestrator `SSReport.jsx` + one file per page under `components/ssReport/`:
- `helpers.jsx` — shared primitives (`Page`, `H1/H2/H3/P/Sub/Kpi`) + SS math (`fullRetirementAge`, `claimFactor`, `survivorBenefit`, `spousalBenefit`).
- `CoverPage.jsx` — household snapshot: names, ages, FRA amounts (PIA in $/mo), FRA ages, filing status, COLA, planned claim ages, advisor block.
- `HowSSWorksPage.jsx` — Plain-English explainer: PIA, FRA table (1943→2060+), full 62→70 reduction/DRC table (70%→124% of PIA), COLA, spousal + survivor rule quick reference. Cites SSA Pub 05-10035 and 20 CFR § 404.410.
- `BenefitsByAgePage.jsx` — Bar chart of nominal + COLA-adjusted monthly benefit at 62/65/67/70 for BOTH spouses, plus a monthly + annual detail table.
- `LifetimeBenefitsPage.jsx` — Multi-line chart of cumulative lifetime SS collected by claim age from current age to life expectancy. Reports the 62-vs-70, 62-vs-67, and 67-vs-70 break-even ages and a "total by LE" table with the winning claim age highlighted.
- `CoordinatedClaimingPage.jsx` — SS sweep winner card + full 16-pair ranked table (Client 62/65/67/70 × Spouse 62/65/67/70). Ranking objective = after-tax legacy at second death. Includes the "Highest lifetime SS ≠ best legacy" callout explicitly tying the ranking to the Roth-conversion + SECURE-10 doctrine.
- `RothInteractionPage.jsx` — Stacked-bar timeline of Roth conversions vs SS ordinary income vs RMDs. Reports pre-SS vs post-SS conversion totals as KPIs. Contains the "delay higher earner to 70" advisor rule of thumb.
- `TaxationIRMAAPage.jsx` — Up-to-85% SS taxability threshold table (MFJ + Single), peak inclusion %, first-85% year, total IRMAA premium paid. Explains the "SS Tax Torpedo" (40.7%–46.25% effective marginal rate on the phase-in) with the Geisler/Harden/Hulse 2021 JFP citation.
- `SurvivorBenefitsPage.jsx` — Survivor-benefit bar chart across the higher earner's claim ages (survivor keeps the higher of the two) + spousal-benefit sub-table showing when the lower earner's 50%-of-higher-PIA spousal claim beats their own reduced benefit.
- `RecommendationsPage.jsx` — Optimizer-derived recommended claim pair + 4 rationale bullets + Next Steps + Caveats.
- `AIReviewPage.jsx` — Streamed AI narrative (Claude Fable 5 via `/api/insights`) with editable textarea, "Chat with Gemini" second-opinion CTA, privacy warning banner.

**Wired into Planner Row 2** between `Client Report` and `Convert vs Skip` — imported `CalendarClock` icon (already in scope from PlanInputs). Uses existing endpoints only: `/api/projection`, `/api/ss-optimizer`, `/api/insights` — zero new backend routes. On tab open the SS sweep auto-runs (~25s for the full 4×4=16 projection sweep) and the live preview populates all 10 pages.

**Bug fix as part of this session** — `${API}/api/insights` in three places was resolving to `${BACKEND_URL}/api/api/insights` (double `/api/`) and returning 404. `API` from `lib/api.js` already ends in `/api`. Fixed the double-prefix in `SSReport.jsx:136`, `ClientReport.jsx:239`, `AIAnalysisCard.jsx:52`. Verified: `curl POST /api/insights` returns HTTP 200 with a Claude Fable 5 streamed response; UI Generate AI Review button now streams cleanly (verified end-to-end via Playwright — 514 chars in 12s, scenario-specific: *"Claiming both benefits at 62 maximizes after-tax legacy — adding ~$1.26M ($49.22M vs $47.95M) versus the current baseline"*).

**Testing.** `testing_agent` iteration 48 — 17/18 (94%) pass on first pass; the one failure was the AI Insights URL bug, which was fixed and self-verified. Regressions checked: `tab-client-report`, `tab-ssopt`, `tab-inputs`, `tab-advisor-info`, and all previously-verified features continue to work. Zero console errors.

## 2026-07-25 (session 6) — What-If Slider + QCD Modeling

Shipped two significant additions requested by the user:

**1. What-If Slider on SS Report toolbar.** A new interactive control in the SS Report toolbar (not on the printed cover, per user preference) lets clients drag two Radix sliders (Client + Spouse claim ages 62–70). Live behavior:
- Instant client-side monthly + COLA-adjusted benefit updates as the slider moves (uses PIA × `claimFactor(age, fraAge) × (1+cola)^yearsToClaim`).
- Debounced (~700ms) `/api/projection` call updates the "after-tax legacy" + "lifetime taxes" tiles.
- Debounced (~1500ms) full 16-pair `/api/ss-optimizer` sweep updates the "best pair" tile (may reveal a different optimum vs the initial sweep).
- Three impact tiles show delta-vs-optimizer, taxes, and best-pair — colored green/orange/gold based on outcome.
- **Apply to plan** button (disabled at parity, enabled when dirty) persists claim ages back to `scenario.household.client_ss_claim_age` + `spouse_ss_claim_age` so Plan Inputs + the whole report body update.
- Reset button restores the household's current claim ages when the slider is dirty.
- File: `frontend/src/components/ssReport/WhatIfSlider.jsx` (~210 lines). Wired via new `setScenario` prop on `SSReport`.

**2. QCD (Qualified Charitable Distribution) modeling.** Charitably-inclined households age 70½+ can route up to $108K/yr (2025 IRS cap, indexed) directly from an IRA to a 501(c)(3) — the dollars satisfy RMD but never enter AGI. Full end-to-end implementation:

*Backend* (`projection.py`):
- New Plan fields: `qcd_annual`, `qcd_start_year`, `qcd_end_year`, `qcd_client_share`, `qcd_annual_cap` (default $108K).
- New helper `_qcd_for_year(plan, status, year, rmd_total)` — respects age-70½ eligibility (approximated at integer 70), spouse split, IRS per-taxpayer cap, capped at RMD.
- `_SolveCtx.qcd_total` threaded into the solver: `taxable_rmd = rmd_total - qcd_total` replaces `rmd_total` everywhere ordinary-income was previously accumulated (tax base `ira_distributions`, funding income, main-loop `funding_income`, reported `ordinary_income` in row output).
- IRA balance still decrements by full RMD (QCD leaves the IRA the same way regular RMD does).
- New row fields: `qcd`, `qcd_by_owner: {Client, Spouse}`, `cashflow.qcd`, plus a "Charitable — QCD" line-item on expenses so the cashflow tab reconciles.
- New summary field: `lifetime_qcd`.
- Golden snapshot rebased once (purely additive fields, all pre-existing numerics identical).
- 8 dedicated pytest cases in `tests/test_qcd_projection.py`: off-is-noop, dollar-for-dollar reduction, IRA-balance parity, IRS-cap enforcement, pre-70 ineligibility, end-year cutoff, cashflow line-item presence, spouse-split with one ineligible spouse. All 8 pass.

*Frontend* (`PlanInputs.jsx`):
- New "Charitable Giving — Qualified Charitable Distribution" card between the household card and Income Streams, with 4 inputs: `hh-qcd-amount`, `hh-qcd-start-year` (auto-suggests client's age-70 year), `hh-qcd-end-year` (blank = through end), `hh-qcd-client-share` (%).
- Cap warning fires when the annual amount exceeds $108K (`hh-qcd-cap-warning`).

*Frontend* (Client Report):
- New KPI tiles on Taxes page (`cr-taxes-qcd-tiles`) — gated on `lifetime_qcd > 0`: "Lifetime QCD directed to charity" + "Estimated tax saved by QCD" (computed as `lifetime_qcd × avg-marginal-rate` during active QCD years).
- New rows on Assumptions page (gated on `qcd_annual_amount > 0`): "QCD annual amount", "QCD active window", "Lifetime QCD (charity total)".

**Testing.** `testing_agent` iteration 49 — **100% pass, backend 305/305 + 8 new QCD tests, frontend end-to-end verified.** What-If Slider dragged 67→70 updated monthly from $3,348 to $4,152 instantly; legacy tile refreshed within ~1.8s; sweep completed and best-pair label updated within ~18s; Apply persisted and reflected on Plan Inputs. QCD KPI tiles rendered correctly with count=2 (preview + print root). Zero console errors. One recurring reviewer note: the master PIN seeder occasionally produces stale bcrypt hashes on this preview environment — the delete-then-restart reseed procedure documented in PRD works and was executed once during this session.

## 2026-07-25 (session 7) — Date-based Plan Inputs + Chart-Click Reproject + Presenter Mode

Shipped a large batch of user-requested changes:

**1. Date-based Plan Inputs.** The Household card now uses ISO date inputs instead of integer ages:
- `client_retirement_date` / `spouse_retirement_date` — replace `_retirement_age`.
- `client_already_retired` / `spouse_already_retired` — new toggle switches; when ON, disable the corresponding date input and treat retirement as today's date.
- `client_ss_claim_date` / `spouse_ss_claim_date` — replace `_ss_claim_age` + `_ss_claim_month`. Default = DOB + 67 years (the 67th birthday).
- A React sync-effect keeps the legacy integer age fields mirrored from the dates so SSOptimizer, backend, and every other consumer keeps working without a full rewrite.
- New helper module `frontend/src/lib/dateAge.js` centralizes date↔age math.

**2. Income-Streams quick-add templates.** Four new template buttons: **+ Client Wages**, **+ Spouse Wages**, **+ Client Pension**, **+ Spouse Pension**. Wages default to start=Jan 1 of current year + stop=owner's retirement date. Pensions default to start=owner's retirement date + 2% COLA + 50% survivor benefit. The original "+ Add" button is preserved (relabeled "+ Custom").

**3. QCD card moved to the BOTTOM** of Plan Inputs (after Tax Assumptions & Heirs) per explicit user preference.

**4. Beneficiaries editor moved from Client Report to Plan Inputs → Tax Assumptions & Heirs.** Extracted the CRUD UI into a new `BeneficiariesEditor.jsx`. Old `[cr-beneficiaries-card]` was replaced by a read-only `[cr-beneficiaries-summary]` (only shown when beneficiaries.length > 0) with an "Edit on Plan Inputs → Tax Assumptions" hint. ClientReport still reads `loadBeneficiaries()` for the SECURE-Act weighted-heir-rate override; edits propagate via `focus` + `storage` events.

**5. Chart Click to Reproject.** SS Report "Benefits by Age" chart is now interactive — clicking any bar calls `onClaimClick(owner, age)` which updates both `client_ss_claim_age` and `client_ss_claim_date` (or spouse equivalents). Toast confirms. Report body reprojects. Click hint `[ssr-benefit-click-hint]` shows above the chart when `setScenario` is available; print root is intentionally NOT interactive.

**6. Presenter Mode.** New "Present" button on the SS Report toolbar opens a fullscreen dark-mode slide deck (`PresenterMode.jsx`). Same 10 pages, client-facing style (`#0E1512` background, off-white slide card centered, `zoom: 1.35` for legibility). Nav via keyboard (← / → / Space / Home / End / Esc), left/right click-zones, chevron buttons on the edges, progress dots at the bottom that jump directly. Best-effort `requestFullscreen()` with silent Promise-rejection handling.

**Testing.** `testing_agent` iteration 50 — **100% pass, frontend-only, zero console errors.** All 18 Plan Inputs testids verified. Presenter counter Slide 1/10 → 5/10 → 6/10 confirmed via arrow keys + dot clicks + left-third click. Reviewer note about `PlanInputs.jsx` growing >900 lines — future refactor opportunity, not blocking.

## 2026-07-25 (session 8) — Security audit + SEC-001 + P3 hardening

Ran `security_audit_agent` on the deployed app. Verdict: **CONDITIONAL PASS — NEEDS ATTENTION**. No confirmed critical/high; one MEDIUM (SEC-001) + three P3 hardening items. User approved fixing all of them.

**SEC-001 fix — Master credential policy + tiered lockout.**
- `auth_licenses.py`: added `is_valid_master_secret` (accepts 6-digit PIN OR 12–128 char passphrase), `MasterAccountLocked` exception, `LOCKOUT_TIERS` (5→15min, 10→60min, 15+→24hr), `_read_master_lockout` / `_lockout_seconds_for_attempts` / `_check_master_lockout` / `_record_master_failure` / `_reset_master_lockout` helpers.
- `verify_master_pin` now: (a) short-circuits on invalid input length while still incrementing the attempt counter (so an attacker can't probe length policy for free), (b) enforces lockout BEFORE bcrypt work, (c) resets the counter on success. Successful login clears state.
- `ensure_master_seeded` accepts passphrases now and logs a WARNING at startup if the current secret is still a weak 6-digit PIN (~20 bits).
- `routes/auth.py`: `pin/verify` catches `MasterAccountLocked` → HTTP 429 with `Retry-After` header + human "Try again after…" message. Combined with the existing `@limiter.limit("5/minute")` per-IP throttle → defense in depth (per-IP + per-account).
- `models.py`: `PinVerifyRequest.pin` and `PinChangeRequest` max_length raised 32 → 128 to accept passphrases.
- **15 new pytest cases** in `tests/test_auth_hardening.py` locking the passphrase policy, lockout tiers (5/10/15), malformed-input attempt counting, expired-lockout replay, and success-reset — all passing.

**Frontend SEC-001 fix — LicenseLogin master-mode UI.**
- Master tab now renders a `type="password"` text input (accepts 6 digits OR 12+ char passphrase) with helper text "Master passcode (6-digit PIN or 12+ char passphrase)".
- Licensee tab still uses the 6-digit `InputOTP` (unchanged).
- Legacy PIN 140431 still works during the transition (users can rotate to a passphrase in env at any time).

**P3 hardening.**
- `backend/.env` — `CORS_ORIGINS` changed from `"*"` to explicit `"https://roth-retirement-tool.preview.emergentagent.com,http://localhost:3000"` allowlist. The Emergent preview ingress (Cloudflare) still emits a wildcard header at the edge — out of the app's control; the fix takes effect when deployed behind a stricter ingress.
- `clientReport/LegacyPage.jsx` — replaced `dangerouslySetInnerHTML` with plain JSX text using real curly apostrophes.
- `lib/advisorLogo.js` — removed `image/svg+xml` from `ACCEPTED`, the `<input accept>` attribute, and the uploader label ("Upload PNG, JPEG or WebP (max 500 KB)"). Client-side check is defense-in-depth; no server upload today.

**Testing.** `testing_agent` iteration 51 — **100% pass across backend (24/24 pytest) and frontend E2E, zero console errors.** Live checks: correct PIN → 200 + 232-char token; 12-char wrong passphrase → 401 (length policy accepted); success resets `master_lockout.attempts` to 0; expired `locked_until` allows the next attempt. `LegacyPage.jsx` has 0 `dangerouslySetInnerHTML` occurrences; `advisorLogo.js` no longer accepts SVG.

## 2026-07-25 (session 9) — Master PIN rotation (SEC-001 close-out)

Rotated the master credential from the legacy 6-digit PIN `140431` to a 24-char base64-alphabet passphrase (env-safe, no shell-escaping needed). Steps taken and verified:

1. Generated via `openssl rand -base64 24 | tr -d '=/+' | head -c 24` — full base64 alphabet minus problematic env chars.
2. `backend/.env`: `MASTER_ADMIN_PIN="i4m07MnVDhpTYkc1giC6wWDv"` (recorded in `/app/memory/test_credentials.md` for downstream agent testing).
3. Deleted `auth_config.master_pin` and `auth_config.master_lockout` docs from Mongo so the seeder re-hashes on next boot.
4. Restarted backend → seeder emitted `master PIN rotated from MASTER_ADMIN_PIN env change` (INFO). The weak-PIN warning is now gone from startup logs.
5. Verified end-to-end:
   - `POST /api/auth/pin/verify {pin:"140431"}` → **HTTP 401 Incorrect passcode** (old creds dead)
   - `POST /api/auth/pin/verify {pin:"i4m07MnVDhpTYkc1giC6wWDv"}` → **HTTP 200** + 232-char master token
   - Frontend login: old PIN → error banner; new passphrase → app unlocks, master badge + Admin tab visible, 0 console errors

**SEC-001 status: CLOSED (CONDITIONAL PASS → CLEAR PASS).**

## Phase 50 (June 2026) — External report-review corrections (6 fixes, all shipped & tested — iteration_67.json)

User supplied a reviewer's critique of the generated Client Report; all six required fixes implemented per user-approved scope (projection-only reconciliation mode; relabel + full bridge; fully neutral no-stars presentation; ship all 3 MC distributions):

1. **Estate ↔ retirement reconciliation** — `ep_flowchart.py` now accepts per-class `y2_roth/y2_taxable/y2_cash_house/y2_traditional` (retirement projection's actual second-death balances) and scales each asset class to land exactly on the cash-flow/tax model (`growth_basis: "projection"`, `y2_reconciled_total`, `implied_growth`, orphan-class dollars credited to survivor). Uniform-growth is now only an internal fallback for zero-Y1 classes. All callers updated: `EpFlowchart.jsx` (reconciled chip `flow-reconciled-total`, "Uniform growth rate" input removed), `ScenarioCompareBar.jsx` (each side reconciles to its own projection), `ClientReport.jsx` (both flowchart fetches), texts/methodology on `EpFlowchartPage.jsx` + `PlanFlowchart.jsx`.
2. **2026 constants** — Fed estate exemption text → $15M/person 2026 OBBBA (LegacyPage, Estate.jsx, EstatePlanningPage, RothTrustNarrativePage, DsueChecklist ×2, WhitePaper); QCD cap → $111,000 2026 (engine default in `projection.py` 108000→111000, PlanInputs texts, BasisStepUpPage); `WidowTaxTrap.jsx` rewritten on 2026 statutory MFJ/Single brackets (matches backend tax tables).
3. **"-59.9% marginal tax at conversion" fixed** — relabeled "Each dollar converted — lifetime tax efficiency" (explicitly NOT the marginal rate paid at conversion; removed misleading "rate arbitrage" row) + new **Conversion tax bridge** table (`cr-legacy-tax-bridge`): conversion-year federal/state/NIIT, lifetime IRMAA Δ (2-yr lookback), later-year savings (residual-folded to tie exactly), heirs' SECURE-10 Δ, net family Δ.
4. **All "winner" references removed from estate/EP surfaces** — deleted `PlanRecommendationBanner.jsx` + `planRecommendation.js`; neutralized `PlanComparisonTable` (no stars), EpFlowchartComparePage narrative, Estate tab verdict box (`estate-verdict` gone), Award icons, winner-highlighting in tables/charts/ribbons/waterfalls, EstatePlanningPage KPIs → "Largest/Smallest/Range", "Which order wins?" → "How the funding order matters", RothTrustNarrativePage "declares a winner" phrase. (RegimeComparePage's market-regime resilience ranking intentionally kept — not an estate endorsement.)
5. **Funding-order contradiction eliminated** — BasisStepUpPage "Order-of-consumption guidance" replaced by `cr-funding-order-statement`: states the engine's ACTUAL funding order (dynamic from scenario) + user-supplied note that taxable-funded conversions can produce larger Roth balances for heirs + $111K QCD note.
6. **MC outcome distributions** — `montecarlo.py` `_simulate` tracks per-trial `conv_paid` (exact) and `tax_paid` (model-locked, halt-aware, inflation-scaled); `run_montecarlo` returns `outcome_distributions` {conversions (p5–p95, planned_total, pct_trials_full_plan, histogram, basis exact), lifetime_taxes (model_locked), after_tax_inheritance (approximation: det heirs÷ending-wealth ratio × trial ending wealth − heir tax on skipped conversions)}. UI: `OutcomeDistributionsCard` on MC tab (`mc-outcome-dist-card`) + Client Report MC page table (`cr-mc-outcome-dist`).

Housekeeping this phase: fixed real regression in `strategy_stress.py` (_simulate unpack); updated stale Phase-44-era constants in test_phase17/18/19 (lifetime_taxes 6351096.5, ending NW 84563211.53, heirs 162500588.9 — verified against engine + golden); NC state test rate 4.25%→3.99% (2026); golden baseline regenerated (only montecarlo section drifted — new key); `test_iter63` unauth test now uses the `X-Test-No-Auth` escape hatch; new `tests/test_phase50_reconciliation.py` (10 tests). Full targeted suites green; testing agent iteration_67: backend 100%, frontend 100%.

### Notes for future agents (Phase 50)
- **Do NOT issue multiple parallel search_replace calls against the SAME file** — they race and can drop edits or duplicate file tails (bit twice this session: montecarlo.py, Estate.jsx). One edit per file per batch; parallelize across files only.
- Mass pytest runs trip the 5/min auth rate limit → spurious 401/429. Export `ADVISOR_PIN` + `REACT_APP_BACKEND_URL` and run suites in batches; clear `auth_config.master_lockout` if locked.
- EP flowchart API contract: send all four `y2_*` fields to get projection mode; omit all for legacy uniform growth.


---

## Phase 51 (2026-08-15) — Second-pass reviewer critique: all 7 points fixed (iteration_68: 13/14 PASS, last item fixed + self-verified)

1. **Net-worth terminology (pt 1)** — Overview donut → "Investable Wealth at 2nd Death" (`cr-overview-investable-label`); removed dead `home` key (projection rows use `real_estate`, so the old caption falsely claimed home was included); wealth-line caption now "investable accounts only … home equity is not included"; Savings KPIs → "Starting/Ending investable wealth (Excl. home equity)"; BasisStepUp "Total household" → "Total investable accounts" + scope note pointing at EP gross estate.
2. **Page-12 estate model RE-BASED (pt 2, user chose the bigger engine change)** — `estate.py project_estate` accepts `y2_roth/y2_taxable/y2_traditional`; per-class Y1→Y2 scaling factors from the retirement projection (`growth_basis:"projection"`, `implied_growth`, `y2_targets`; orphan classes credited to survivor); `_trust_value_at` splits Y1→Y2 (projection factor) vs post-Y2 (trust rate). Route `POST /api/estate/analyze` gained the 3 optional fields. Callers ClientReport.jsx + Estate.jsx fold cash+house into Taxable at BOTH deaths and send y2_*. Green chip `cr-estate-recon-chip` verified live: Roth $25,300,494 + Taxable(incl. cash & house) $13,303,857 = **$38,604,351 — exact match to EP Projection pages**. Note: GST-vs-portability deltas are now small/negative by construction (differences come only from post-death drag/step-up, not between-death rate spreads).
3. **Legacy disclaimer (pt 3)** — bold "do NOT include" (the italic `<em>not</em>` had vanished in the reviewer's PDF render).
4. **Pages 19-22 replaced (pt 4)** — NEW `clientReport/SensitivityPage.jsx` (`cr-page-sensitivity`): 3 controlled single-variable runs off the CURRENT plan — A conversion-schedule-only, B funding-order-only, C per-tax-type return-assumptions-only — plus a full-compare reference row marked "not attributable". Plus ONE `EpFlowchartCombinedComparePage` (`cr-page-flow-combined-compare`) with amber "all differences at once — not causal attribution" warning (lists differing death years when applicable). Old 4 per-plan `EpFlowchartScenarioComparePage` pages deleted. Sensitivity variants built + run (3 extra projections) inside ClientReport.jsx's compare effect.
5. **FET line-by-line (pt 5)** — `FetCalcTable` (`cr-flow-fet-calc-N`) on every EP plan page: survivor's gross estate → Y2 exclusion (indexed) → DSUE (frozen at Y1 value per Form 706, minus Y1 trust funding) → combined shelter → excess ×40% = FET, + GST-non-portability note. Plus gross-estate↔investable-wealth bridge box (`cr-flow-bridge-N`) with exact dollars (uses new `y2Split` cash/home prop). Verified current engine gives FET $0 on Plans 1/4 for these estates (reviewer's $697,583 was from an older build; now reproducible line-by-line).
6. **MC methodology copy (pt 6)** — allocation text now: modeled mix "is taken from the Household Allocation card on Plan Inputs" (stale "does not currently gather" sentence removed). Engine already consumed `allocationToAssets(scenario.allocation)` — copy-only fix.
7. **Regime P90 anomaly ROOT-CAUSED & FIXED (pt 7)** — `montecarlo._plan_return_path` exploded when the deterministic plan depleted under a stressed regime: implied return divides by near-zero balances → +100%/yr clip → ×1.87/yr anchor multiplier compounding in the bootstrap → billion-dollar P90 tails on ~50%-success regimes (reviewer's $2.086B). Fix: validity floor (prev balance > 5% of starting liquid), invalid years filled with the plan's flat liquid-weighted return (fallback param), clip tightened ±50%/yr. Repro: Plan A lost-decade P90 $119M → $1.35M. RegimeComparePage methodology note added.

Housekeeping: `tests/conftest.py` now mints post-Phase-34 master-role tokens locally (auth_licenses.mint_master_token) and only attempts HTTP pin verify when ADVISOR_PIN is explicitly set — stale-PIN attempts were poisoning the master lockout and 401-failing ~118 HTTP tests. Full backend suite green (512 tests; 5 rate-limit collisions in parallel run pass in isolation). New `tests/test_phase51_report_critique.py` (10 tests: anchor-path guardrails, estate re-base conservation/orphans/legacy-parity/h0-horizon). Tester's `TEST_Compare_Scenario` removed from DB.

**Recurring pitfall hit AGAIN this session:** parallel search_replace calls against the SAME file silently dropped 3 edits (ClientReport.jsx y2 payload, Estate.jsx y2 payload, montecarlo.py caller). Always ONE edit per file per parallel batch, then grep-verify.


---

## Phase 52 (2026-02-15) — Preset Auto-Run: "Run sweep now" toast action jumps Plan Inputs → Strategy Optimizer

- **UX**: After clicking any goal preset on Plan Inputs (Legacy-first, Tax-minimizer, Roth-maximizer, Portfolio at 2nd death), the confirmation toast now shows a green "Run sweep now" action button. Tapping it navigates directly to the Strategy Optimizer tab and immediately kicks off the sweep with the preset's goal / phased / funding-sweep settings already applied. Reduces preset-to-results from 4 clicks to 1.
- **Wiring**:
  - `Planner.jsx` — `Tabs` converted from `defaultValue` to controlled (`value={activeTab}` + `onValueChange={setActiveTab}`); new `autoRunPending` state; new `handleRequestRunSweep()` sets both `autoRunPending=true` and `activeTab="strategy"` atomically; initial tab derived from `?share=` URL param so the shared-view default still lands on Optimizer.
  - `PlanInputs.jsx` — accepts optional `onRequestRunSweep` prop, forwards to `GoalPresetButtons` as `onRunSweep`.
  - `strategy/GoalPresetButtons.jsx` — when `onRunSweep` prop is provided, the preset toast is enriched with `action: { label: "Run sweep now", onClick }` + `actionButtonStyle` (brand green) + `duration: 8000ms` so the advisor has time to react. StrategyOptimizer-tab preset buttons pass no callback, so their toasts stay plain (no need for a jump since the sweep is right there).
  - `StrategyOptimizer.jsx` — accepts optional `autoRunPending` / `onAutoRunConsumed` props; a mount-guarded `useEffect` fires `run()` exactly once when the flag lands true, then notifies the parent to clear it (`useRef` guard against StrictMode double-invocation). Non-breaking: default props (`false`, `null`) mean tab-native manual navigation is unaffected.
- **Verified live**: Playwright smoke-test flow logged in as master → Plan Inputs → click Legacy-first / Tax-minimizer preset → "Run sweep now" toast action → auto-navigation to Strategy Optimizer with `data-state=active` → `[data-testid="strategy-run"]` reads "Sweeping…" → completes with `[data-testid="strategy-winner"]` populated. Regression: manual tab clicks + StrategyOptimizer's own preset row (no auto-run) still behave exactly as before.
- **Pitfall re-hit**: parallel `search_replace` calls against `Planner.jsx` in the same batch caused one edit to silently drop (the `defaultValue` → `value` swap). Verified via `git diff` and re-applied sequentially. Same lesson as Phase 51 — one edit per file per batch when files are large.


---

## Phase 53 (2026-02-15) — Advisor-preferred defaults + Report/Presentation control strip

**Batch 1 — default flips (all engine-level, applied on both `defaults.py` and the cached `user_defaults.json` so a fresh session picks them up without editing state):**

- Anonymize client identity → **default ON** in ClientReport, SSReport, and Presentation branding. Existing users keep whatever they had persisted in localStorage; new sessions see anonymized names by default.
- Strategy Optimizer: `DEFAULT_INCLUDE_PHASED = false`, `DEFAULT_SWEEP_FUNDING = true` (previously the opposite). Applies to any scenario without an explicit `optimizer.include_phased` / `optimizer.sweep_funding_orders` — presets still override.
- Monte Carlo: correlated draws → **ON by default**, early bear-market shock → **ON by default** (–20% for 2 yrs), halt conversions → **ON by default at 25% drawdown, resume after 2 positive years**. All done via `useState` init values in MonteCarlo.jsx.
- Plan defaults: `tax.state_code = "NC"` (rate remains 3.99%). Heir tax rates: `heir_federal_rate = 0.32`, `heir_state_rate = 0.04`, `heir_ltcg_rate = 0.228` (15% + 3.8% NIIT + 4% state). Both `backend/defaults.py` and `backend/user_defaults.json` updated so a `POST /api/planning/defaults/reset` (via UI Reset-to-defaults) reloads to the new values.

**Batch 2 — new `PlanControlStrip` component (`frontend/src/components/PlanControlStrip.jsx`):**

- Three-cell dropdown row rendered at the top of Client Report AND Presentation tabs.
  - **Strategy Modeled** — Full ladder per user directive: No conversion, Fill 12%, 22%, 24%, 32%, 35%, 37%. Picking a bracket writes `scenario.roth.{enabled, target_bracket, start_year, end_year}` and clears any `year_targets`. Phased schedules (from the Strategy Optimizer) are surfaced as a read-only "Custom / phased (locked)" sentinel with an inline note directing the advisor back to the Optimizer.
  - **Funding Order** — matches `backend/strategy_optimizer.FUNDING_ORDERS` (Cash → Taxable → IRA → Roth · Cash → IRA → Taxable → Roth · Split IRA & Taxable). Writes `scenario.withdrawal.funding_order`.
  - **Market Regime** — reuses `useMarketPresets`, writes `scenario.market_scenario.id`. Same source of truth as Plan Inputs / Monte Carlo.
- Because it mutates the shared `scenario` object, every downstream page (Projection, Estate, EP Flowchart, Client Report body, Presentation deck) re-computes automatically. No extra prop drilling.
- Presentation.jsx now accepts `setScenario` (Planner.jsx passes it through). The strip renders only when `setScenario` is provided so the read-only shared-view path is untouched.

**Verification (Playwright smoke test, master login, Reset-to-defaults, localStorage cleared):**
- Plan Inputs → State selector shows "North Carolina"; `[data-testid="heir-fed-rate"] = 0.32`, `[data-testid="heir-state-rate"] = 0.04`, `[data-testid="tax-state-rate"] = 0.0399`.
- Client Report → `[data-testid="cr-plan-control-strip"]` present; `[data-testid="cr-anonymize-toggle"]` = checked.
- Presentation → `[data-testid="pres-plan-control-strip"]` present; `[data-testid="pres-anonymize-toggle"]` = checked.
- Strategy Optimizer → `[data-testid="strategy-include-phased"]` = unchecked; `[data-testid="strategy-sweep-funding"]` = checked.
- Monte Carlo → `mc-shock-toggle` = checked, `mc-shock-rate` = -20, `mc-corr-toggle` = checked, `mc-halt-toggle` = checked, `mc-halt-drop` = 25, `mc-halt-resume` = 2.


---

## Phase 54 (2026-02-15) — Per-license "Save my defaults" on the control strip

**Problem:** the existing header "Save as defaults" writes to a shared `user_defaults.json` — good for a single-tenant tool but stomps every licensee's defaults in a multi-advisor deployment. Advisors want their own reset picks to boot next time without touching other licensees.

**Backend (`backend/routes/planning.py` + `deps.py`):**
- New MongoDB collection `advisor_defaults`, docs `{_id: "<role>:<sub>", config: {...}, updated_at: iso}`.
- New helper `_advisor_id_from_auth(authorization)` reads the JWT and returns `master:master` or `licensee:<license_id>` — stable per-advisor identity.
- `GET /api/defaults` is now auth-aware: **per-advisor doc → shared `user_defaults.json` → hard-coded `DEFAULT_SCENARIO`** (falls back gracefully when the caller has no bearer token, e.g. share links).
- `POST /api/defaults/mine` (new) — upserts the caller's per-license doc. Rate-limited 10/min. Uses `require_advisor` so master OR licensee can save; the doc is keyed by their JWT identity.
- `DELETE /api/defaults/mine` (new) — clears the per-license doc; GET then falls back to the shared file / hardcoded scenario.
- Legacy `/defaults/save` (writes to shared `user_defaults.json`) is untouched — the header button still targets it, so nothing regresses.

**Frontend (`PlanControlStrip.jsx`):**
- Two new buttons on the strip header: `save-mine` (green outline, Save icon) and `revert-mine` (amber outline, RotateCcw icon). Live on both Client Report and Presentation because the strip is shared.
- Hooked to new `saveMyDefaults` / `revertMyDefaults` API methods (`lib/api.js`). Save persists the CURRENT `scenario` — every downstream tab already reads from `scenario`, so whatever the advisor sees now is exactly what boots next time.
- Toast feedback ("Saved as your defaults · Every future login on this license will boot with these picks."; "Reverted to app defaults · Your license-scoped defaults were cleared."). Loader spinners while requests are in flight; disabled state when shared-view (no `setScenario`).

**Verification:**
- API curl round-trip: master sets `target_bracket=0.32` → GET returns 0.32 → DELETE → GET returns 0.24 (shared fallback). Then created a test licensee, saved 0.37 for THAT licensee, confirmed master still sees 0.24 (isolation proven).
- Playwright: Client Report → change Strategy Modeled to Fill 32% → click "Save my defaults" → hard reload → dropdown reads "Fill 32%" ✓. Click "Revert" → dropdown returns to "Fill 24%" ✓.
- Backend suite untouched; new endpoints added without altering the existing `/defaults` / `/defaults/save` contracts.

## Phase 56 — 2026-06 (fork): Goal-linked chips, end-year sweep, FET grid, MC-rebasis envelope + stale detection
Six features from user request, all verified (testing agent iteration_69 + follow-up Playwright):
1. **Best funding order per bracket → goal-linked** (`BestFundingChipStrip.jsx`): chips now rank by the ACTIVE optimization goal (`sortKey`/`rankDir` props from `StrategyOptimizer`), not static after-tax legacy. Popover copy is metric-aware ("wins on lifetime tax… $X less than").
2. **Sweep horizon control** (`strategy-sweep-horizon`): presets Plan boundary / Life-expectancy year / Custom year. When the horizon exceeds the plan end-year, `strategy_sweep(horizon_end_year=…)` deep-copies cfg and extends `projection.end_year` so all rows (baselines included) are scored on the same extended horizon. Response now returns `plan_end_year` + `horizon_end_year_used`. Persisted per scenario via `optimizer.sweep_horizon(_year)`.
3. **Economic-completion callout + boundary badge** on the winner card: earliest same-bracket/start/order stop-year within 0.05% of the winner's goal metric. If that year == last sampled stop → amber "Stops at the sweep boundary" badge; else green "Economic completion year: X — conversions past X are tax prepayments with no heir-rate benefit."
4. **FET sensitivity grid** (`/api/estate/fet-sensitivity`, `FetSensitivityGrid.jsx`): 3×3 growth (5/7/9%) × death timing (±5 yrs both deaths). Each cell pairs Portability-Only + Layered GST fed estate tax; bold = HIGHEST of the pair (user forbade the word "Winner"). Y2 re-basing intentionally dropped so the growth axis moves the outcome; card copy explains the approximation.
5. **MC-rebasis stale detection**: `Planner.handleMcResult` keeps a persistent `mcForEstate` copy (survives the scenarioSig-triggered `mcResult` reset) stamped with `mcScenarioSig(scenario)` (`lib/mcSignature.js`, excludes `optimizer` subtree). Estate shows amber `estate-mc-stale-warning` when the sig no longer matches.
6. **Net-to-heirs envelope chart** (`EstateMcEnvelopeChart.jsx`): grouped bars, Portability vs Layered GST at Deterministic / MC P90 / MC Median / MC P10, rendered under the rebasis picker via 4 parallel `/estate/analyze` calls sharing `rebasisFor(mode)`.

**Critical wiring fix found by testing agent:** Estate's rebasis had been reading `stressResult.outcome_distributions` — but `/api/strategy-stress` never returns that key; it comes from `/api/montecarlo`. Estate is now fed `mcForEstate` (MonteCarlo tab result) instead of the stress-test result; prop renamed `stressResult` → `mcResult` throughout `Estate.jsx`. This means the pre-existing MC-rebasis picker had NEVER actually activated e2e until now.

**Also this session (analysis):** explained why Fill-24%·2026–2046 beats 2026–2062 by ~$85K — both plans fully drain the IRA during life at the same rates (identical non-Roth at death to the dollar), so late conversions are pure tax PREPAYMENTS (~$170K paid 3-6 yrs earlier) that lose compounding. This insight drove feature 3.


## Phase 57 — 2026-02-16 (fork): Client Report PDF — chart/legend crop + text sharpness fix
User bug report: "on the client report pdf the charts on pages 2, 3, 4 and 5 are cut off on the right side and the text on these pages does not render cleanly." Verified by pdftoppm rasterization of the uploaded PDF — pages 3/4/5 x-axis showed "205" instead of "2056", and page 5 Fed/State legends truncated "Social Security (taxable)" to "Social Security (tax".

Root cause: html2canvas clips SVG content to the SVG's declared viewBox during serialization. Recharts places tick-label `<text>` centered on the last tick with only 8px of `margin.right` — the right half of each last label extended past the SVG's right edge and got cropped. Same mechanism truncated Legend items rendered inside the ResponsiveContainer.

Fix (`/app/frontend/src/lib/pdf.js`, `clientReport/*.jsx`):
1. **Chart right margin 8 → 24** on every Recharts chart across the Client Report pages (Overview, Savings, Income & Expenses ×4, Legacy, Monte Carlo, Taxes) so tick labels stay inside the SVG.
2. **StaticLegend HTML component** in `IncomeExpensesPage.jsx` replaces Recharts `<Legend>` on the two 8-series stacked BarCharts (Federal + State taxable income by source). Renders items as flex-wrap HTML BELOW the chart — outside the SVG viewport — so html2canvas captures the full legend and items wrap naturally when they don't fit on one row.
3. **html2canvas scale 2 → 3** in `renderChunkedPdf` for sharper text after the fit-to-190mm downscale. Added RAF+setTimeout(200ms) settle so Recharts' ResizeObserver finishes reflowing after the preview→print `transform: none; width: 100%` switch before rasterization.

**Tests**: Testing agent iteration_70 confirmed the tick-label fix + flagged remaining legend truncation. Iteration_71 confirmed the legend fix — 100% of acceptance criteria met (page 3/4 "2056" full, page 5 Fed+State legends fully readable, download OK, no JS console errors). Zero backend changes.

## Phase 57.1 — 2026-02-16: StaticLegend rollout across all Client Report + SS Report charts
Follow-up to Phase 57. Extended the plain-HTML flex-wrap `StaticLegend` (introduced for the Federal/State stacked bar charts on Client Report page 5) to every multi-series chart across BOTH the Client Report and the SS Report so no legend renders inside a Recharts SVG anymore — eliminating any residual risk of html2canvas clipping legend items to the SVG viewBox.

Changes:
- Promoted `StaticLegend` from a local component in `IncomeExpensesPage.jsx` to a shared export in `/app/frontend/src/components/clientReport/helpers.jsx`. Added an `items | keys+colors` prop shape, optional `size` and `align` props.
- Re-exported `StaticLegend` from `/app/frontend/src/components/ssReport/helpers.jsx` so SS Report pages consume the same component.
- Replaced Recharts `<Legend>` with `<StaticLegend>` in: `OverviewPage.jsx` (donut + wealth line), `SavingsPage.jsx` (area), `IncomeExpensesPage.jsx` (all 4 stacked bars), `LegacyPage.jsx` (heir bars), `MonteCarloPage.jsx` (percentiles), `TaxesPage.jsx` (taxes-by-type), `BenefitsByAgePage.jsx`, `LifetimeBenefitsPage.jsx`, `RothInteractionPage.jsx`, `SurvivorBenefitsPage.jsx`. Also removed the now-unused `Legend` import from each recharts import statement.
- Bumped `margin.right` from 12 → 24 on the LifetimeBenefitsPage line chart, RothInteractionPage bar chart, and SurvivorBenefitsPage bar chart (they were still at 12) for x-axis tick-label safety consistent with Client Report charts.

Tests: iteration_72 — 100% pass. Verified zero remaining Recharts `<Legend>` usages via grep. Visual inspection of all Client Report pages 3-9 and SS Report pages 3-8 confirmed legends render as a centered flex-wrap HTML row BELOW each chart. `Social Security (taxable)` on the Fed/State legends wraps to a 2nd row as expected. The "2056" x-axis fix from Phase 57 still holds. Zero JS console errors, zero page errors during PDF generation. Client Report: 9 pages, ~6.5MB. SS Report: 9 pages, ~5.7MB.


## Phase 58 — 2026-02-16: EP Projection Plan 5 — Disclaimer Trust
User asked: "Add Plan 5 to Client Summary. This is a Disclaimer Trust Plan. Note that the plan assumes that only the Client's Roth IRA is disclaimed to the Disclaimer GST Trust. This preserves the ability for the Spouse's estate to receive a second step on taxable assets. Note that this strategy provides flexibility for the Spouse to determine if GST or Estate Tax planning is necessary or desirable given the state of the family's assets up to nine months (or more with extensions) after the date of the Client's death."

Added Plan 5 (`disclaimer_roth`) to the EP Projection flowchart engine:
- **Backend** (`/app/backend/ep_flowchart.py`): New PLAN_DEFS entry with a 1,000+ char subtitle describing the 9-month disclaimer election, DSUE preservation, and 2nd-step-up preservation on non-Roth assets. `run_plan()` `y1_order` dispatch aliases `disclaimer_roth → roth_only` — Plan 5 is numerically identical to Plan 3 in the modeled central case (Spouse elects to disclaim); the distinction is narrative optionality.
- **Frontend**:
  - `ReportCustomizationCard.jsx`: Added Plan 5 toggle row ('Plan 5 — Disclaimer Trust (Roth disclaimed, 2nd step-up preserved)').
  - `ClientReport.jsx`: `flowPlans` default merged with `{5: true}` so users upgrading from the 4-plan version pick up Plan 5 without losing prior selections; `flowSel` filter now iterates `[1,2,3,4,5]`.
  - `PlanFlowchart.jsx`: Plan-5-specific labels — Y1 arrow reads "Spouse DISCLAIMS Roth only (within 9 months) — up to exclusion"; Y1 funding box titled "Disclaimer GST trust funding"; Y2 trust box titled "Client Disclaimer GST Trust" with an extended note referencing the 9-month election window.
  - `EpFlowchart.jsx` + `EpFlowchartPage.jsx`: Intro/summary narrative updated from "Four → Five" plans; comment references now include "Plans 2, 3, and 5" as first-death funding structures.

Tests (`test_iter74_ep_flowchart_plan5.py`, iteration_74, 100% pass):
- Backend: `POST /api/estate/ep-flowchart` now returns 5 plans. Plan 5 metrics/funding/totals equal Plan 3 to the dollar (guardrail assertion).
- Frontend: ReportCustomizationCard shows all 5 toggles; preview renders Plan 5 section with disclaimer-specific box titles + arrow label; standalone EP tab intro reads "Five estate-funding plans"; no Plan 6 present.
- No regression on Plans 1-4.


## Phase 58.1 — 2026-02-16: Plan 5 Disclaimer Fallback Callout
User asked: "Disclaimer Fallback Callout: Add a subtle 'If Spouse elects NOT to disclaim' annotation on the Plan 5 flowchart so clients see the fallback = Plan 1 outcome."

Added an additive UI block at the bottom of the Plan 5 flowchart card in `PlanFlowchart.jsx` — gated on `plan.key === "disclaimer_roth"`. The callout surfaces (a) plain-English text that not electing collapses to Plan 1's baseline, (b) the 9-month election window + no-prior-benefit language, and (c) a three-cell metric row showing Plan 1's total-to-Trusts-and-Children, Plan 1's federal estate tax, and the delta ("cost of NOT disclaiming"). When Plan 5 == Plan 1 (small-estate scenarios), the delta cell renders "no dollar impact in this scenario" instead of a negative number. Style is deliberately subtle: 1px dashed border (#C9BFA8), warm-neutral background (#FDFBF6), 10.5px muted body text — reads as a note, not an alert. Renders in both the on-screen preview AND the printed PDF (plain HTML, no SVG dependency).

Testids: `flow-p5-disclaimer-fallback`, `flow-p5-fallback-total`, `flow-p5-fallback-fet`, `flow-p5-fallback-cost`.

Tests (iteration_75, 100% pass): callout renders exclusively on Plan 5, dollar amounts match Plan 1's chip values (source-of-truth), edge case "no dollar impact" branch works, subtle-style CSS spec satisfied.


## Phase 58.2 — 2026-02-16: Plan 5 Interactive Fallback Toggle
User asked: "add an interactive 'What if Spouse doesn't disclaim?' toggle on the on-screen Plan 5 card that live-swaps Plan 5's numbers to Plan 1's? Same fallback semantics, but interactive — advisors could show clients the exact drop in a single click during the meeting instead of reading it from a static callout."

Added a pill-shaped toggle button in the top-right of the Plan 5 (`disclaimer_roth`) flowchart card. Click behavior:
- Toggle OFF (default): "What if Spouse doesn't disclaim?" — neutral styling (`#F9F8F6` bg, `#D6CFB8` border). Flowchart renders Plan 5's central-case data.
- Toggle ON: "Fallback view — click to reset" — warm-amber active state (`#8A5A20` bg, white text) + companion "SHOWING FALLBACK" badge. The ENTIRE flowchart body (Y1 funding boxes, trust boxes, survivor estate boxes, children boxes, arrow labels, metric chips) live-swaps to Plan 1's data via a `showPlan = fallback && p1 ? p1 : plan` computed value. The card header, PLAN 5 badge, title, and subtitle stay put so advisors can talk the client through the swap live. The static bottom callout retains its role and continues to read Plan 5's ORIGINAL metrics (documented in code) so the delta text there stays stable regardless of toggle state.

PDF safety: state is local `useState`, never persisted. A `useEffect` listens for the shared `cr-reset-isolation` window event (already dispatched by every doPrint before html2canvas capture) and resets the toggle to `false` — so the printed PDF always captures Plan 5's central case regardless of on-screen state at click-time.

New testids/attributes: `flow-p5-fallback-toggle`, `flow-p5-fallback-badge`, `data-fallback="true|false"` on the card root (only when the plan supports it), `data-active` on the toggle.

Tests (iteration_76, 100% pass): only Plan 5 has the toggle; initial state correct; 1st click swaps body to Plan 1 (Y1 trust boxes disappear, chip totals match Plan 1); bottom callout numbers stay put (uses original `plan.metrics`); 2nd click reverts; window-event reset guarantees the PDF captures the un-toggled Plan 5 view.


## Phase 58.3 — 2026-02-16: EP Projection plan reorder — Disclaimer Trust promoted to Plan 2
User asked: "On client Report reorder Plans so the current Plan 5 becomes Plan 2, current Plan 2 becomes Plan 3, current Plan 3 becomes Plan 4, current Plan 4 becomes Plan 5."

New authoritative order (Client Report + standalone EP tab consistent):

| Plan # | Key                 | Title (short)                     |
|--------|---------------------|-----------------------------------|
| 1      | `no_trust`          | No Trust Planning                 |
| 2      | `disclaimer_roth`   | Disclaimer Trust (Roth disclaimed)|
| 3      | `roth_and_taxable`  | Taxable-First GST Trust           |
| 4      | `roth_only`         | Roth-Only GST Trust               |
| 5      | `second_death_only` | GST Trust at Second Death         |

Rationale: promote the disclaimer structure so the FLEXIBLE/optionality-preserving strategy sits right after the no-planning baseline, and the more committed dynasty structures follow in progression.

Changes:
- `ep_flowchart.py` — reordered `PLAN_DEFS`; docstring rewritten; Plan 2 subtitle now references "Plan 4 (Roth-only)" as its numeric twin (was "Plan 3").
- `ReportCustomizationCard.jsx` — toggle labels reordered.
- `ClientReport.jsx` — localStorage key bumped `client_report_flow_plans_v1` → `v2` so users with stale v1 state (keyed by old plan numbers) reset cleanly to all-5-on instead of silently mismapping.
- `EpFlowchart.jsx` + `EpFlowchartPage.jsx` — intro/summary narrative rewritten around the new order ("Plan 2 uses a Disclaimer Trust... Plans 3–5 fund a GST-exempt trust..."; "Plans 2, 3, and 4 preserve more shelter than Plan 5").
- Backend tests (`test_iter74`, `test_iter63`, `test_phase48`, `test_phase46`) — all updated to the new authoritative order. 26/26 pass. One pre-existing MC test failure (`test_mc_outcome_distributions_present_and_shaped`) is unrelated to this iter.

The `PlanFlowchart` fallback-toggle + fallback-callout logic still gates on `plan.key === "disclaimer_roth"` (key-based, not number-based), so the Plan 2 card correctly inherits both.

Tests (iteration_77, 100% pass): backend 26/26; frontend confirmed 5-in-order rendering, correct toggle labels, `flow-p2-fallback-toggle` present + `flow-p5-fallback-toggle` absent, and localStorage v1→v2 migration correctly discards stale state.


## 2026-02-17 — Client Report parity & UX cleanup (5-issue batch)

**Issue 1 — Spending guardrail on Client Report matches Monte Carlo tab.** The `MonteCarloBehaviorCard` on the Client Report has always exposed the same guardrail fields as the MC tab (enabled + cut %). Defaults now align: `grOn=false`, `grCut=10`. No divergence.

**Issue 2 — Halt conversions on Client Report now includes RESTART (`resume_after_positive_years`).**
- Added `haltResume` state to `ClientReport.jsx` (default 2, matching the MC tab). Persisted to `client_report_mc_halt_resume_v1`.
- `MonteCarloBehaviorCard.jsx` rewritten as a two-column halt-inputs card: **YoY drop threshold (%)** + **Resume after N positive years** (0 = permanent halt).
- MC + regime API payloads now send `conversion_halt.resume_after_positive_years: haltResume`. Backend `ConversionHaltSpec` (models.py:258) already validated this field — no backend change needed.
- Client Report halt defaults also aligned to MC tab defaults (`haltOn=true`, `haltDrop=25`, `haltResume=2`). Bumped storage keys to `client_report_mc_halt_v2` / `_drop_v2` so existing users get the new defaults on next load.

**Issue 3 — EP flowchart Client Report defaults changed to Plans 1, 2, 3 + comparison table.** `flowPlans` default was `{1,2,3,4,5: all true}`; now `{1: true, 2: true, 3: true, 4: false, 5: false}`. Bumped localStorage key to `client_report_flow_plans_v3` so the reset propagates cleanly. `flowCompareOn` remains defaulted to `true`.

**Issue 4 — Stale-MC banner on Client Report.** Added `mcResultSig` state (records the `sig||mcSig` that produced the current mcResult). When it diverges from the current `sig||mcSig` (edits to Plan Inputs, MC halt/guardrail toggles, etc.), an amber banner appears at the top of the MC behavior card: *"Monte Carlo inputs or plan data have changed since the last run — the results shown below are out of date."* Includes a **Rerun now** button (`cr-mc-rerun-btn`) that fires `runMc` on demand (and disables + shows a spinner while running). Auto-run debounce is preserved; the banner is the safety net for cases where the auto-run failed silently or the advisor hasn't opened the tab since editing.

**Issue 5 — Fallback toggle on Plan 2 now affects the entire Client Report page.** Previously the toggle only swapped the flowchart body inside `PlanFlowchart`; the H2 title, subtitle paragraph, and FetCalcTable stayed on Plan 2 data, so advisors reported the click "did nothing".
- `PlanFlowchart` accepts optional controlled `fallback` + `onFallbackChange` props (falls back to internal state on the standalone EP Flowchart tab).
- `EpFlowchartPage` lifts the state, and when engaged:
  1. Appends a **"Showing fallback — Plan 1 baseline"** pill next to the H2 (`cr-flow-fallback-h2-badge-2`).
  2. Swaps the subtitle paragraph (`cr-flow-subtitle-2`) to a fallback-narrative wrapper that quotes Plan 1's subtitle inline.
  3. Passes `showPlan` (= Plan 1's data) into `FetCalcTable` and the investable-wealth bridge — so every number below the flowchart also collapses to the Plan 1 baseline.
- `PlanFlowchart` still receives `plan={plan}` (Plan 2), so `isDisclaimer` stays true, the toggle button remains visible in the header, and the built-in Disclaimer-fallback callout at the bottom is unchanged (it reads `plan.metrics` deliberately to preserve the delta calculation).
- `cr-reset-isolation` continues to reset fallback before every PDF/DOCX capture, so printed reports always render Plan 2's central case.

Tests: EP flowchart backend suite 26/26 still green. Frontend compiles without new warnings. Preview screenshot confirms MC card shows both halt inputs (25 / 2).


## 2026-06-19 — Reviewer corrections: bracket snapshots, beneficiary-rate band, neutral framing

Advisor/reviewer punch-list on the Client Report. Nothing about the estate alternatives changed; the work was clearer educational framing plus four presentation/model fixes.

**1. Tax Bracket snapshots print page (default ON, advisor can switch off).**
- NEW `clientReport/BracketSnapshotsPage.jsx` — three bucket diagrams (first conversion year / the year RMDs begin / final conversion year) on ONE shared dollar scale, each with a caption table (ordinary taxable income, RMD included, conversion, marginal rate on the last dollar, federal tax on the conversion, blended rate, headroom to the next bracket).
- NEW `lib/brackets.js` — bracket geometry + `bracketFactsForRow()` extracted from `BracketVisualizer.jsx` so the interactive tab and the printed page share one source of truth.
- `bracketOn` state in `ClientReport.jsx` (localStorage `client_report_bracket_snapshots_v1`, default true); toggle lives in `MonteCarloBehaviorCard` (`cr-bracket-snapshots-toggle`). Page sits right after Taxes. Each SVG is wrapped in `data-docx-rasterize` so Word gets an image.

**2. Paired Monte Carlo label is now neutral.** "Trials Roth wins" → **"Trials with higher ending liquid wealth under Roth strategy"** (report page + MC tab card), tone de-colored, and both surfaces gained a "this measures ONE definition of success" note listing the competing definitions (liquid wealth, parents' taxes, after-tax inheritance, beneficiary rate exposure, tax/estate structure). The report does not rank strategies.

**3. Beneficiary tax-rate sensitivity (elevated, not buried).**
- Backend: `projection.heir_rate_sensitivity(cfg, rates)` + `POST /api/legacy/heir-rate-sensitivity` (`DEFAULT_HEIR_SENS_RATES = 14% / 26% / 41%`, always includes the modeled rate). The heir rate only feeds the post-death SECURE-10 horizon, so each branch projects ONCE and only the heir horizon is re-priced per rate (~0.2s round trip).
- Frontend: NEW `clientReport/HeirRateSensitivityPage.jsx` — modeled rate / band / **break-even beneficiary rate** KPIs plus a no-conversions vs with-conversions vs Δ table, and a "range of family possibilities, not a forecast of your children's careers" note. The Legacy page keeps a prominent `cr-legacy-heir-assumption` callout naming the 36% assumption.
- Default plan output: converting trails by ~$4.0M at a 14% heir rate, ties near 37%, and leads at 41% — and the with-conversions column is FLAT across the band because a fully converted IRA removes the heirs' rate exposure entirely.
- Tests: `backend/tests/test_heir_rate_sensitivity.py` (5) + `..._http.py` (5).

**4. `$NaN` fixed on the NPV rows (root cause).** `legacy.post_death_rows` are keyed by `year_after_death` (1..10), not a calendar `year`; `LegacyPage` discounted `p.year` → `NaN`. Now `heirYearOf()` derives the calendar year from the last projection year (used by both the NPV math and the heir chart's x-axis), the NPV block returns `null` if any term is non-finite, and `fmtUSD` returns "—" for non-finite input so no client report can ever print `$NaN`.

**5. Conversion-window labeling.** Assumptions page now shows **"Roth conversion window (permitted)"** and **"Roth conversions actually modeled"**; the Planned Roth Conversions KPI reads "Conversion window — actually modeled" with a "Permitted through YYYY" sub-label, plus a note when they differ ("Permitted window: 2026–2062. Actual modeled conversions: 2026–2049 (24 years)") explaining the IRA is exhausted first.

**6. Client Inputs Appendix printed $0 living expenses.** `expenses` and `income_streams` are line-item ARRAYS; the page read `expenses.annual` off an object that never existed. Rewritten with itemized tables (amount, frequency, annualized, active-year window, inflation/COLA) and an annualized first-year total; monthly amounts are annualized ×12. Funding order now reads from `withdrawal.funding_order`.

**7. Flat state rate flagged as a simplifying assumption.** Displayed with 2 decimals (3.99%, not "4.0%"), labeled "flat, held constant", with a `cr-assumptions-simplifications` box noting NCDOR lists 3.99% after 2025 with statutory mechanisms that can reduce it further, and that the heirs' 32%+4% rates are assumptions (pointing at the sensitivity page).

**8. Estate wording + non-ranking framing.** "GST exemption is wasted (forever)" → **"is not utilized (and cannot be recovered later)"** across the report, Estate tab, Advisor Info, White Paper, exemption-gauge narratives, and PlanFlowchart. The "Roth outside a GST trust loses decades of protected tax-free compounding" sentence is replaced with the reviewer's: "GST-trust ownership can preserve transfer-tax protection, creditor protection, governance, and multigenerational control, while subsequent income taxation depends on whether income is retained in or distributed from the trust." A prominent `EstateFramingNote` ("hypothetical educational illustrations, not ranked recommendations…") renders immediately before the first estate illustration and on the plan-comparison page.

Tests: iteration_82 report — 12/12 frontend checkpoints and 10/10 heir-rate backend tests passed. Post-change self-verification: 20 printed pages with footers sequential and `Page 20 of 20`, no "NaN" anywhere in the report text, DOCX export completes with zero console errors, bracket toggle off/on renumbers correctly and persists.

Test-suite maintenance: `tests/_golden.json` re-baselined (it predated the $500K cash default), heir-rate/state-rate assertions in `test_phase9/test_phase10` derived from config instead of hardcoded, and a 2-cent rounding tolerance in `test_phase39_state_tax.py`. Remaining pre-existing failures with stale hardcoded dollar expectations are catalogued in `/app/memory/known_test_debt.md`.


---

## 2026-06-19 (later) — Presentation copy rework, shortfall column, and the squished-PDF fix

Second half of the advisor punch-list. Four copy/presentation defects plus the real root cause of "squishy text" in the generated Client Report PDF.

**9. Presentation §3 is now a trade-off, not a winner.** `FundingOrderRationale` in `Presentation.jsx` dropped the driver-detection narrative ("The winning withdrawal order is … switching adds $3,091,009 …") and prints the advisor-authored trade-off copy verbatim: IRA-funded conversion tax creates a circular calculation and shrinks the balance available to convert; taxable-funded conversion tax erodes the survivor's step-up; higher Roth balances vs step-up is often a function of longevity — **there is no one-size-fits-all winner**. The 3-order comparison table stays (it is data); ★ is now defined as "highest projected after-tax legacy under these assumptions — a projection outcome, not a recommendation". The Legacy verdict box likewise names its metric ("On the after-tax-to-heirs measure…") and points at the beneficiary rate band.

**10. Closing statement replaced** in `Presentation.jsx`, `WhitePaper.jsx` §6.3 and `strategy/AggressiveStrategyExplainer.jsx`: "Never pay taxes early because an assumption produces a better result…" → "Be very careful before paying current taxes based upon projections of future returns and tax rates. Current taxes are real and reduce assets available to support your and your spouse's lifestyle. Current taxes are real and not refundable; assumptions are hypothetical…" (typos in the dictated copy corrected: linear/simplified/projection).

**11. Cash Flow "Surplus / (Shortfall)" never showed a shortfall — root cause.** `CashFlowPage` computed `income + withdrawals − expenses`; since withdrawals are sized to fill the gap, that always nets to ≈0. It now reads the engine's `cashflow.surplus` (funding income − spend need, i.e. BEFORE portfolio withdrawals), so 2026 correctly prints `($19,505)` in red, and the copy explains the gap is filled by the Withdrawals column.

**12. Squished text in the Client Report PDF — root cause + content fix.**
- `lib/pdf.js renderChunkedPdf()` was CLAMPING an over-tall section to the usable page height while keeping full width — a vertical squeeze (the Income & Expenses page was compressed to ~52% of its height). It now (a) draws at full width when the section fits, (b) scales proportionally and centers when slightly taller, (c) slices into page-height strips at full width when a section is more than ~18% too tall. Aspect ratio is never distorted again.
- Over-tall pages were split so nothing needs scaling at all: **Income & Expenses → 3 pages** (charts / milestone cash-flow / taxable income by source), **Legacy → 3 pages** (comparison / what the family nets / tax mechanics), **Inputs Appendix → 2 pages**. Each component takes the parent's `pageNo` and offsets it with a local `foot(i)` helper; `ClientReport` page math updated (`basePages = 14`, Legacy spans 3, appendix spans 2).
- Verified geometry: 19 printed pages, footers sequential `1..19 of 19`, every page inside the 720 × 1050 px printable box (only the Monte Carlo page is 3px over → a 99.7% uniform scale).
- `BracketVisualizer` / `BracketSnapshotsPage` SVG labels rendered as "% bracket" because JSX emitted the rate and the literal as two adjacent SVG text nodes; they are now single interpolated strings ("24% bracket").

**Duplicate-page cleanup.** Two parallel implementations of the beneficiary sensitivity landed in the same session (an inline `cr-page-legacy-sensitivity` page inside `LegacyPage` and the standalone `HeirRateSensitivityPage`). The standalone page won (it adds modeled-rate / band / break-even KPIs and an heirs'-IRA-tax column and renders nothing until data arrives); the inline duplicate and its memos were removed. The Legacy page keeps the `cr-legacy-heir-assumption` callout that names the assumption.

Verification: DOCX export 2.3 MB and PDF export 11.4 MB both complete with zero page errors and no failure toast; report text contains no "NaN", "undefined", "Roth wins" or "wasted".

---

## 2026-06-20 — Optimizer leader chip fix, Winner→Leader vocabulary, longevity + beneficiary-band deck pages

**13. Strategy Optimizer "Different from best" chip was permanently stuck — root cause.** It compared the sweep row's LABEL to the applied-strategy label. Sweep labels carry decoration the applied label can never reproduce (`" · IRA-1st"` from the 4D funding-order sweep; `"Aggressive 37% pre-SS (2026-2027), stop after"` versus the applied `"Phased: 37% · 2026–2027 → …"`), so equality never held and the chip stayed on even immediately after Apply. New `appliedMatchesSweepRow()` in `lib/strategyLabel.js` compares **structurally**: baseline vs enabled, target bracket + start/stop year for single rows, and the expanded per-year map for phased rows, plus the funding order. Verified in-browser: chip clears after Apply for both a single-bracket and a phased leader.

**Sweep staleness (the "doesn't recognize the Goal Preset" half).** `sweepInputStamp()` fingerprints only the inputs a sweep GRID depends on — it deliberately strips `roth`, `withdrawal.funding_order` and `optimizer` (those are the dimensions the sweep varies, and applying a leader rewrites them) and adds the sweep switches explicitly. A new banner (`data-testid="strategy-sweep-stale"`) with a Re-run button appears when phased schedules / funding-order sweep / horizon / IRMAA cap / annual cap / plan inputs changed since the run — which is what happens when a Goal Preset is first applied. Switching between two presets changes only the goal metric, which legitimately re-sorts the existing grid live (leader card, ranked table and AI lens all follow), so no re-run is demanded.

**14. "Winner" → "Leader" across all user-visible text and buttons.** `Apply leader`, `Different from leader`, the `Leader` column header, `Leader: …` / `Leader uses funding order …` tooltips, "Robust leader differs.", "The deterministic leader is robust.", "Leader of the deterministic sweep", "on leader" in the funding-order compare, "Trophy marks the leading cell per row", "Leading claim pair" (SS Report), "Apply the leader in your plan.", "auto-applies the leader", "Bold = column leader" and "the leader shifts with the …" (White Paper), "The sweep's deterministic leader …", "The leading window runs to …", plus the deck's "a trade-off, not a leader" / "no one-size-fits-all leader". Code identifiers, `data-testid`s and backend payload keys (`winner`, `best_funding_order`) intentionally unchanged.

**15. Deck: "Recommendations & caveats" split.** Sections 1-3 stay on `presentation-page-recs` (798px); sections 4-6 moved to a new `presentation-page-caveats` (390px). All 15 deck pages now sit inside the 720 × 1050 printable box, so nothing is scaled. The "Approx. N pages" hint formula was also corrected (it under-counted by 4).

**16. NEW deck page — funding order across longevity** (`presentation/LongevityTradeoffPage.jsx`, `POST /api/longevity/funding-order`). Backend `funding_order_longevity()` runs the SAME conversion strategy through all three funding orders at several survivor life expectancies (default −10/−5/0/+5/+10/+20 years, rows whose second death would precede the first death are skipped), tracking the projection horizon to the second death in both directions. The page tabulates after-tax legacy per order, the Taxable-first − IRA-first delta, and which order leads — for the default plan the leader genuinely flips (IRA-first at −5 years, Taxable-first from the planned lifespan onward), and the narrative adapts to whether a flip occurred. Toggle: `pres-toggle-longevity` (default ON).

**17. NEW deck page — beneficiary tax-rate band** (`presentation/BeneficiaryBandPage.jsx`) reusing `/api/legacy/heir-rate-sensitivity`: low / middle / high heir marginal rate, Δ column, heirs' inherited-IRA tax, break-even rate, and the "read this with your own family in mind" framing. Toggle: `pres-toggle-beneficiary-band` (default ON).

Also: `presentation/printPrimitives.jsx` extracted (Page/H2/H3/P/Sub) so deck pages can live in their own files. Tests: `tests/test_funding_order_longevity.py` (7) + `tests/test_iter84_longevity_http.py` (6) + heir-rate suites all pass; deck PDF exports at ~7 MB with no page errors.

---

## 2026-06-20 (later) — Goal-preset "Modified" state, Convert-or-Skip two-milestone model, broken-JSX repair

**18. Goal presets no longer deselect when a sweep switch is changed.** `matchesPreset()` demanded that the goal AND both sweep switches match, and `setOptimizerField()` deleted `preset_id` on any manual edit — so turning off "Include two-phase schedules" made Roth-Maximizer go dark, which read as "the app forgot my goal". New `presetMatchState()` treats the GOAL as the preset's identity: the card stays selected and is badged amber **"Modified"** with a note naming the divergence ("Goal still active, with two-phase schedules off. Click to restore the preset defaults."). Only changing the goal abandons the preset. Verified in-browser.

**19. Convert-or-Skip page was rendering literal JSX tokens.** The opening `{notReady ? (` of the loader ternary had been deleted at some earlier point, so the page displayed the text `) : (` and `)}` plus a permanent "Running both projections…" box above the real content. Restored the conditional. (The page was live in this state — worth a redeploy.)

**20. Convert-or-Skip now models BOTH milestones explicitly** (`TwoMilestoneCard`): net worth at the second death (pre-heir tax) and net worth at the end of the SECURE Act 10-year window, plus a bridge that reconciles exactly:
gross estate at 2nd death − estate settlement − income tax embedded in the inherited IRA at death = after-tax estate at 2nd death; + growth in the heirs' hands over the window (net of heir tax) = net worth at the end of the window. The embedded-at-death tax is derived (`gross − settlement − after_tax_estate_at_death`) rather than reusing `inherited_ira_tax`, which is the larger amount actually paid across the window; a footnote explains the difference. Default plan reconciles to the penny in both columns.

**21. Real-estate bug in "total wealth" figures.** Three components summed `(r.home || 0)` — the projection row field is `real_estate`, so home equity was silently dropped. Fixed where the label promises it: the Convert-or-Skip wealth chart ("Total investable + home equity") and its ending-net-worth KPI, and the deck's "Total Household Wealth (Cash + Taxable + IRA + Roth + Home)". Deliberately NOT changed in the Client Report Overview chart, whose printed footnote states it is investable-only and reconciles against the EP gross estate.

## 2026-08-20 — Convert-or-Skip in the Client Report + Presentation deck, optional advisor cover letter

**22. Convert-or-Skip is now printed in both client deliverables** (user request: Client Report after Overview / before Planned Roth; Presentation after Assumptions / before Sources of Income). Each surface gets TWO pages — a verdict page and a full year-by-year detail page:
- Client Report: `clientReport/ConvertSkipPage.jsx` (page 4) + NEW `clientReport/ConvertSkipDetailPage.jsx` (page 5).
- Presentation: `presentation/ConvertSkipDeckPage.jsx` + NEW `presentation/ConvertSkipDetailDeckPage.jsx`, inserted between `presentation-page-assumptions` and `presentation-page-income`.
- Shared math in `lib/convertSkip.js`: `buildMilestoneBridge` (existing) + NEW `buildYearDetail(withRoth, noRoth, heirRate)` → per-year conversion, both tax columns, single-year and cumulative tax delta, tax-adjusted net-worth gap, parity/break-even year, deepest shortfall.

**23. Two reconciliation decisions inside `buildYearDetail` (do not "simplify" these):**
- Year tax = `cashflow.income_tax + cashflow.medicare` (total burden), NOT income tax alone. `summary.lifetime_taxes` sums `total_burden`, so the cumulative column now ends EXACTLY on the "lifetime tax you pay" delta shown on the verdict page (−$1,841,389 on the default plan). Using income tax alone left an unexplained ~$18K gap between consecutive pages.
- Net worth is compared tax-adjusted: `cash + taxable + roth + real_estate + traditional × (1 − heir ordinary rate)`. Without the haircut the no-conversion column always "leads" because it still carries an unpaid tax liability. The final row's gap therefore tracks the verdict page's "after-tax estate at the 2nd death" delta (−$432K vs −$396K; the remainder is estate settlement + taxable step-up, which the bridge applies and this page does not).
- Parity KPI: the dip is searched ONLY within the conversion window (`i <= lastConvIdx`). The default plan is ahead from year one and only slips behind at the second death, so a naive "first year behind → never recovers" reading printed a false "Not within plan". States are: `aheadFromStart` / `breakEvenYear` / `neverBreaksEven`, with `endsBehind` handled in the prose.
- Row sampling: every conversion year always prints; other years thin out at the widest interval that keeps ≤ 30 rows (dashed rule marks a skip). Earlier 20-row cap produced a 13-year blind stretch where the cumulative column appeared to jump by $3M in one line.

**24. Optional advisor cover letter (default OFF).** `branding.cover_letter_on` + `branding.cover_letter` (persisted per-advisor in the existing localStorage branding blob). Toggle + textarea live in the Client Report branding card (`cr-cover-letter-toggle`, `cr-input-cover-letter`); NEW `clientReport/CoverLetterPage.jsx` prints as page 2 — letterhead, date, "Dear <household>," paragraph-split body, signature block from the advisor fields. Blank line = new paragraph.

**25. Client Report page numbering re-based.** `basePages` 14 → **16** (the two Convert-or-Skip pages) plus a variable `L = letterOn ? 1 : 0` offset applied to every fixed page number after the cover, and `cursor` starts at `12 + L`. Verified live: 21 contiguous pages with the letter off, 22 with it on, footers reading 1..22 with no gaps or duplicates. The stale "Approx. 9 pages" preview label was replaced (it had been wrong for months). Deck page-count estimate 10 → 12.


**26. Conversion-halt controls are now SHARED between the Monte Carlo tab and the Client Report, with new defaults (20% drawdown / resume after 1 positive year).** NEW `hooks/useSharedHalt.js` — a copy of the `useSharedGuardrail` pattern (single `shared_mc_halt_v1` localStorage key + same-tab `mc-halt:change` CustomEvent + cross-tab `storage` listener). Both `MonteCarlo.jsx` (was three plain `useState`s at 25 / 2) and `ClientReport.jsx` (was three separate `client_report_mc_halt_*` keys at 25 / 2) now consume the hook, so the printed report can no longer be generated on a different behavioral rule than the one the advisor is looking at on the MC tab. The old per-surface keys are abandoned, which also resets every existing browser onto the new 20 / 1 default. Verified live: fresh session = ON / 20 / 1; edit on MC tab (30 / 3) reads through on the Client Report; edit on the Client Report (15 / 0) reads back on the MC tab. Backend `ConversionHaltSpec` defaults were left alone — both surfaces always send explicit values.


## 2026-08-20 (later) — Editorial de-ranking pass, family objectives, present-value framing

The advisor's brief: *"strip out remaining language that implies there is a 'winner', move the family/objectives discussion earlier, make estate planning more visual, and separate the core client conversation from the advisor/technical appendix"* — plus render future-dollar differences in present value.

**27. Verbatim wording changes (all in the deck).** "Key Recommendations" → **"Planning Considerations"**; "Commit to the conversion window, but phase it" → **"Illustrated conversion approach and annual review considerations"**; "Best on the average path" → **"Result under the average-market assumption"**; "Best when markets disappoint (P10)" → **"Result under the P10 market assumption"** (combined case: "Leads under both the average-market and P10 market assumptions"). Also neutralized: "the recommendation passes the crash test" → "the illustrated schedule does not depend on favorable markets"; "wins if returns run near the long-term average" → "leads under the average-market assumption"; "avg #1 / P10 #1" → "leads on average markets / leads at P10"; the ★ on the regime table → a plain "highest success in this set" tag; "in one heroic year" removed; "the recommended strategy" → "the illustrated schedule" (wealth, legacy, conversions pages); the methodology line now adds that maximising a modeled quantity is not the same as recommending a course of action.

**28. Parity KPI carries its own qualification.** Label is now **"Tax-adjusted wealth under the modeled beneficiary rate"** on both convert-skip detail pages, with the modeled heir rate named in the sub-line, because the metric is only meaningful under this page's tax-adjusted definition of wealth.

**29. NEW "What are we planning for?" page** — `lib/objectives.js` (ten competing objectives, each with the family question behind it and which report pages speak to it) + `components/ObjectivesEditor.jsx` (tick + High/Medium/Watch) + `clientReport/ObjectivesPage.jsx` + `presentation/ObjectivesDeckPage.jsx`. Dollar-free by design. Stored on the SCENARIO as `scenario.planning_objectives = {key: "high"|"medium"|"watch"}` so the selection travels with a shared plan; the editor renders in BOTH toolbars and the scenario is the only source of truth (no state to sync). Unticked objectives print as "To discuss" rather than being hidden, and the note block switches wording once anything is ticked. `ObjectiveTensions` prints the three classic conflicts (lifetime tax vs step-up, children vs survivor flexibility, protection vs simplicity) so the page reads as a trade-off study. NOTE: objectives persist across sessions only when the scenario itself is saved — same as `custom_milestones`.

**30. IMPORTANT — `mcScenarioSig` now excludes `planning_objectives` as well as `optimizer`.** Ticking an objective would otherwise change the scenario signature and re-run every projection (projection ×2, funding order ×3, longevity, heir sensitivity) on each click. Call sites switched from raw `JSON.stringify(scenario)` to `mcScenarioSig`: `Presentation.jsx`, `ClientReport.jsx`, `RegimeComparePanel.jsx`, `StrategyStressTest.jsx` — all four must stay consistent or the deck falsely reports the stress/regime results as stale.

**31. HoldConstantBand on every comparative page** (`components/shared/PrintBlocks.jsx`, inline styles so it works in both print pipelines). States "Constant in both scenarios" / "Variable changed" so a reader cannot attribute an outcome to something that never moved. Live on: cr-convert-skip, cr-convert-skip-detail, cr-legacy, cr-heir-sens, deck-convert-skip, deck-convert-skip-detail, deck-legacy, deck-wealth, deck-robustness, deck-regimes, deck-beneficiary-band, deck-longevity.

**32. Present value everywhere** — `lib/pv.js` `makePv(scenario, override, rows)` → `{rate, start, at(year), of(v, year)}`. Default rate is the plan's own `projection.general_inflation` (3.0%) so PV can never contradict the model; advisor-adjustable sliders on both surfaces (`cr-pv-rate-slider`, `pres-pv-rate-slider`, the deck's persisted under `presentation_pv_rate_v1`). Added a paired "in today's $" column to: the convert-skip milestone bridge (each bridge line carries its own `pvYear` — second-death lines discount from the death year, window lines from the window end), both convert-skip detail tables (each year discounted individually, so `cumTaxDeltaPv` is a sum of discounted deltas, NOT the nominal total discounted once), the beneficiary band, the heir-rate sensitivity page, and the deck legacy page. Verified: at 3% the end-of-window difference is +$1,374,869 against a +$5,355,176 nominal; at 8% it falls to +$155,339.

**33. Estate visual-first** — `EstateThreeQuestions` (Where does the asset go? / What tax characteristics are preserved or lost? / What flexibility does the surviving spouse retain?) prints above the flow diagram on the first EP flowchart page, and the federal-estate-tax line-by-line table now carries an `AdvisorDetailTag` ("Advisor & attorney detail").

**34. "Advisor & Technical Appendix" divider** — `AppendixDividerBody` in PrintBlocks, wrapped by `clientReport/AppendixDividerPage.jsx` and `presentation/AppendixDividerDeckPage.jsx`. In the report it prints immediately before the first technical page (basis / EP flowchart / inputs appendix) and only when at least one of those is enabled (`techPages > 0`); in the deck it precedes Methodology & Disclosures.

**35. Client Report renumbering (again).** `basePages` 16 → **17** (objectives) plus the conditional divider page. Verified live: 23 contiguous pages with default toggles, 27 with the EP flowchart pages on, divider landing at page 21 immediately before the flowchart block.

**36. Deck bug fixed: the conversion window printed a blank stop year.** `Presentation.jsx` read `scenario.roth.stop_year`; the field is `end_year`. Now reads `end_year` with the old key as a fallback, and both the assumptions table and the Planning Considerations page distinguish the **permitted** window from the window **actually modeled** (derived from rows with `roth_conversion > 0`) — e.g. "converts $5,649,083 across 23 years, 2026–2048 (inside a permitted window of 2026–2062)".

**37. Print-height watch.** `cr-page-convert-skip-detail` grew to 1203px (nominal box 900px, ~277mm A4 limit ≈ 1050px) once it gained an 8th column, which would have clipped in the PDF. Fixed by `whiteSpace: nowrap` on numeric cells (they were wrapping to two lines), tighter row padding, MAX_ROWS 30 → 26 and trimmed prose — now 1013px, in line with the pre-existing `cr-page-taxable-income` (1022) and `cr-page-montecarlo` (1053). PRE-EXISTING overflow still unaddressed: `cr-page-flow-plan-*` run 1359–2026px.


## 2026-08-21 — Six advisor requests: page removals, opt-in objectives, hidden tab, flowchart plan picker, survivor account retitling, SECURE window chart

**38. "Convert or Don't Convert — the year-by-year drivers" REMOVED from both surfaces.** Deleted `clientReport/ConvertSkipDetailPage.jsx` and `presentation/ConvertSkipDetailDeckPage.jsx`, plus `buildYearDetail` / `sampleYears` / `MAX_ROWS` from `lib/convertSkip.js` (nothing else used them). The verdict page (`cr-page-convert-skip` / `presentation-page-convert-skip`) stays. Client Report `basePages` 17 → **15**.

**39. "What are we planning for?" is now OPT-IN, default OFF.** New `hooks/useObjectivesPage.js` (`shared_objectives_page_v1`, same event + storage sync pattern as `useSharedHalt`) — ONE switch in the ObjectivesEditor header (`<prefix>-include-toggle`) governs BOTH the report page and the deck slide. Page numbering uses an `O` offset alongside the cover-letter `L` offset. Verified: 21 pages off / 22 pages on with the objectives page landing at 3 and contiguous footers.

**40. Single-Year Optimizer tab trigger hidden** (`Planner.jsx`, per advisor request). The `TabsContent value="optimizer"` is deliberately LEFT MOUNTED so existing deep links keep working — only the navigation button is withdrawn. `SlidersHorizontal` import dropped.

**41. EP Flowchart tab gained a plan picker, shared with the Client Report.** New `hooks/useFlowPlans.js` owns the existing `client_report_flow_plans_v3` key (default Plans 1–3) and is consumed by BOTH `ClientReport.jsx` (replacing its private useState/useEffect pair) and `EpFlowchart.jsx`. The tab used to render all five plans unconditionally, so the two surfaces disagreed; now the flowchart stack AND the comparison table both filter to `visiblePlans`, and switching a plan on either surface updates the other live.

**42. IMPORTANT BACKEND FIX — the pooled taxable brokerage now sits on the SURVIVOR's line.** `_step_up_basis` pools every Taxable account into one blended-basis account at the first death (workbook convention). The anchor was hardcoded to `plan.taxable_ids[0]`, i.e. usually the CLIENT's brokerage — so when the client died first the whole balance kept growing for years on the "Client Taxable Brokerage" row of a dead person. The anchor is now the survivor's own taxable account (falling back to a Joint account, then the first). Also added `_living_owner_target()` so the annual surplus sweep credits an account whose ORIGINAL owner is still alive instead of resurrecting the decedent's row. Verified both directions (client-survives → TAXC, spouse-survives → TAXS) and all 34 tests in `test_phase20_routing_and_copula.py`, `test_phase43_excel_parity.py`, `test_heir_rate_sensitivity.py`, `test_funding_order_longevity.py` still pass — totals and lifetime tax are unchanged, only which row carries the dollars.
  - NOT changed: Traditional IRA and Roth balances still stay on the decedent's account row. Merging those would zero out `ROTS`/`ROTC` and break the per-owner conversion-routing feature and its tests (`test_spouse_conversions_land_in_spouse_roth`, `test_default_scenario_unchanged_totals`). Only the taxable pool was in scope.

**43. "After first death" focus now points AT the first-death year.** `lib/useGridPrefs.js` `detectMilestones(rows, scenario)` derives `firstDeath` from the household (`dob + life_expectancy`, earlier of the two) instead of the first `filing_status` change. The filing flip lands on the year AFTER the death (2057 vs the true 2056) and never fires at all when the survivor's filing status is configured as MFJ. Row-based detection is kept as a fallback and the result is clamped to the projected range. Call sites updated to pass `scenario`: `DetailCashflow.jsx`, `TaxDetail.jsx`, `Cashflow.jsx`. This is the same derivation `EpFlowchart.deriveDeathYears` already used, so the tabs now agree. Verified: focus window starts at 2056.

**44. NEW `components/SecureWindowChart.jsx`** — stacked bar of the heirs' account values across each year of the SECURE Act 10-year window, rendered on the Accounts & Cashflow tab below the Account Detail table. Driven by `legacy.post_death_rows` (buckets: `taxable_and_reinvested`, `inherited_traditional`, `inherited_roth`, `real_estate`, `cash`); buckets that are zero across the whole window are dropped from the stack, so a fully-converted plan correctly shows only Roth + real estate. Header shows year-1 → year-10 totals and the cumulative heir income tax paid over the window.



## 2026-08-21 (later) — Client Deck tab, today's-date defaults, scenario-specific basis caveat, editorial pass 3

**45. NEW top-level "Client Deck" tab** (`Planner.jsx`, `value="client-deck"`, sits immediately after Presentation, before Client Report) renders `<Presentation ... curated />`. The `curated` prop drops the four detail pages and the appendix divider — Sources of Income & Spending, Account Composition, Account Values by Year, Tax Cost Composition, `AppendixDividerDeckPage` — and adds a one-page `presentation/EstateComparePage.jsx` (plan comparison table + the three family questions), fed by `lib/epFlowchart.js` `buildEpFlowchartRequest` so the deck, the EP Flowchart tab and the Client Report can never disagree on death years / the 50-50 first-death split. Per advisor decision the curated deck KEEPS "Total Household Wealth Over Time" and "What Your Heirs Receive", and every optional page (recs, robustness, regimes, longevity, beneficiary band) still follows its own toggle — no auto-suppression. Download filename switches to `<client>-client-deck.pdf` / `.docx`; the page-count estimate uses a base of 7 (vs 12) plus 1 when the estate page resolves. Verified live: 13 pages with default toggles, PDF export produced `client-client-deck.pdf`.
  - Also removed a duplicate "Client name override" input in the Presentation toolbar that carried a second `data-testid="pres-input-client"`.

**46. Report date always defaults to TODAY** on all three surfaces (`clientReport/helpers.jsx`, `ssReport/helpers.jsx`, `Presentation.jsx`). `defaultBranding.presentation_date` was already today, but `loadBranding()` merged the saved localStorage blob over it, so a report generated weeks later printed the date the advisor last touched the settings. `loadBranding` now overrides `presentation_date` with `new Date().toISOString().slice(0,10)` after the spread. Verified: set 2020-01-15, saved, reloaded → back to today.

**47. `BasisTradeoffCaveat` (shared/PrintBlocks.jsx)** — shaded box that prints ONLY when the "Forgone 2nd step-up" line is < $1,000 for EVERY plan on the page (and ≥ 2 plans are shown). Live on the Client Report EP Plan Comparison page (`cr-flow-basis-caveat`) and the curated deck's estate page (`deck-estate-basis-caveat`). Reason (advisor): with little taxable brokerage left at the estate-planning point, Plans 2 and 3 look numerically identical and a client can wrongly generalize that funding a first-death GST trust with taxable assets never costs a second step-up.

**48. Editorial pass 3 (Client Report).** "This is the most consequential decision in the plan" → "This is one of the most consequential tax and legacy decisions modeled."; "…is where advisors earn their keep" → "…is the trade-off this section quantifies"; break-even KPI now reads "None in tested band" / "Conversion delta remains positive (or negative) across the tested beneficiary-rate range" instead of "None in band" / "One strategy leads throughout", and the tie sub-line is "The two strategies tie at this rate". NOTE: the advisor also flagged "Parity with not converting — Ahead from year one"; that KPI lived on the year-by-year drivers page deleted in item 38, so no change was needed (confirmed with the advisor).

**49. EP flowchart print pages now fit A4.** Item 44's split (diagram page + "— the arithmetic" page) left Plan 2 at 1118px against a ~1050px budget. `PlanFlowchart` now suppresses the in-card PLAN badge + title whenever `hideSubtitle` is set (the Client Report page H2 already prints "Plan N: <title>"), saving ~84px. Measured after the fix: plan-1 942, plan-2 1034, plan-3/4/5 981, every `-detail` page 900, compare page 900–930 — all inside the budget. Client Report with plans 1/2/3 + compare = 28 contiguous pages.

Testing: `test_reports/iteration_85.json` — 13/13 frontend checks pass, no console errors. Backend subset (`test_phase20_routing_and_copula`, `test_phase43_excel_parity`, `test_heir_rate_sensitivity`, `test_funding_order_longevity`) = 34 passed after the spousal IRA/Roth retitling.


## 2026-08-21 (evening) — Deck Page Picker, estate settlement cost → 0, shared branding store

**50. Deck Page Picker (`lib/deckPages.js` + `hooks/useDeckPages.js` + `presentation/DeckPagePicker.jsx`).** The curated Client Deck is no longer a fixed short list — the advisor ticks exactly which pages print. `DECK_PAGES` is the manifest (17 rows); `src` decides where each row's state lives: `"deck"` → this module's own selection map (localStorage `client_deck_pages_v1`, defaults = the original short deck), `"branding"` → an existing `presentation_branding_v1` flag (assumptions, robustness, regimes, longevity, beneficiary band, planning-considerations), `"objectives"` → the shared `useObjectivesPage` switch. Cover + Disclosures always print and are named in the card so the advisor sees the whole deck. `DECK_PAGE_SPAN = {recs: 2}` keeps the counter honest (planning considerations + caveats print two pages). Rows whose underlying run doesn't exist yet (stress test, regime comparison, estate projection) render disabled with an amber hint. In `Presentation.jsx` every page is now gated by `show(k)` = `curated ? !!deckPages[k] : true`, so the FULL Presentation deck is unchanged and only the Client Deck is curated. Verified live: counter matched the rendered `.pdf-page` count on 4 different selections (13/13, 14/14, 17/17, 12/12), selection survives reload, PDF export still produces `client-client-deck.pdf`.

**51. `hooks/usePresentationBranding.js` — branding is now a shared, write-through store.** Found by the testing agent (iteration_86): `branding` was plain `useState` persisted ONLY by the "Save settings" button, and the Presentation tab and Client Deck tab mount two separate `<Presentation>` instances. So a branding-backed picker row (e.g. Assumptions) switched off in the deck reverted the moment the advisor switched tabs, and never reached the other tab. The hook now owns `defaultBranding` / `loadBranding` / `saveBranding` and writes to localStorage + fires `presentation-branding:change` on EVERY change, with a cross-tab `storage` listener — same pattern as `useDeckPages` / `useFlowPlans`. "Save settings" stays as an explicit confirmation. Verified: deck toggle → localStorage `include_assumptions:false` → Presentation tab's `pres-toggle-assumptions` unchecked and the page dropped → returning to the deck keeps it off.

**52. Estate settlement cost default 1% → 0%** (advisor request). Changed in `backend/defaults.py` (`legacy.estate_settlement_pct`), the shared `backend/user_defaults.json`, AND the master's saved `advisor_defaults` doc in Mongo — the per-advisor doc WINS over both, so leaving it at 1% would have kept the Plan Inputs field (`estate-settlement-pct`) showing 1%. Golden baseline refreshed (`python tests/golden_snapshot.py save`).

**53. BACKEND FIX found while re-running the suite — the spousal retitling was too greedy.** Item 46's `_apply_spousal_rollover` block moved EVERY account type of the decedent onto the survivor's same-type account, including Taxable. That silently defeated `merge_basis_at_first_death=False` (whose whole point is to keep the stepped-up lot separate so the survivor spends it first) — `test_basis_merge_off_yields_lower_lifetime_tax` asserted a strictly lower lifetime tax and got equality. The retitle loop is now limited to `("Tax-Deferred", "Tax-Free")`; Taxable / Real Estate remain governed by `_step_up_basis` and the merge flag. Two tests updated for the intended new behaviour: `test_phase20_http_public.test_projection_spouse_heavy_routes_to_ROTS` (check per-owner routing while both spouses are alive, then assert the combined Roth survives) and `test_phase44_http_endpoints.test_projection_merge_toggle_affects_taxes` (pins `funding_order` to "Cash → IRA → Taxable → Roth"; `/api/defaults` can return an advisor-saved Taxable-first order that leaves nothing to step up, making the merge toggle a legitimate no-op).

Testing: `test_reports/iteration_86.json` — 13/14 frontend checks passed; the failing shared-flag check was fixed by item 51 and re-verified by hand (deck ↔ Presentation round trip). Backend: `test_phase44_basis_merge_and_giving`, `test_phase20_routing_and_copula`, `test_phase43_excel_parity`, `test_phase44_http_endpoints`, `test_phase20_http_public`, `test_golden_snapshot` all green. Full-suite run still shows the pre-existing failures catalogued in `known_test_debt.md` plus the `test_phase33_pin_auth` lockout cascade (that file intentionally posts wrong PINs; clear with `db.auth_config.deleteOne({_id:"master_lockout"})`).

**54. Pre-redeploy verification (2026-08-21 evening).** Preview smoke-verified after the session's changes: settlement input reads 0, Client Deck = 13 pages, Client Report = 21 pages, Presentation = 17 pages, zero page errors. `deployment_agent` initially FAILED on one blocker — `.gitignore` was ignoring `.env` / `.env.*` / `*.env` (and `git ls-files` confirmed neither .env was tracked), which contradicted the comment directly above it; those three lines were removed and the re-run returned PASS. Also added the production origin `https://roth-retirement-tool.emergent.host` to `CORS_ORIGINS` in `backend/.env` and to `_default_origins` in `server.py` — production is same-origin today so it worked without it, but a split API domain would have broken silently.


## 2026-08-22 — Monte Carlo guardrails on the deck, three-way linkage, new Convert-or-Don't-Convert wording

**55. Monte Carlo behavioral rules on the Presentation / Client Deck (`presentation/McBehaviorCard.jsx`).** The spending guardrail and the drawdown conversion-halt now have controls on the deck as well as the Monte Carlo tab and the Client Report, all three reading and writing the SAME shared stores (`useSharedGuardrail` → `shared_mc_guardrail_v1`, `useSharedHalt` → `shared_mc_halt_v1`). Verified three-way: change the cut % on any one tab and the other two show it immediately, switches included. Advisor chose "controls plus a short one-paragraph note, no chart" — the deck does NOT run its own simulation.

**56. `shared/McBehaviorNote.jsx` — the printed disclosure.** Renders nothing when both rules are off; when either is on it prints which rules are active with their live values, and states plainly that the rules shape the Monte Carlo dispersion only while the deterministic tables hold spending and the conversion schedule fixed. It calls the shared hooks ITSELF rather than taking props, so the printed note can never disagree with the switches (no prop threading through a dozen pages). Placed on: deck Assumptions page, deck Perspective & Caveats page, both Convert-or-Don't-Convert pages, and the Client Report Assumptions page (advisor picked assumptions + caveats + convert-or-don't-convert).

**57. New Convert-or-Don't-Convert wording, one copy for both surfaces (`lib/convertSkipCopy.jsx`).** Advisor-supplied text: paragraph 1 kept, NEW second paragraph added ("The following illustration is based upon the assumed heir tax rates which you have provided… while projected conversion benefits may seem large in amount, they are often relatively small gains in percentage terms"), and the closing footnote sentence replaced with the longer step-up-in-basis passage (hypothetical conversion gains are already adjusted for the projected loss of a second basis step-up where capital gains are realized in the parents' lifetimes to fund conversion tax). Typos in the supplied text fixed with the advisor's approval ("genration" → "generation", double period). The Client Report's amber verdict box ("Read both numbers, not just one" / "The two milestones disagree") was DELETED at the advisor's instruction, and its old intro sentence ("Every figure on this page comes from running your plan twice") replaced, so both surfaces now read identically. `ConvertSkipNarrative` / `ConvertSkipFootnote` take the calling page's own print primitives as props (`P`, `Sub`) because the report and the deck style them differently.

Testing: `test_reports/iteration_87.json` — 11/11 frontend checks pass (three-way linkage both directions, notes appear/disappear with the toggles and track live values, both page texts, no page-height regressions, PDF export, no console errors).


## 2026-08-22 (later) — % of plan column + Sequence-of-Returns stress test

**58. "Δ as % of plan" column on the Convert-or-Don't-Convert bridge** (both surfaces, `pctOfBase` exported from `clientReport/ConvertSkipPage.jsx`). Every bridge row now shows the delta as a share of the DON'T-CONVERT figure on that same row, so a $2.3M swing is read against the $35.5M estate it came from. Zero-base rows (estate settlement at 0%) print a neutral grey "—" rather than a meaningless percentage. The deck's extra lifetime-tax row carries the column too.

**59. Sequence-of-returns stress test — the math (`backend/sequence_stress.py`, `POST /api/sequence-stress`).** The engine now accepts `cfg["return_path"]` = `{start_year, equity_share, equity_returns[]}` (`Plan.return_path`, `_parse_return_path`, `_equity_for_year`, `_grow_balances(..., eq)`); a shocked year grows a market account at `w x equity_return + (1-w) x its own flat return`, so only the equity sleeve moves and cash / the residence keep their own rates. Absent the key, nothing changes — golden + Excel-parity confirm it.
  - Four scenarios: early bear (first N years), late bear at the end of the projection, late bear at the end of the CONVERSION window (anchored to the last year money is actually converted, not the permitted end — usually 2049, and the row collapses when the two anchors coincide), and a seeded volatile path drawn in the advisor's band.
  - Two variants each, per the advisor: **mean-preserved** (recovery years lifted so the path compounds to exactly (1+r)^N — with no cashflows it lands on the identical balance, which is asserted in a test, so any surviving difference IS sequence risk) and **raw** (bear years bite uncompensated = a genuinely worse market).
  - Every path runs twice, with and without the conversion schedule, reporting lifetime tax saved, ending portfolio, heirs' after-tax delta, worst single portfolio year, whether the portfolio ever runs dry, and a Δ against the flat plan for each.
  - Finding worth remembering: on the default plan the conversion benefit roughly **doubles** when the bear market lands late ($6.65M saved vs $3.20M flat) and **reverses** in an uncompensated early bear (heirs −$1.75M) — converting at the top and then watching the market fall is the one case where the hedge argument fails.

**60. Sequence risk UI.** New top-level **Sequence Risk** tab after Monte Carlo (`components/SequenceRisk.jsx`) with advisor-editable inputs (bear severity, early/late leg lengths, volatility band) in a shared store (`hooks/useSeqParams.js`), 3 KPI cards, a line chart of the four paths against the plan assumption (`sequenceRisk/ReturnPathChart.jsx`) and the paired-column results table (`sequenceRisk/SequenceRiskTable.jsx`, mean-preserved block vs raw block). The same table + chart print as an optional Client Report page (`cr-seq-toggle` → `cr-page-sequence-risk`) and an optional deck page (`pres-toggle-sequence-risk` / picker row `sequence_risk`, branding flag `include_sequence_risk`, default OFF and disabled until a run exists).

Testing: `test_reports/iteration_88.json` — backend 10/10 pytest + 19/19 endpoint checks; frontend 12/12 including page fit (CR 900, deck 737), contiguous 1..22 report numbering and a real 9.8 MB PDF export. Two cosmetic nits it raised ("9 return paths" copy, green "—" cell) are fixed.

**61. Legacy-first is now the default Planning Goal** (advisor request). Set in FOUR places because the later ones win: `frontend/src/lib/optimizerPrefs.js` (`DEFAULT_INCLUDE_PHASED` flipped to `true` so the fallback matches the preset exactly instead of rendering as "Modified", plus a `DEFAULT_PRESET_ID`), `backend/defaults.py` (new `optimizer` block in DEFAULT_SCENARIO), `backend/user_defaults.json`, and the master's saved `advisor_defaults` doc in Mongo (which had `roth_maximizer` selected). Verified live: Plan Inputs → Planning Goal shows Legacy-first badged **Active**. Golden + Excel-parity + sequence-stress suites still pass (23 tests) with the new config key present.

**62. Taxable lifetime gifts — Stage 1 (core engine), 2026-06.** Extended the giving module to model gifts ABOVE the annual exclusion that consume the unified credit under standard unified §2001(b) mechanics (gifts added ONCE to the tentative-tax base; full exclusion + DSUE as shelter — no double count). `backend/estate.py` `project_estate` already accepted `adjusted_gifts_first_death`/`adjusted_gifts_second_death` (portability outcome). New this stage in `backend/projection.py`: `_parse_taxable_gifts` (validates `cfg["giving"]["taxable_gifts"]` = `[{year, amount, donor∈Client|Spouse|Joint}]`), `_drain_for_gift` (drains donor-owned Taxable→Joint→other, then Cash; consumes basis pro-rata), `_first_decedent_donor`/`_split_adjusted_gifts` (maps per-donor cumulative gifts onto first/second death by DOB+life-expectancy), and `_cfg_adjusted_gifts` (cfg-only helper for the funding-order estate path). Main loop accumulates `adj_gifts_by_donor` + a per-year `taxable_gift_rows` ledger and adds gifted principal to the family gift pot. Result exposes `giving.taxable_gifts` **only when gifts exist** (key absent otherwise → golden byte-identical). Wired into `_fo_fed_estate_tax_no_trust` (funding-order comparison) and the `/api/estate/analyze` endpoint (`EstateAnalyzeRequest` gains `adjusted_gifts_first_death`/`_second_death`, default 0.0). Joint gifts split 50/50. Tests: `tests/test_taxable_gifts.py` (7 pass — §2001(b) hand-computations + projection parser/cap/attribution); golden snapshot byte-identical. NOT YET DONE: Stage 2 (§1015 carryover basis income-tax side) and Stage 3 (reporting page + UI inputs for entering taxable gifts).

**63. Taxable lifetime gifts — Stages 2 & 3 + UI + Estate callout, 2026-06.** Completed the four follow-ups to item 62.
(1) **§1015 carryover basis (Stage 2, backend)**: `_drain_for_gift` now returns the carryover basis that travels with a gift (pro-rata basis on taxable lots; full face on cash). `run_projection` tracks `gift_pot_basis`; `_aggregate_results` adds `giving.carryover_basis {pot_basis, embedded_gain, heir_ltcg_rate, ltcg_owed_at_sale, pot_after_tax}` — present ONLY when a pot exists (golden byte-identical). Embedded gain = pot − carryover basis; heirs owe LTCG at the heir rate (no §1014 step-up).
(2) **Gift Input UI (PlanInputs.jsx)**: dynamic "Taxable Gifts (above the annual exclusion)" editor inside the Lifetime Giving card — add/remove rows with year/amount/donor (Client/Spouse/Joint), running "requested" total + capped-at-liquidity note. Writes `scenario.giving.taxable_gifts`.
(3) **Unified Credit report page (Stage 3)**: new `clientReport/UnifiedCreditPage.jsx`, toggle-gated in ClientReport.jsx (`unifiedCreditOn`, localStorage `client_report_unified_credit_v1`, default OFF, numbered `totalPages − (statutoryOn?1:0)` so it slots before the statutory appendix). Shows cumulative exclusion vs taxable gifts, per-spouse exclusion consumed vs remaining, DSUE (net of first-decedent gifts), the §1015 carryover trade-off, and the counterfactual estate-tax-saved (taxWithout − taxWith vs the combined shelter → $0 when under exclusion; flat-40% federal, notes the Estate-tab engine incl. state tax is authoritative).
(4) **Estate tab impact callout (Estate.jsx)**: passes `adjusted_gifts_first/second_death` (from the projection's taxable_gifts summary) into `/api/estate/analyze`; runs a no-gift counterfactual projection (rebuilds BOTH Y1 and Y2 balances) + estate analysis and renders `estate-gift-impact` (tax without/with/saved) with a net-to-heirs narrative that ADDS the after-tax family gift pot to the with-gifting side. Also added carryover-basis row to BasisStepUpPage (totals now foot) and fixed the exemption-gauge headroom sign.
Testing: iteration_28 (backend 16/16 HTTP+offline; frontend found 3 HIGH + 3 minor consistency defects, all fixed) → iteration_29 (frontend 6/6 fixes verified 100%, both under- and over-exclusion regimes). Golden snapshot byte-identical throughout. Remaining OPTIONAL polish: the Estate exemption-gauge combined shelter doesn't net lifetime gifts out of DSUE (the Unified Credit page does) — minor display inconsistency, left as-is.

**64. Deterministic dollars by regime (RegimeCompare), 2026-06.** Added actual single-path dollars beneath the Monte Carlo regime table on both the in-app panel and the printed report.
- **Backend** `projection.py::regime_deterministic_compare(cfg)`: for each named market regime (the 6 PRESETS excluding the 'custom' no-op passthrough, matching the MC table) re-runs the FULL deterministic projection under that regime's return+inflation profile (via `apply_market_scenario`, not a scaling of baseline) for BOTH the with-conversions and no-conversions (`roth.enabled=False`) branches. Returns per regime: net worth at 2nd death, after-tax wealth to heirs at the SECURE-window end (both branches), and the conversion delta in nominal + today's dollars (each regime discounted by its own CPI from the heir-deliver year back to start). Cached per config-hash (14 projections). New route `POST /api/regime-deterministic-compare` in routes/planning.py (10/min, advisor/share gated). `runRegimeDeterministicCompare` in api.js.
- **Frontend**: `RegimeComparePanel.jsx` fetches deterministic data alongside the MC run and renders a 2nd table 'Deterministic outcomes by regime' (data-testids mc-regime-det-table/section/row-<id>/worst-<id>); `clientReport/RegimeComparePage.jsx` renders the same table beneath the MC table (cr-regime-det-*), with a caption noting these are single-path runs and the MC table is the dispersion around them. Worst regime (lowest after-tax to heirs) is flagged so the report never shows only the baseline; baseline regime carries a 'Your baseline' badge. `ClientReport.jsx` fetches + threads `regimeDetData` to the report page.
- **Tests**: `tests/test_phase64_regime_deterministic.py` (5) — historical_avg row == plain baseline projection EXACTLY, delta nominal/today consistency, regime differentiation, cache identity, custom excluded. Testing agent iteration_30: backend 22/22 (incl. HTTP through ingress), frontend 100% (panel + printed report, printed page 723/1037px — no clipping). Fixed the one design issue (custom row duplicated the baseline) by excluding 'custom'. Golden snapshot byte-identical.

**65. Two-Way Sensitivity — Heir Rate × Market Regime, 2026-06.** New printed report page + in-app view.
- **Backend** `projection.py::two_way_sensitivity(cfg)`: evaluates `heir_rate_sensitivity` under every named market regime (6, custom excluded — reuses the deterministic-regime machinery; each regime projects both branches ONCE and re-prices the heir horizon per rate). Rates 0/10/14/26/36/41% (0% = charitable-beneficiary, labeled "0% (charity / no income tax)"). Returns a rate×regime `matrix` of the conversion delta in after-tax wealth to heirs (nominal) + per-regime break-even rate via `_break_even_from_points` (interpolated crossover; extrapolates the last segment linearly + flags "extrapolated" when no crossover in 0–41%; returns null/n-a for a flat/wash column). Verbatim caption stored in `TWO_WAY_CAPTION`. Cached per config-hash (`_TWO_WAY_CACHE`; 12 projections). Route `POST /api/two-way-sensitivity`. `runTwoWaySensitivity` in api.js.
- **Frontend**: `TwoWaySensitivityPanel.jsx` (heat-grid, green=converting wins / amber=not / neutral for ~$0 wash, rows=heir rates, cols=regimes, break-even row underneath, verbatim caption) rendered in `MonteCarlo.jsx` below the regime panel after an MC run; `clientReport/TwoWaySensitivityPage.jsx` prints the same grid (auto-included after the Heir Rate Sensitivity page; page 900px < 1050 frame, no clipping). `ClientReport.jsx` fetches `twoWayData` on sig change and threads it through.
- **Tests**: `tests/test_phase65_two_way_sensitivity.py` (7) — shape, charity label, verbatim caption, break-even interpolation + extrapolation + flat-column n/a, regime differentiation, cache identity. Testing agent iteration_31: backend contract/caching/auth all pass; found + fixed a CRITICAL crash (`onTwoWayResult is not defined` in MonteCarlo.jsx — added the prop to the signature; Planner doesn't pass it so it's a safe no-op via optional chaining) that had blanked the MC tab; frontend then 100%. Post-test polish applied: neutral coloring + "$0" for wash cells, n/a break-even for flat columns, and updated the stale iteration-30 HTTP test (custom excluded → 6 rows). Golden byte-identical.

**66. Two-way sensitivity polish + report page-fit, 2026-06.** Four follow-ups.
- **(1) Report overflow fixed**: `AssumptionsPage.jsx` (table row padding 6px→3px + tightened the two note boxes) now renders 958px (was 1130); `MonteCarloPage.jsx` (chart 175→145px + reduced margins) now ~1013px (was 1053). Both under the ~1037px PDF frame; testing agent confirmed scrollHeight==clientHeight (no clipping).
- **(2) Surface headline**: backend `two_way_sensitivity` now returns per-regime `delta_at_modeled` + top-level `wins_at_modeled`/`n_regimes`; the in-app panel, printed report page, and deck slide all show "Conversions win in N of 6 market regimes at your modeled heir rate of X%".
- **(3) Client Deck slide**: new `presentation/TwoWaySensitivityDeckPage.jsx`; `Presentation.jsx` fetches `twoWay` (mirrors heirSens), adds a `branding.include_two_way` toggle (`pres-toggle-two-way`) + a curated-deck picker row (`lib/deckPages.js` `two_way` entry, availability `!!twoWay`), and renders the slide after the beneficiary-band page.
- **(4) Break-even band**: each grid cell now has a green/amber left rail (4px in-app, 3px print) by conversion-delta sign, so each regime column's winning zone reads as a shaded band that flips at its break-even rate; ~$0 cells get no rail.
- Testing agent iteration_32: 4/4 tasks verified 100% (page heights, headline text match, 36/36 rails per grid across panel/report/deck flipping at break-even, deck toggle disabled-until-loaded). Fixed the one MEDIUM gap it found (curated Client Deck had no picker control for the new slide) by adding the `two_way` manifest row. Remaining: 2 LOW cosmetic notes (intentional rail-width difference; pre-existing recharts -1 mount warning). Golden byte-identical; backend two-way pytest 7/7.
